# MMN Perception Engine Agent Rules

本文件是所有接手 MMN Perception Engine 的研发智能体必须先阅读的项目规则入口。任何智能体都只是 MMN 研发执行者，不是 MMN 产品定义者。

## 必读文档

接手任务前必须先阅读：

1. `README.md`
2. `docs/mmn-strategy-model.md`
3. `docs/HANDOFF_QWEN_AGENT.md`
4. `README_DEPLOY.md`
5. `docs/研发档案/README.md`

## 核心原则

- MMN 的核心资产是汽车营销策略模型、工作流、知识库、输出规范和模型调度逻辑。
- Qwen、DeepSeek、OpenAI、客户私有模型等都只是底层模型供应商或执行引擎。
- 用户侧不得把底层模型名称作为 MMN 核心卖点展示。
- 新增模型分析或策略输出能力时，必须遵守 MMN 多模型调度、RAG 优先、人工复核和策略结论交付规则。
- 事实类问题必须优先查询结构化数据库、RAG、官方来源或已导入资料，模型只负责解释和表达。

## 产品边界

- MMN 与 Credo 是两条独立产品线，不得混成一个系统。
- MMN 面向车企品牌、市场部门、车企代理公司和汽车垂直 MCN，聚焦营销策略、达人蒸馏、内容生产、品牌传播与报告输出。
- Credo 面向经销商、销售顾问和门店管理者，聚焦本品卖点、竞品对比、销售攻防话术与到店接待辅助。
- 两者可以共享底层车型知识库、竞品资料和部分数据结构，但前端产品、用户权限、功能路径、交付话术、页面信息架构和商业化模型必须独立设计。
- 禁止把 Credo 的门店销售功能直接塞进 MMN，也禁止把 MMN 的营销策略、达人蒸馏和品牌传播功能直接塞进 Credo。

## 开发边界

- 不允许硬编码 API Key、Cookie、Token、账号密码或固定公网 IP。
- 不允许绕过验证码、登录限制、付费墙、平台风控、人机验证或平台权限。
- 不允许直接在服务器手工改代码。
- 不允许删除或覆盖用户数据、原始导入文件、数据库文件和研发档案。
- 不允许大规模重构无关模块。
- 每次只修改当前任务直接相关的最少文件。

## MMN 页面设计强制约束

- 所有 MMN 页面迭代必须先读取并继承当前产品的信息架构、左侧导航、一级/二级模块命名、功能边界和既有视觉 Token；现状是设计输入和验收基线，不得凭通用模板重新定义。
- 除非用户明确要求“重构”，不得新增、删除、迁移、合并、拆分或重命名任何一级/二级功能模块，不得改变左侧导航的分组、顺序、入口归属和路由语义，也不得把 Credo 或其他产品能力引入 MMN。
- 默认任务是“在现有布局内完成视觉优化”：只优化审美、视觉层级、排版、间距、色彩应用、组件一致性、状态反馈、可访问性和细节品质；不得以视觉优化为由改变信息架构或功能边界。
- 新样式必须复用既有视觉 Token。若确需补充 Token，必须先证明既有 Token 无法表达需求，并保持向后兼容；未经用户明确批准不得替换、删除或重新定义既有 Token。
- 涉及 UI/UX 的任务必须同时使用 `frontend-design`、项目内 `.codex/skills/ui-ux-pro-max/SKILL.md` 与 `.codex/skills/taste-skill/SKILL.md`。三者只提供视觉与体验建议；与 MMN 既有结构或 Token 冲突时，本节约束和现状优先。
- `design-taste-frontend` 主要用于营销页、落地页和适合视觉重塑的页面；它明确不负责仪表盘、数据表格和高密度产品界面。遇到 MMN 决策驾驶舱、数据表格或多步骤产品流程时，只继承其审计、反模板化、可访问性和交付前检查原则，不得套用其落地页布局、技术栈或动效默认值。
- 页面文案必须沿用既有模块命名与 MMN 品牌营销语境，不得擅自改名、创造同义入口或混入底层模型供应商与 Credo 的产品话术。

### 页面迭代与验收顺序

1. 修改前记录目标页面的现有桌面端截图，并盘点该页面的导航归属、模块名、交互入口和实际使用的视觉 Token。
2. 仅在现有页面结构内提出视觉优化方案；逐项声明哪些内容保持不变，若方案触及信息架构、导航或功能边界则停止实施并请求用户明确授权。
3. 实现时优先复用现有组件与 Token，保持所有路由、入口、模块名和功能行为不变。
4. 完成后必须在桌面端真实运行页面并截图核验；至少检查目标页面、左侧导航完整状态、关键交互状态，以及无布局溢出、遮挡、错位或视觉回退。
5. 对照修改前后截图复核，并执行与改动范围相称的功能检查。未取得桌面端截图证据或截图暴露问题时，不得提交。
6. 提交说明必须列出：视觉改动、明确未改动的结构/模块、复用的 Token、桌面端截图路径、功能检查结果和残余风险。

## MMN 功能开发质量闭环

所有 MMN 功能新增、缺陷修复、重构和行为变更，默认强制使用以下已安装技能：

- `superpowers:test-driven-development`
- `superpowers:systematic-debugging`
- `superpowers:verification-before-completion`
- `agent-skills:code-review-and-quality`
- `agent-skills:doubt-driven-development`

技能规则与本文件冲突时，以 MMN 既有产品边界、信息架构、功能命名和用户当前明确指令为最高优先级。功能开发必须完成“理解现有架构与影响范围 → 最小改动 → 测试/构建 → 真实浏览器验收 → 独立代码审查”的完整闭环，不得跳步。

### 1. 理解架构与影响范围

- 修改前必须阅读任务直接相关的架构文档、现有实现、测试、调用方、路由、数据流、配置与近期差异；代码发现优先使用项目知识图谱的 `search_graph`、`trace_path` 和 `get_code_snippet`，不足时才使用文本搜索。
- 先写清楚目标行为、保持不变的行为、影响文件、调用链、数据与权限边界、潜在回归面和验证方案，再开始修改。
- 禁止凭猜测修改代码。无法复现、证据不足、调用关系不清或不理解既有约束时，必须继续取证或明确说明未知项，不得用“应该”“大概”“可能是”替代验证。
- 遇到缺陷、测试失败、构建失败或异常行为时，必须先按 `systematic-debugging` 完成复现、证据采集、根因追踪和单一假设验证；没有根因调查不得提出或实施修复。
- 非平凡决策必须按 `doubt-driven-development` 明确约束并进行反证；行为性结论可由 TDD 的失败测试承担第一轮反证，架构或跨边界结论必须接受独立、对抗式审查。

### 2. 测试驱动与最小改动

- 新功能、缺陷修复、重构和行为变更必须遵循 RED → GREEN → REFACTOR：先写能够因目标行为缺失而正确失败的测试并实际观察失败，再写使其通过的最少生产代码，最后在测试持续通过的前提下整理实现。
- 未观察到预期失败的测试不构成有效回归测试；测试一开始就通过、因语法或环境错误失败，必须修正并重新完成 RED 阶段。
- 每次只验证一个假设、只实现当前需求所需的行为；禁止捆绑无关重构、顺手清理、未请求的新能力或新的依赖。
- 配置文件、生成代码或一次性原型若确实无法采用测试优先，必须先取得用户明确同意，并提供替代的可重复验证方法。
- 任何改动都不得擅自改变既有信息架构、左侧导航、一级/二级模块、模块命名、路由语义、功能边界或视觉 Token；需要改变时必须停止实施并取得用户明确授权。

### 3. 测试、构建与真实浏览器验收

- 实现后必须执行与改动范围相称的定向测试、相关回归测试、语法/类型/静态检查和完整构建；不能用某一项通过推断其他项通过。
- 验证命令必须在本轮完成声明前重新执行，读取完整输出与退出码，明确报告通过数、失败数、警告和未运行项。历史结果、局部结果或其他代理的口头结论不得作为完成证据。
- 涉及前端、页面、用户交互或前后端联动的改动，必须启动真实应用并在真实桌面浏览器中验收目标路径；检查关键交互、网络请求、控制台错误、页面状态和数据结果。UI 改动还必须保留桌面端截图并按本文件的页面验收规则复核。
- 浏览器页面能打开不等于验收通过；必须实际操作受影响功能并核对预期结果。无法完成浏览器验收时，只能报告“未完成/受阻”，不得宣称功能完成。

### 4. 独立代码审查

- 测试、构建和浏览器验收完成后，提交前必须对实际 diff 进行独立审查。审查者不得只复述实现者结论，必须从正确性、可读性、架构、安全、性能和验证证据六个维度主动寻找问题。
- 非平凡改动优先使用与实现上下文隔离的独立审查者；若当前运行环境无法提供真正独立上下文，必须明确标注为降级审查，不得把自审包装成独立审查。
- 审查必须先看需求与测试，再看实现；所有发现按 Critical、Required、Optional/Nit 分类。Critical 和 Required 未解决前不得提交；修复审查问题后必须重新运行受影响测试、构建和浏览器验收。
- 审查必须检查是否出现功能逻辑泄漏到共享模块、近重复帮助函数、无依据的静默降级、文件继续失控膨胀、秘密信息泄露、未验证外部输入、性能无界操作或 MMN 产品边界漂移。

### 5. 完成声明与证据

- 严格执行 `verification-before-completion`：没有本轮新鲜、完整、可复现的验证证据，不得使用“完成”“修复”“通过”“可发布”或任何同义表述。
- 最终交付必须列出：架构与影响范围结论、实际改动、RED 失败证据、GREEN/回归测试结果、构建结果、真实浏览器验收步骤与截图、独立审查结论、已修复问题、未验证项和残余风险。
- 若任一门禁未通过，必须报告真实状态与具体阻塞条件；不得通过弱化措辞、隐藏失败、引用旧日志或信任代理报告来绕过门禁。

## Auto Consulting OS 方法论约束

- 所有汽车策略、市场研究、机会地图、认知诊断、内容策略、BF、周报和报告任务必须先完整读取 `docs/mmn-auto-consulting-os.md`。
- `develop-auto-marketing-strategy` 是 MMN 汽车行业核心方法论；`management-consulting` 只补充问题树、假设验证、选项取舍和压力测试，`market-research` 只补充研究设计、多源交叉验证和证据透明。通用方法不得覆盖汽车购车决策判断。
- 所有数据、图表与结论必须显式区分事实、推断、假设和未知，并保留来源、时间、样本、车型版本、适用边界、推理链和补证计划。
- 机会地图与主策略必须经过来源独立性检查、多源交叉验证和反向压力测试。两个模型读取同一证据不算多源；平台曝光、互动、情绪或热度不能单独证明购买机会。
- 每项策略必须输出真实选项与明确取舍、推荐路径、执行动作、领先/结果指标、验证阈值、观察窗口、停止条件和不做事项。缺少关键字段时只能标记为待补证或待人工确认。
- 禁止用 SWOT、PESTLE、五力、漏斗、4P 等通用框架堆砌，用模型一致性、置信度或机会分替代证据强度，或用平台指标替代同预算同场景下的候选车型、购买理由、购买阻力、竞品替代与转化判断。

## 标准工作流

1. 先说明本次将检查哪些文件。
2. 先给修改计划，再改代码。
3. 本地修改和本地测试。
4. 写入研发档案。
5. 通过基础检查后再提交 GitHub。
6. 服务器只在用户明确要求“发布到服务器”时更新。

## 必测项

根据改动范围至少执行：

- `python3 -m py_compile server.py`
- 前端脚本语法检查
- 关键接口测试
- 本地 `http://127.0.0.1:8765/` 打开验证
- 涉及云端时执行部署文档中的健康检查

## 研发档案

每次功能新增、问题修复、流程优化，都必须在 `docs/研发档案/` 新增一份研发记录。研发记录应使用“MMN研发团队”视角，写清楚研发日期、版本号、需求背景、问题分析、技术方案、功能实现、修改模块、测试结果、版本变更说明和后续计划。
