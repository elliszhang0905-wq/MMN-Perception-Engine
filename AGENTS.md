# MMN Perception Engine Agent Rules

本文件是所有接手 MMN Perception Engine 的研发智能体必须先阅读的项目规则入口。任何智能体都只是 MMN 研发执行者，不是 MMN 产品定义者。

## 必读文档

接手任务前必须先阅读：

1. `README.md`
2. `docs/mmn-strategy-model.md`
3. `docs/HANDOFF_QWEN_AGENT.md`
4. `README_DEPLOY.md`
5. `docs/研发档案/README.md`

## 核心原则

- MMN 的核心资产是汽车营销策略模型、工作流、知识库、输出规范和模型调度逻辑。
- Qwen、DeepSeek、OpenAI、客户私有模型等都只是底层模型供应商或执行引擎。
- 用户侧不得把底层模型名称作为 MMN 核心卖点展示。
- 新增模型分析或策略输出能力时，必须遵守 MMN 多模型调度、RAG 优先、人工复核和策略结论交付规则。
- 事实类问题必须优先查询结构化数据库、RAG、官方来源或已导入资料，模型只负责解释和表达。

## 产品边界

- MMN 与 Credo 是两条独立产品线，不得混成一个系统。
- MMN 面向车企品牌、市场部门、车企代理公司和汽车垂直 MCN，聚焦营销策略、达人蒸馏、内容生产、品牌传播与报告输出。
- Credo 面向经销商、销售顾问和门店管理者，聚焦本品卖点、竞品对比、销售攻防话术与到店接待辅助。
- 两者可以共享底层车型知识库、竞品资料和部分数据结构，但前端产品、用户权限、功能路径、交付话术、页面信息架构和商业化模型必须独立设计。
- 禁止把 Credo 的门店销售功能直接塞进 MMN，也禁止把 MMN 的营销策略、达人蒸馏和品牌传播功能直接塞进 Credo。

## 开发边界

- 不允许硬编码 API Key、Cookie、Token、账号密码或固定公网 IP。
- 不允许绕过验证码、登录限制、付费墙、平台风控、人机验证或平台权限。
- 不允许直接在服务器手工改代码。
- 不允许删除或覆盖用户数据、原始导入文件、数据库文件和研发档案。
- 不允许大规模重构无关模块。
- 每次只修改当前任务直接相关的最少文件。

## 标准工作流

1. 先说明本次将检查哪些文件。
2. 先给修改计划，再改代码。
3. 本地修改和本地测试。
4. 写入研发档案。
5. 通过基础检查后再提交 GitHub。
6. 服务器只在用户明确要求“发布到服务器”时更新。

## 必测项

根据改动范围至少执行：

- `python3 -m py_compile server.py`
- 前端脚本语法检查
- 关键接口测试
- 本地 `http://127.0.0.1:8765/` 打开验证
- 涉及云端时执行部署文档中的健康检查

## 研发档案

每次功能新增、问题修复、流程优化，都必须在 `docs/研发档案/` 新增一份研发记录。研发记录应使用“MMN研发团队”视角，写清楚研发日期、版本号、需求背景、问题分析、技术方案、功能实现、修改模块、测试结果、版本变更说明和后续计划。
