# MMN Perception Engine 发布记录

## 2026-07-20｜v1.2.6—v1.2.8 云端演示一致性交付候选

- 交付状态：已推送 GitHub `main` 并完成 ECS 生产部署与业务流验收，可由负责人进行最终确认。
- 发布版本码：`beta-1.02-20260720-cloud-demo-consistency-6`。
- 对应档案：`2026-06-27_v1.2.6_MMN车型资产月度撞库机制.md`、`2026-06-27_v1.2.7_试用账号模型分析权限优化.md`、`2026-06-27_v1.2.8_品牌车型归属规则污染修复.md`；本次新增合并收口档案 `2026-07-20_v1.2.6-v1.2.8_云端演示发布一致性收口.md`。
- 月度车型资产撞库：修复脚本与当前租户化 `vehicle_assets` 表的冲突键不一致问题，统一按 `org_id + edition + platform + model_name` 幂等写入；入库前复用品牌、车型标准身份规则，保留原始载荷、哈希、运行记录和报告。
- 试用账号模型分析权限：分析类 POST 路径由显式白名单管理；未列入白名单的写入类接口继续默认仅管理员可用，未扩大试用账号的数据管理权限。
- 品牌车型归属污染：前后端统一保留乐道 L60、银河 L6、智己 L6/LS 的完整品牌车型规则；裸 `L60` 不再自动认领为乐道；同时修复极氪、荣威标准身份能源类型与 canonical key 不一致。
- 模型结果防污染：车型分析结果进入资产库前再次执行完整身份门禁；即使分析结果把裸 `L60` 标为乐道，也会改为“待人工确认”并清理同名旧污染记录。
- E7X 完整数据恢复：从原始 Excel 重新生成五车完整数据集，恢复 207 条逐车型属性记录、9 个分平台声量字段、7 个平台整体 NSR 及全网/垂媒车主口碑/抖音三类属性来源；不再使用只含 E7X 属性和竞品均值的手工压缩中间资产。
- 全车型防串库：所有全局车型选择入口统一经过车型上下文；有对应产品评价数据时重绑定该数据集，没有对应数据时清空上一车型的行数据、汇总指标和属性基准并明确显示暂无数据。发布门禁自动遍历销量预警车型，并验证从无数据车型切回已注册车型后原数据可恢复。
- 同车型旧缓存升级：浏览器不再只凭“当前绑定车型名相同”跳过刷新；后端数据版本、记录数、车型集合、属性来源、分平台声量或平台 NSR 任一变化，都会自动用完整服务器数据升级本地旧缓存。
- 生产数据热加载修复：产品评价规范路径在每次请求时重新解析，部署脚本在版本数据资产同步后强制重启数据读取服务并等待健康，避免容器先启动时绑定旧兼容路径。
- 生产代理恢复：数据读取服务重启健康后同步重启 Nginx，使其重新解析应用容器地址，并从代理内部验证健康接口，避免应用健康但公网持续 502。
- 统一数据治理：新增 `MMN_DATA_ROOT` 与模块路径注册表；本地统一保存于 `data/`，服务器统一保存于 `/app/data`，原始导入与处理资产分区保存并记录 SHA-256。部署前服务器备份现在包含逐文件清单、归档校验值，并在宿主机与 `/app/backups` 各保留一份。
- 数据保护：未重建、清空或覆盖 `data/commercial_demo.db`；月度同步兼容性使用临时数据库验证。当前数据库与 2026-07-18、2026-07-19 两份备份中的项目快照均无 E7X 逐条产品评价行，快照状态一致。
- 本地验证：完整 Python 测试 `430/430` 通过；发布门禁 Python 专项 `147/147` 通过；JavaScript、数据优先浏览器检查、桌面与 390 像素全表面检查通过。覆盖 19 个客户页面、8 个管理视图，并真实点击 E7X 后断言五车数据集、207 条记录、15 个属性、3 类属性来源、9 个分平台声量字段、7 个平台 NSR、竞品配置及卖点车型一致；另将同车型浏览器缓存降级为旧版后，桌面与移动端均自动恢复完整服务器数据；`failed: []`、`runtimeErrors: []`、`failedResponses: []`，整页横向溢出为 0。
- 备份验证：本地完整数据备份 `backups/data-governance/local_data_20260720_185850.tar.gz`，SHA-256 `e6f41909e3878edae1fafce044b779c0e5b5e39cc071a60f45be7da79c5d6a94`，附逐文件清单；本轮最终服务器发布前数据备份 `/opt/mmn-perception-engine/backups/mmn_backup_20260720_224830.tar.gz`，SHA-256 `fad93cc5f1d4eec3e2a6514df8d7d0088aa88b49805ecb6716b9d544575abd0c`，附逐文件清单，并已同步副本到容器 `/app/backups`。
- 生产部署：GitHub `main` 运行提交 `9257486`；ECS 健康接口返回 `beta-1.02-20260720-cloud-demo-consistency-6`，应用、任务、数据库、缓存、调度与反向代理六个容器均正常。
- 生产浏览器验收：完整 82 项桌面与 390 像素门禁通过，`failed: []`、`runtimeErrors: []`、`failedResponses: []`；E7X 仍为 207 条记录、5 车、15 个属性、3 类属性来源、9 个分平台声量字段和 7 个平台 NSR，逐一切换无数据车型后未复用上一车型数据，切回 E7X 可恢复完整资产。
- 生产权限与污染验收：真实试用账号调用 `/api/ai/model-identities` 返回 HTTP 200；`乐道L60`、`银河L6`、`智己L6` 归属正确，裸 `L60` 返回“待人工确认 / UNKNOWN / low”。同一账号调用月度销量刷新接口返回 HTTP 403；生产库中裸 `L60` 仅保留 1 条安全记录。部署后日志未出现 `Traceback`、HTTP 5xx 或 `BrokenPipeError`。
- 发布结论：GitHub 推送、ECS 部署、数据备份、试用账号分析权限、管理员隔离、车型切换防串库与品牌车型污染清理均已完成；剩余动作仅为负责人最终业务确认。
- 回滚：代码通过对应提交的 `git revert` 回滚；本批次不包含数据库迁移或数据覆写，无需数据回滚。生产部署时仍必须保留发布前代码归档、`.env` 与 `/app/data` 备份。

## 2026-07-20｜决策驾驶舱与博主蒸馏交付闭环

- 最终版本：`beta-1.02-20260720-cockpit-creator-2`；功能提交 `b805069`，导入资产发布修复提交 `ddaab3b`，均已推送 GitHub `main`。
- 决策驾驶舱：车型线索看板跟随统一车型上下文；管理层简报与同一周度市场批次绑定；增加周度官方数据刷新、周二至周五补抓、月度销量预警刷新；新能源预警按尺寸与统一新能源组计算；模型治理合同覆盖驾驶舱各输出表面。
- 博主蒸馏：导入任务以导入、蒸馏、分析、交付四阶段持久化，支持失败原因、原文件重试和完整交付校验；新导入达人会自动发布到脚本生成选择器，不再停留在能力卡孤岛。
- 脚本交付：支持达人、抖音/视频号/B站/小红书、品牌、车型、传播重点和标题输入；输出开头钩子、完整口播、字幕重点、画面建议，支持修改后重生成、复制和 Word 导出；生成过程保留进度、失败原因与重试状态。
- 本地验证：完整 Python 测试 `421/421` 通过，最终修复专项 `28/28` 通过，管理层和车型线索前端合同通过，差异及敏感信息检查通过。
- 生产业务验证：周度简报返回 4 条可追溯事实和自然周/月内累计双周期；销量预警返回 8 台上汽车型、22 款同市场车型，执行新能源统一分群与 25%/50%/80% 阈值。
- 生产达人验证：`超哥超车` 导入任务 `blogger_import_8b888a6c065441919155312f0825edb4` 完成，500 条样本、30 天选题库及完整能力卡交付；脚本任务 `creator_script_017ac22166f64f9e88ec6d33fc82a8ed` 完成，进度 100%，自然表达质检通过且 Word 可正常打开。
- 生产全表面验证：桌面与 390 像素共 19 个客户页面、8 个管理视图全部通过；`failed: []`、`runtimeErrors: []`、`failedResponses: []`，整页横向溢出为 0。
- ECS 最终重建时间：2026-07-20 03:41（Asia/Shanghai）；6 个服务运行，核心服务健康。发布窗口精确 HTTP 5xx、Traceback、BrokenPipeError、应用 ERROR 均为 0；3 条 499 为浏览器门禁快速切页主动取消请求，不是服务失败。
- 数据保护：服务器原有 `.env`、`commercial_demo.db` 和 `sales_warning_cycles.json` 均保留；最终 `/app/data` 为 197 MB。整批发布前回滚点为 `backups/mmn_source_pre_20260720_032628.tar.gz`、`backups/mmn_env_pre_20260720_032628.env`、`backups/mmn_backup_20260720_032650.tar.gz`；修复前回滚点为 `backups/mmn_source_pre_hotfix_20260720_034051.tar.gz` 与 `backups/mmn_backup_20260720_034052.tar.gz`。
- 公网正式验收入口：`http://121.40.60.90`。独立域名 HTTPS 的备案、云侧放行和证书问题仍是既有外部基础设施风险，本次未修改其他产品路由。

## 2026-07-16｜beta 1.02 T周期卖点匹配与白皮书产品证据

- 发布范围：T-45至T+120上市周期、集团卖点映射、标签级竞品选择、初步营销匹配度、产品白皮书上传、双模型复核、页码原文硬校验、产品证据10分计分和服务端持久化恢复。
- E7X 口径：2026-05-08首发预售、2026-05-29正式上市为T0；2026-07-16为T+48销售转化期。
- 真实文件验收：《AUDI E7X 产品白皮书 V260410.pdf》307页全部可读；共同验证结果包含P8外观、P106配置等逐页证据。
- 界面验收：1440像素三栏完整显示且无横向溢出；390像素页面宽度与视口一致；长引句改为可展开原文。
- 本地验证：发布闸门通过，114项后端测试、前端专项测试和真实浏览器检查均无失败项；刷新后P8证据与产品证据10/10稳定恢复。
- 版本码：`beta-1.02-20260716-whitepaper-evidence-1`。
- GitHub：提交 `a7e9541` 已推送 `main`；ECS 于 2026-07-16 06:59（Asia/Shanghai）完成归档部署。
- 云端备份：代码 `backups/code_before_a7e9541_20260716_065811.tar.gz`；运行数据 `backups/mmn_backup_20260716_065901.tar.gz`。
- 公网验收：首页与版本化 `app.js` 均为 HTTP 200；`/api/health` 返回 `beta-1.02-20260716-whitepaper-evidence-1`；未登录读取产品白皮书证据正确返回 401。
- 生产实测：E7X 白皮书 307/307 页可读，返回 `dual_model_verified` 并持久化；6 个服务运行，应用与 Web 近 10 分钟错误日志均为 0。
- 回滚：使用本次部署前代码归档与数据备份，再执行 `MMN_SKIP_GIT_PULL=true bash deploy.sh`。

## 2026-07-14｜beta 1.02 社媒与数据智能增量整包发布

- 发布范围：品牌传播采集进度与商用车隔离、平台 NSR 指纹矩阵、社媒趋势后台任务及竞品品牌车型选择、本品正向热度基准、风险内容气泡、懂车帝销量新鲜度、抖音热点实体识别、小红书内容排名与雷达、趋势数字避让和对应发布门禁。
- GitHub：主功能提交 `23a7f8e`；销量持久化卷热修提交 `28a9ba2`；均已推送 `main`。
- 本地验证：JavaScript 11/11、Python 175/175、最终发布门禁专项 85/85；真实浏览器 `failed: []`、`runtimeErrors: []`。
- 版本：`beta 1.02`；健康版本码：`beta-1.02-20260714-social-data-1`。
- ECS 发布目录：`/opt/mmn-perception-engine`；使用提交归档同步并执行 `MMN_SKIP_GIT_PULL=true bash deploy.sh`。
- 第一轮备份：`backups/code_before_23a7f8e_20260714_022425.tar.gz`、`backups/mmn_backup_20260714_022531.tar.gz`。
- 上线验收发现 Docker 持久化卷遮挡镜像内销量 feed，公网仍显示 2026 年 5 月；未隐瞒该问题，增加发布脚本的数据资产同步与回归测试后重新发布。
- 最终备份：`backups/code_before_28a9ba2_20260714_022900.tar.gz`、`backups/mmn_backup_20260714_022931.tar.gz`。
- 最终验收：公网首页和 `/api/health` 通过，版本化 `app.js`、抖音/小红书新增资源和竞品品牌选择器已生效；`/api/sales-marquee` 仅返回 `2026-06`；本地与服务器关键文件哈希一致。
- 运行状态：`mmn-app`、`mmn-creator-worker`、`mmn-db`、`mmn-scheduler`、`mmn-web`、`redis` 共 6 个服务运行，近 10 分钟错误日志 0。
- 回滚：恢复 `code_before_28a9ba2_20260714_022900.tar.gz` 与同时间点 `mmn_backup_20260714_022931.tar.gz`，再执行 `MMN_SKIP_GIT_PULL=true bash deploy.sh`；若需回到本批次前，使用第一轮代码归档和数据备份。

## 2026-07-12｜beta 1.02 系统级迭代

- 迭代范围：2026-07-11 至 2026-07-12 连续开发成果，统一作为一次系统级版本发布，不再拆成零散页面热修。
- 产品定位：面向汽车品牌市场营销负责人的决策系统；以“真实 NSR → 垂媒竞品关系 → 机会判断 → 产品事实补证 → 策略动作 → Learning / Know-how 回流”为主链路。
- 驾驶舱升级：完成 E7X 真实数据驾驶舱、属性来源矩阵、竞品气泡、双车周趋势、产品证据缺口、策略选择和决策资产回流；移除重复的“NSR × 垂媒合并判断”展示层。
- 数据可信度升级：修复缺失与真实 0 混淆、整体/属性 NSR 串读、汽车之家重复解析、Excel 日期和富文本读取、社媒助手新旧字段兼容、小红书达人字段错位与竞品并列截断。
- 系统能力升级：补齐客户空间隔离、机会地图文档与任务归属、执行周期恢复、前端缓存按客户/版本/车型隔离、社媒趋势与达人蒸馏基础能力。
- 验证结果：Python 119 项、JavaScript 5 组、完整发布门禁、真实 Excel 对账和真实浏览器交互均通过；生产数据迁移保持汽车之家 1,200 条、懂车帝 2,067 条。
- 当前版本：`beta 1.02`；版本码：`beta-1.02-system-iteration-1`。下列条目保留为本次系统迭代的详细变更与验证记录。
- GitHub：主提交 `0481374 feat: release beta 1.02 system iteration` 已推送 `main`；服务器与该提交变更的 99 个文件哈希全部一致。
- 云端备份：`backups/code_before_beta_1_02_20260712_035556.tar.gz`、`backups/commercial_demo_before_beta_1_02_20260712_035556.db`；部署脚本另生成 `backups/mmn_backup_20260712_040139.tar.gz`。
- 云端验收：公网返回 `beta 1.02` / `beta-1.02-system-iteration-1`，六个容器正常，近 10 分钟应用异常 0；管理员空间可读 3,267 条垂媒关系，试用空间为 0，发布前后数据量一致。

## 2026-07-12｜决策驾驶舱合并判断区块隐去

- 按客户汇报界面要求，移除竞品走势下方“NSR × 垂媒合并判断”整块展示，不以 CSS 遮挡方式保留空白。
- 保留上方 NSR 原始证据、垂媒正反向走势、车型竞争气泡，以及下方产品证据缺口、策略选择和 Learning / Know-how 回流，数据计算链不变。
- 本地专项测试、JavaScript / Python 语法、差异检查和完整发布门禁通过；`failed: []`、`runtimeErrors: []`。
- 版本码：`beta-1.01-cockpit-hide-evidence-1`；云端代码备份：`backups/code_before_cockpit_hide_evidence_20260712_034654.tar.gz`。
- 云端采用无数据库变更的容器热更新；公网 HTML 与 JavaScript 均不再包含该区块或渲染函数，六个容器正常，近 5 分钟应用异常为 0。

## 2026-07-12｜全局数据读取审计与租户隔离修复

- 审计范围：产品评价汇总、垂媒正反向周数据、社媒助手内容/达人表、机会地图证据、执行闭环与浏览器缓存。
- 修复产品评价相邻 NSR 区块串读、空值被转成 0、Excel 日期序列未转换、富文本分段丢失、万/k/w 数值缩写误读和旧表空结果误成功。
- 修复汽车之家同一周被专用/通用解析器重复读取；生产库副本演练从 4,467 条精确收敛为 3,267 条，其中汽车之家 1,200 条、懂车帝 2,067 条，重跑幂等且零丢失。
- 修复社媒助手无标题新版导出，按“标题 → 大家都在搜 → 视频话题 → 所属合集”保留可追溯文本；修复小红书博主 ID/昵称/主页/简介/属地字段错位。
- 前后端统一保持缺失与真实 0 的区别；竞品来源按同口径均值比较，并列车型不截断，重复来源不虚增覆盖，界面统一使用“百分点”。
- 所有业务缓存、垂媒资产、机会地图文档/任务/复核、执行周期和策略资产按客户空间与版本隔离；旧管理员数据一次性安全认领，试用空间不得继承。
- 真实文件批量抽检：4 份产品评价表（2 份有效、2 份因缺少必要属性 NSR 被安全拒绝）、9 份垂媒表、12 份社媒助手表；有效数据合计 633 条产品属性、4,730 条垂媒关系及 3,582 条社媒内容/达人记录。
- 本地验证：Python 119 项、JavaScript 5 组、浏览器发布门禁全部通过；`failed: []`、`runtimeErrors: []`。
- 真实浏览器回归：E7X 207 条产品评价和汽车之家 1,200 条关系刷新后仍可恢复；小米YU7“辅助/自动驾驶”垂媒值保留为 `0.0%` 并进入三平台竞品气泡，控制台错误与警告均为 0。
- 云端发布前备份：`backups/code_before_data_audit_20260712_033326.tar.gz`、`backups/commercial_demo_before_data_audit_20260712_033326.db`。
- 前端资源与健康版本码：`beta-1.01-data-audit-1`。
- 云端发布：部署脚本另生成 `backups/mmn_backup_20260712_033426.tar.gz`；公网首页、健康接口和管理员认证通过，六个容器正常运行，近 10 分钟应用异常为 0。
- 生产迁移结果：垂媒关系 3,267 条，其中汽车之家 1,200 条、懂车帝 2,067 条；8 个导入批次与 8 条 Learning 完整保留。管理员历史数据已一次性归入 MMN 管理空间，试用空间读取 0 条，租户隔离验证通过。
- Docker BuildKit 在首次镜像解包收尾遇到损坏的父快照；新镜像已成功接管并返回新版本码，随后清理 3.417GB 损坏/无用构建缓存，运行容器未受影响。

## 2026-07-12｜E7X决策驾驶舱证据矩阵与竞品气泡升级

- 客户对象：汽车品牌市场营销负责人。
- 数据基准：《AUDI E7X等5车产品评价_0710_v2.xlsx》与《汽车之家上汽集团八车周对比次数正反向排名.xlsx》。
- 新增决策问题、真实候选车型集合、属性NSR来源冲突状态和属性级本品/竞品气泡。
- 保留现有驾驶舱页面设计、交互和颜色体系，不改变NSR计算口径，不重复采集全量数据。
- 本地验证：Python 84项、JavaScript 4组、发布门禁16项全部通过；`failed: []`、`runtimeErrors: []`。
- 前端资源：`beta-1.01-e7x-cockpit-1`。
- 云端发布：归档同步至 `/opt/mmn-perception-engine`，执行 `MMN_SKIP_GIT_PULL=true bash deploy.sh`。
- 云端备份：`backups/code_before_e7x_cockpit_20260712_012528.tar.gz`、`backups/mmn_backup_20260712_012530.tar.gz`。
- 云端数据（当时记录）：汽车之家周度正反向关系曾写入 2,400 条；本次全局审计确认其中 1,200 条为重复解析，修复后保留源表真实的 1,200 条。
- 云端验证：基础访问检查通过，健康接口返回 `ok: true`；六个运行容器正常，前端已加载 `beta-1.01-e7x-cockpit-1`，近10分钟未检出应用异常。

## 2026-07-11｜社媒趋势中心增量发布

- 新增导航：核心工作流 → 决策驾驶舱（可展开且仍可进入原页）→ 社媒趋势中心。
- 新增独立三平台 TikHub 聚合、热度/情绪/矩阵识别、证据追溯、双模型 Evidence QA、排行榜看板与 SQLite 快照。
- 本地验证：Python 76/76；社媒趋势单测 4/4；发布门禁 9/9，`failed: []`、`runtimeErrors: []`。
- 云端同步：归档文件同步至 `/opt/mmn-perception-engine`，执行 `MMN_SKIP_GIT_PULL=true bash deploy.sh`。
- 云端备份：`backups/code_before_social_trends_20260711_211644.tar.gz`、`backups/mmn_backup_20260711_211732.tar.gz`。
- 云端验证：`mmn-app`、`mmn-db`、`mmn-redis`、`mmn-creator-worker`、`mmn-scheduler` healthy；前端与快照表存在；最近应用异常 0 条。
- TikHub 真实验收：已恢复用户此前提供的完整密钥并仅写入本地/服务器后端 `.env`；账户接口 HTTP 200。
- 真实数据结果：智己 L6 共采集 52 条，其中抖音 16、小红书 20、微博 16；三平台均 HTTP 200、采集警告 0、置信度 1.0、证据可追溯。
- 真实快照：`6b0be0c2db4327f581a218c7`；模型复核快照：`006d1ab0841fa6ca0b626f24`。
- 模型复核：DeepSeek 已成功返回；服务器当前 Qwen/百炼请求被供应商拒绝，因此严格标记 `manual_required`，未伪装为双模型一致。
- 域名状态：`mmnsh.com` 仍受备案/入口策略限制；ECS 内部 Nginx 验证通过。


## 发布日期

2026-06-28

## 发布版本

beta 1.01

## 发布负责人

MMN研发团队

## 发布目标

将本地已完成的车型资产一致性修复、达人蒸馏入库、MMN页面输出能力、打法知识库来源提示和版本信息统一发布到服务器，使本地与服务器均进入 beta 1.01。

## 需求背景

本次发布解决以下问题：

- 车型库品牌和车型归属不稳定。
- 同一车型因空格、中英文、能源写法不同而重复出现。
- 蒸馏后的达人资产需要进入对应平台达人库。
- 功能页需要具备 MMN 分析输出能力。
- 打法知识库需要显示当前车型来源于策略驾驶舱。
- 每次更新必须形成正式文档。

## 本次变更

- 功能新增：人工结论学习页增加品牌 / 车型选择和 MMN 草案生成。
- 功能新增：打法知识库显示当前车型来源、品牌和车型。
- 问题修复：荣威 / 宝马 i5、极狐贝塔 S3、大众途观 L PHEV、ID.ERA 9X、极氪 / ZEEKR 等车型归一。
- 问题修复：垂媒正反向矩阵严格显示当前所选周期。
- 流程优化：蒸馏后的创作者画像进入对应平台达人库。
- 文档更新：新增 beta 1.01 研发档案，更新版本规则和 README。

## 影响范围

- 前端页面：策略驾驶舱、垂媒竞争格局、内容资产中心、打法知识库、人工结论学习、版本架构。
- 后端接口：`/api/health` 返回版本信息。
- 数据库与数据文件：不改变数据库结构；不清空既有数据。
- 定时任务：不改变既有调度逻辑。
- 部署脚本：沿用现有 Docker Compose 发布流程。

## 本地测试结果

- 静态检查：`python3 -m py_compile server.py` 通过。
- 静态检查：`new Function(app.js)` 通过。
- 功能验证：本地服务启动显示 `中国汽车营销引擎 beta 1.01 已启动`。
- 页面访问：首页返回 200。
- 版本验证：本地 `/api/health` 返回 `version: beta 1.01`、`versionCode: beta-1.01`。

## GitHub 版本信息

- 分支：`main`
- Commit：`80ab6e2 release: beta 1.01`
- Tag：`beta-1.01`

## 云端发布结果

- ECS 环境：阿里云 ECS，目录 `/opt/mmn-perception-engine`
- 部署时间：2026-06-29 00:06 左右
- 部署方式：由于服务器连接 GitHub 超时，使用本地 `beta-1.01` Git 归档包同步到服务器，并执行 `MMN_SKIP_GIT_PULL=true bash deploy.sh`
- 数据备份：`backups/mmn_backup_20260629_000646.tar.gz`
- 云端工作目录备份：已 stash `server-pre-beta-1.01-20260629-000332`
- 容器状态：`mmn-app` healthy，`mmn-db` healthy，`mmn-scheduler` running，`mmn-web` running
- 健康检查：`http://121.40.60.90/api/health` 返回 `version: beta 1.01`
- 公网测试地址：`http://121.40.60.90`
- 域名状态：`http://mmnsh.com` 当前被阿里云备案拦截，返回 Non-compliance ICP Filing 页面

## 回滚方案

回滚目标版本：

```bash
bash rollback.sh HEAD~1
```

如需恢复服务器发布前数据：

```bash
bash restore.sh backups/mmn_backup_20260629_000646.tar.gz
```

如需查看服务器发布前代码改动：

```bash
git stash show -p stash@{0}
```

## 发布结论

beta 1.01 本地与服务器 IP 版本验证通过。域名访问受备案限制影响，需后续处理备案或域名解析策略。

## 后续计划

- 处理 `mmnsh.com` 备案拦截。
- 建立发布后自动验证：GitHub commit、服务器 health、前端资源版本、容器状态、备份文件。
- 后续每次功能更新继续新增研发档案。

---

# MMN Perception Engine 发布记录补充

## 发布日期

2026-07-05

## 发布版本

beta 1.01

## 发布负责人

MMN研发团队

## 发布目标

将本地完成的内容能力采集、达人内容学习与蒸馏流水线、达人能力资产系统和研发智能体交接体系同步到 GitHub 与服务器演示环境，保证本地与云端演示版本能力一致。

## 本次变更

- 内容资产中心新增内容能力蒸馏库交互升级。
- 达人蒸馏从内容片段展示升级为达人 DNA 资产包、脚本模板、选题公式、30 天选题库、账号孵化建议和客户 brief 模板。
- 社媒助手导出文件导入规则升级，优先识别账号昵称、内容标题、正文描述和平台字段，避免把账号 ID 当成达人名。
- 新增 MMN 内容采集器桌面版相关研发档案。
- 新增研发智能体交接手册，并明确后续每次更新必须同步交接手册、交接流程和发布记录。

## 本地测试结果

- `python3 -m py_compile server.py` 通过。
- `node --check app.js` 通过。
- 本地 `/api/health` 访问通过。
- 内容能力知识库接口可返回猴哥说车相关样本和达人资产。

## 发布要求

本次同步必须包含代码、研发档案、交接手册和发布记录。服务器仍作为稳定演示环境，后续业务功能仍以本地为主开发与验证。

---

# MMN Perception Engine 发布记录补充

## 发布日期

2026-07-08

## 发布版本

beta 1.01

## 发布负责人

MMN研发团队

## 发布目标

将 MMN PPT Agent 工作流同步到 GitHub 与服务器演示环境，使 MMN 具备可编辑、可复用、可校验的商业咨询型 PPTX 生成基础设施。

## 本次变更

- 新增 `skills/mmn-consulting-pptx/SKILL.md`，沉淀 MMN 专用 PPT Agent 工作规范。
- 新增 `src/ppt-agent/`，以 PptxGenJS 作为可编辑 `.pptx` 主生成引擎。
- 新增 MarkItDown 资料解析脚本、python-pptx 读取检查脚本和一键运行脚本。
- 新增 Marp CLI 结构版演示稿生成能力。
- 新增 Mermaid CLI 流程图、逻辑图生成能力。
- 新增页面结构 JSON、预览图、自动校验报告和示例 PPTX 产物。
- 自动校验覆盖中文溢出、元素重叠、标题层级、图表可读性、品牌配色、页码、目录一致性和 PPTX 页数一致性。
- 更新研发档案和智能体交接手册。
- 优化 Dockerfile 与定时任务脚本，移除云端镜像构建对 apt 安装 curl/nodejs 的依赖，改用 Python 标准库完成健康检查和内部定时 POST 调用，降低服务器构建网络风险。

## 影响范围

- 新增独立 PPT Agent 报告输出基础设施。
- 不替换现有 MMN 首页、策略驾驶舱、内容资产中心和既有策略报告接口。
- 不改变数据库结构，不清空既有数据，不修改云端登录权限。

## 本地测试结果

- `bash scripts/run_mmn_ppt_agent.sh` 通过。
- PPT Agent 校验报告 `ok: true`，`issueCount: 0`。
- `python3 -m py_compile server.py` 通过。
- `node --check src/ppt-agent/generate_deck.mjs` 通过。
- `node --check src/ppt-agent/export_preview.mjs` 通过。
- `node --check src/ppt-agent/validate_deck.mjs` 通过。
- `skills/mmn-consulting-pptx/SKILL.md` 通过 skill 校验。
- `pnpm release:gate` 通过。

## 发布要求

本次同步必须包含代码、依赖锁文件、PPT Agent 示例输入、示例输出产物、研发档案、交接手册和发布记录。服务器发布后需执行云端健康检查并记录结果。

## 云端发布结果

- GitHub Commit：`a7bbfe5 fix: stabilize MMN Docker build`
- 服务器目录：`/opt/mmn-perception-engine`
- 服务器拉取版本：`a7bbfe5`
- 发布前数据备份：`backups/mmn_backup_20260708_163756.tar.gz`
- 容器状态：`mmn-app` healthy，`mmn-db` healthy，`mmn-scheduler` healthy，`mmn-web` running。
- 健康检查：`http://121.40.60.90/api/health` 返回 `ok: true`、`version: beta 1.01`。
- 公网基础检查：`bash scripts/test_mmn_cloud.sh http://121.40.60.90` 通过首页与健康接口检查。
- 服务器 PPT Agent 产物确认：`output/ppt-agent/mmn-strategy-deck.pptx`、`page-structure.generated.json`、`validation-report.json`、预览图、Marp 结构稿和 Mermaid 图均已存在。

## 发布结论

MMN PPT Agent 工作流已同步至 GitHub 与阿里云服务器。服务器当前运行版本健康，PPT Agent 代码、示例输入、示例输出和所有记录已完成同步。

---

# MMN Perception Engine 发布记录补充

## 发布日期

2026-07-10

## 发布版本

beta 1.01

## 发布负责人

MMN研发团队

## 发布目标

修复本机没有全局 Node.js 时发布门禁静默退出的问题，使本地开发环境可以直接完成语法检查、研发档案检查和浏览器检查，并将修复、交接说明与发布记录同步到 GitHub 和服务器。

## 本次变更

- `scripts/release_gate.sh` 不再因 `command -v node` 失败而在 `set -e` 下静默退出。
- Node.js 解析顺序统一为显式 `NODE_BINARY`、系统 `PATH`、本机内置运行时。
- `NODE_BINARY` 同时兼容绝对路径和 PATH 中的命令名。
- 显式配置无效时直接报错，不静默切换到其他 Node.js 版本。
- 新增对应研发档案，并更新研发交接手册。

## 影响范围

- 仅影响本地与服务器代码库中的发布门禁脚本和研发记录。
- 不改变 MMN 页面、后端接口、数据库结构、模型路由、定时任务或登录权限。
- 服务器部署仍沿用现有 Docker Compose 流程；部署前备份运行数据和当前代码归档。

## 本地测试结果

- 自动发现本机内置 Node.js：完整发布门禁 16 项检查通过，`failed: []`、`runtimeErrors: []`。
- `NODE_BINARY=node`：完整发布门禁 16 项检查通过。
- `NODE_BINARY=/path/to/node`：完整发布门禁 16 项检查通过。
- 显式无效 `NODE_BINARY`：退出码 1，并输出明确错误。
- Node.js 完全不可用：退出码 1，并输出明确错误。
- `bash -n scripts/release_gate.sh` 与 `git diff --check` 通过。

## GitHub 与云端发布结果

- GitHub Commit：`fb3dfdf fix: make release gate resolve bundled Node`。
- 服务器目录：`/opt/mmn-perception-engine`。
- 同步方式：GitHub `main` 推送后，使用 `fb3dfdf` 已提交文件归档同步到无 `.git` 的服务器目录，再执行 `MMN_SKIP_GIT_PULL=true bash deploy.sh`。
- 部署时间：2026-07-10 04:48（Asia/Shanghai）。
- 发布前数据备份：`backups/mmn_backup_20260710_044802.tar.gz`。
- 发布前代码归档：`backups/code_before_fb3dfdf_20260710_044756.tar.gz`。
- 容器状态：`mmn-app` healthy、`mmn-db` healthy、`mmn-scheduler` healthy、`mmn-web` running。
- 健康检查：`http://121.40.60.90/api/health` 返回 `ok: true`、`version: beta 1.01`。
- 公网基础检查：`bash scripts/test_mmn_cloud.sh http://121.40.60.90` 通过首页与健康接口检查。
- 文件一致性：服务器 `scripts/release_gate.sh` SHA-256 为 `cdc827a8b5e07d5f64d9ae5a0af61c36b6755c44e3003b73cabfb4cc730ac47e`，与本地提交一致。
- 发布后日志：应用、Web 与定时任务近 5 分钟未发现 `traceback`、`exception`、`critical` 或 `error`。

## 回滚方案

如发布后出现异常，恢复本次发布前的服务器代码归档，保留 `.env` 与运行数据，再执行 `MMN_SKIP_GIT_PULL=true bash deploy.sh`；如涉及数据问题，使用本次发布前生成的数据备份恢复。

## 发布结论

发布门禁 Node.js 运行时解析修复、研发档案、交接手册与发布记录已同步到 GitHub 和阿里云服务器；公网服务与容器状态健康。

---

# MMN Perception Engine 发布记录补充

## 发布日期

2026-07-10

## 发布版本

beta 1.01｜BF工厂 P0

## 发布目标

发布品牌商业化内容 Brief 生成与训练系统，打通“原始BF上传 → 结构化与溯源 → 历史样本检索 → 自适应BF生成 → 在线校正 → Word导出 → 最终版回流”的 MMN 商业闭环。

## 本次变更

- 新增 BF工厂与BF资产库一级入口，以及新建BF、种子范式快捷入口、人工业务字段校正、证据展示和Word导出。
- 新增 `bf_factory/` 领域模块、A–F六层JSON Schema、项目/客户隔离数据库、文件解析、标签、来源和版本记录。
- 探店、云评/口播、高质感摄影仅作为种子范式；未知和混合需求进入 `CUSTOM` 并动态组合章节。
- 优质自定义终稿可沉淀脱敏范式，并被后续相似需求实际召回；反例只用于风险，禁用样本不进入生成。
- 接入 DeepSeek策略判断、Qwen初稿、DeepSeek风险复核，并保留模型不可用时的可编辑降级结果。
- 默认对外部模型输入脱敏；原始文件按组织、客户和项目隔离。
- Docker增加LibreOffice、中文OCR和Noto CJK字体，支持旧版Office转换、扫描件识别和中文Word导出。
- Docker系统依赖安装切换到阿里云Debian镜像源，并设置超时与重试，避免ECS访问官方源时长时间挂起。
- 同步发布决策驾驶舱指标市场策略表达、平台筛选和对比展示优化。

## 本地测试结果

- BF完整单元与集成测试：28/28通过。
- 系统Python兼容与HTTP端到端测试：4/4通过。
- `bash scripts/release_gate.sh` 通过，`failed: []`、`runtimeErrors: []`。
- `git diff --check` 通过，提交内容未发现密钥或真实客户资料。
- Word样例中文首屏、两页版式和PDF文本层检查通过，无裁切或重叠。

## 发布与回滚要求

- GitHub目标分支：`main`。
- ECS目录：`/opt/mmn-perception-engine`，该目录无 `.git`，需使用提交归档同步。
- 发布前创建服务器代码归档和运行数据备份。
- 同步后执行 `MMN_SKIP_GIT_PULL=true bash deploy.sh`，再验证容器、首页、`/api/health`、BF页面与Schema接口。
- 如发布异常，恢复本次发布前代码归档并重新部署；运行数据默认保留，如涉及数据问题再使用发布前备份恢复。

## 云端发布结果

- GitHub功能提交：`e5b21f0 feat: ship adaptive BF factory P0`。
- GitHub构建修复：`7e769d5 fix: stabilize BF image build on ECS`。
- 服务器目录：`/opt/mmn-perception-engine`，使用 `7e769d5` 提交归档同步到无 `.git` 发布目录。
- 部署时间：2026-07-10 19:19（Asia/Shanghai）。
- 发布前代码归档：`backups/code_before_e5b21f0_20260710_191059.tar.gz`。
- 最终部署前运行数据备份：`backups/mmn_backup_20260710_191652.tar.gz`。
- 首次构建访问Debian官方源长时间无进展，旧服务已先恢复；切换阿里云镜像源后，10.1MB索引约2秒完成、218MB依赖下载和安装成功。
- 容器状态：`mmn-app`、`mmn-db`、`mmn-scheduler` healthy，`mmn-web` running。
- 公网检查：`bash scripts/test_mmn_cloud.sh http://121.40.60.90` 通过首页和健康接口；`/api/health` 返回 `ok: true`、`version: beta 1.01`。
- BF前端资源：公网首页包含 `BF FACTORY` 和 `bf-factory.js?v=beta-1.01-bf-p0-1`。
- 生产镜像测试：设置仅作用于测试进程的免登录开关后，BF完整测试28/28通过。
- 生产依赖：LibreOffice `25.2.3.2`、Tesseract `chi_sim/eng/osd`、`Noto Sans CJK SC` 均已验证可用。
- 云端认证验证：管理员登录后 `GET /api/bf/schema` 返回 `schemaVersion: 1.0.0`，A–F六层字段完整。
- 发布后日志：近10分钟未发现 `traceback`、`exception`、`critical` 或 `error`，`RECENT_ERROR_LINES=0`。

## 发布结论

BF工厂 P0、开放式范式学习、资产回流、Word导出、客户项目隔离、默认脱敏、驾驶舱策略表达优化及全部交接记录已同步至 GitHub 和阿里云 ECS。公网服务、生产依赖、容器、BF认证接口和回滚备份均验证通过。

---

# MMN Perception Engine 发布记录补充

## 发布日期

2026-07-10

## 发布版本

beta 1.01｜BF产品规则外显清理

## 修复内容

- 删除BF工厂顶部将内部产品规则直接展示给用户的说明横幅。
- 隐藏BF工厂顶部重复的生成器横向子菜单，减少页面层级和视觉噪音。
- 新增静态实拍、动态实拍、底盘实拍三种BF类型及对应生成章节和执行要求。
- 清理BF快捷入口、生成稿、证据空状态、回流提示和文件状态中的内部机制文案。
- 保留智能识别、自定义内容方向、历史证据、安全开关和终稿回流等业务能力。
- 新增单元测试与浏览器发布门禁，阻止内部规则再次进入BF页面或交付稿。
- BF前端资源版本更新为 `beta-1.01-bf-ui-2`，强制刷新修复后的页面和交互脚本。

## 发布要求

- 本地完整测试和发布门禁通过后推送 GitHub `main`。
- 服务器发布前创建代码归档和运行数据备份。
- 同步到 `/opt/mmn-perception-engine` 后执行 `MMN_SKIP_GIT_PULL=true bash deploy.sh`。
- 发布后检查公网首页不包含本次清理短语，并验证健康接口、容器和近期错误日志。

## 云端发布结果

- GitHub功能提交：`0172cca fix: keep BF product rules internal`。
- GitHub缓存刷新提交：`5c9c561 fix: refresh BF factory frontend assets`。
- ECS最终部署时间：2026-07-10 19:43（Asia/Shanghai）。
- 最终部署前代码归档：`backups/code_before_5c9c561_20260710_194257.tar.gz`。
- 最终部署前运行数据备份：`backups/mmn_backup_20260710_194259.tar.gz`。
- 本地完整测试31/31通过；浏览器发布门禁通过，`failed: []`、`runtimeErrors: []`。
- 公网BF工厂已确认不含内部规则横幅，不含生成器横向子菜单，并加载 `beta-1.01-bf-ui-2` 前端资源。
- 公网页面已显示静态实拍、动态实拍、底盘实拍；生产类型库确认三种类型均已初始化。
- 生产容器内完整测试31/31通过；`mmn-app`、`mmn-db`、`mmn-scheduler` healthy，`mmn-web` running。
- `/api/health` 返回 `ok: true`；近10分钟日志 `RECENT_ERROR_LINES=0`。

## 发布结论

BF内部规则外显清理、重复子菜单隐藏、三种实拍BF类型、前端缓存刷新和全部交接记录已同步到 GitHub 与阿里云 ECS，线上验证通过。

---

# MMN Perception Engine 发布记录补充

## 发布日期

2026-07-10

## 发布版本

beta 1.01｜产品评价汇总表导入质量修复

## 修复内容

- 对“数据整理”产品评价汇总表按真实区块解析，禁止把情感字段、车型名或平均值误识别为平台。
- 从 `Read Me` 读取数据周期，并从文件名与车型列表确定本品，避免默认取第一款车型。
- 仅使用源表提供的全网NSR和属性NSR；目标人群、购买意向、标签声量和风险量级缺失时明确展示“不适用”。
- 旧版错误导入结果自动隔离并要求重新导入；缺少必要NSR区块的新文件直接拒绝导入。
- 新增结构化导入单元测试和浏览器发布门禁，覆盖真实字段边界、指标边界和旧数据隔离。

## 验证与发布要求

- 本地完整测试、汇总表专项测试、发布门禁和空白检查通过后推送 GitHub `main`。
- 服务器发布前创建代码归档和运行数据备份；使用提交归档同步到 `/opt/mmn-perception-engine`，再执行 `MMN_SKIP_GIT_PULL=true bash deploy.sh`。
- 发布后验证公网首页、健康接口、前端资源版本、容器状态、生产测试和近期错误日志。

## 云端发布结果

- GitHub提交：`768d7c4 fix: validate product summary workbook imports`。
- ECS部署时间：2026-07-10 22:05（Asia/Shanghai）。
- 发布前代码归档：`backups/code_before_768d7c4_20260710_220429.tar.gz`。
- 发布前运行数据备份：`backups/mmn_backup_20260710_220429.tar.gz`；部署脚本运行数据备份：`backups/mmn_backup_20260710_220444.tar.gz`。
- 本地完整测试33/33通过；专项汇总表测试2/2通过；浏览器发布门禁通过，`failed: []`、`runtimeErrors: []`。
- 公网首页和 `/api/health` 通过，首页已加载 `app.js?v=beta-1.01-summary-import-2`。
- 生产容器内完整测试33/33通过；`mmn-app`、`mmn-db`、`mmn-scheduler` healthy，`mmn-web` running。
- 发布后近10分钟日志未发现 `traceback`、`exception`、`critical` 或 `error`。

## 发布结论

产品评价汇总表导入已从“可被错误字段污染的推断”改为“按已验证区块和数据边界展示”。旧版错误结果会被隔离；同类不完整文件会被拒绝导入，防止再次生成看似正常但不可信的驾驶舱指标。

## 补充修复

- 替换导入现在同步刷新驾驶舱品牌、车型与平台筛选，不会保留上一项目的车型选择状态。
- 驾驶舱车型选择器只列出当前导入数据的车型；前端资源版本更新为 `beta-1.01-summary-import-3`。
- 已用 `AUDI E7X等5车产品评价_0710_v2.xlsx` 走通真实浏览器上传：本品奥迪E7X、全网NSR 75.1%、15个有效标签、数据周期2026.6.1–6.30，且页面无脚本错误。

### 云端补充发布结果

- GitHub提交：`4674cf8 fix: reset dashboard context on summary import`。
- ECS部署时间：2026-07-10 22:19（Asia/Shanghai）；发布前代码归档：`backups/code_before_4674cf8_20260710_221845.tar.gz`；部署脚本运行数据备份：`backups/mmn_backup_20260710_221848.tar.gz`。
- 公网首页已加载 `app.js?v=beta-1.01-summary-import-3`；健康接口正常。
- 本地及生产容器完整测试33/33通过；浏览器发布门禁通过，`failed: []`、`runtimeErrors: []`；近期生产日志无错误记录。

---

# MMN Perception Engine 发布记录补充

## 发布日期

2026-07-11

## 发布版本

beta 1.01｜整体平台NSR与属性NSR拆分

## 发布内容

- 从同一产品评价Excel中独立解析“车型 × 平台整体NSR”，保存为 `summaryPlatformNsr`，不再与产品点属性NSR混用。
- 整体平台NSR支持全网、垂媒车主口碑、抖音、小红书、微博、B站、视频号切换；车型筛选与声量模块同步，本品固定第一且不可移除。
- 点击车型后展示七平台气泡；竞品逐平台与本品对照，本品淡蓝、竞品粉红。
- NSR主图和气泡统一使用-100%至100%零轴，负值向左、正值向右；缺失值不转换为0。
- 全局吸顶样式限定到 `main>header`，修复滚屏后模块标题覆盖主导航的问题。

## 云端发布结果

- GitHub功能提交：`e7f19b1 feat: split overall platform NSR from attribute diagnostics`。
- ECS部署时间：2026-07-11 00:16（Asia/Shanghai）。
- 发布前代码归档：`backups/code_before_e7f19b1_20260711_001551.tar.gz`。
- 发布前运行数据备份：`backups/mmn_backup_20260711_001612.tar.gz`；部署脚本运行数据备份：`backups/mmn_backup_20260711_001632.tar.gz`。
- 公网资源版本：`style.css?v=beta-1.01-platform-nsr-split-1`、`app.js?v=beta-1.01-platform-nsr-split-1`。
- 真实 `AUDI E7X等5车产品评价_0710_v2.xlsx` 已验证5台车型、7个平台、正负值与缺失值均按源表读取；浏览器无横向溢出和控制台错误。
- 本地与生产容器完整测试33/33通过；发布门禁 `failed: []`、`runtimeErrors: []`；公网首页及健康接口通过，近期错误日志为0。

---

## 2026-07-12｜E7X决策数据链与资产回流

- 删除“大字报”式决策问题和候选车型卡片，以紧凑锚点串联 `NSR现状 → 竞品占位 → 机会判断 → 策略动作 → 资产沉淀`。
- 汽车之家正反向关系按竞品逐行展示最新排名、对比占位、周趋势和关系增强/减弱。
- 将属性NSR与真实竞品集合交叉为可放大、需补证、竞品占位和暂不决策，不把声量直接等同于市场需求或成交。
- 产品事实缺失统一标为待验证，不臆造官网配置；策略动作只在证据链之后出现。
- 新增一键沉淀Learning与Know-how：保留车型、竞品、周期、数据来源、适用条件和反例，并进入后续RAG检索。
- 真实两份E7X Excel验收：8条竞品趋势、6条合并判断、6条策略动作、75个属性气泡入口；无横向溢出、无浏览器错误。
- 前端资源版本：`beta-1.01-e7x-cockpit-2`。
- 竞品周趋势口径修正：由“首周至最新周的共同对比增强/减弱”改为“最新周较上周的车系对比次数占比变化”；增加指标释义，并明确不代表偏好、口碑或销量。资源版本更新为 `beta-1.01-e7x-cockpit-3`。
- 修复同一周被两种周期格式重复解析导致的伪 `0.0pt`；按工作表和排名观测去重，只比较相邻两个不同自然周，直接外显周期跨度。完全持平不再显示无意义的 `0.0pt`。
- 驾驶舱周趋势点击复用“竞品格局”双线详情气泡，展示本品与目标车型的正/反向排名、对比占比及8周明细，不再修改全局竞品选择。
- 机会地图调整为“一项属性一颗气泡”：气泡以本品为判断主体，灰色标签显示“本品领先”或真实主要对标；例如安全显示“对标 M7”，点击后展示本品第2/4及完整车型排名。资源版本更新为 `beta-1.01-e7x-cockpit-4`。
- 移除竞品周环比中的英文缩写 `pt`，不再只展示抽象差值；改为直接展示占比原值变化，例如“由2.5%升至5.3%”“由14.0%降至5.0%”或“3.2%→3.5%，基本持平”。资源版本更新为 `beta-1.01-e7x-cockpit-5`。
- 修复Excel数值`0`被 `cell_text` 当成空字符串的导入缺陷；0%现在作为有效NSR保留，空白、`-`、`—`继续视为缺失。小米YU7辅助/自动驾驶恢复垂媒车主口碑0%，三来源均值8.8%并参与排名。
- 属性排名覆盖规则同步明确：2个及以上来源可参与排名并标注覆盖度，0–1个来源才判定数据不足。资源版本更新为`beta-1.01-e7x-cockpit-6`，NSR地图脚本为`beta-1.01-data-first-detail-2`。

---

## 2026-07-17｜全量细分市场销量预警与全局检查发布

- 发布版本：`beta-1.02-20260717-sales-warning-global-1`。
- GitHub 功能提交：`b30f0ae feat: ship full-segment sales warning release`。
- 全量细分市场销量预警、最新本品监测集合、双旗舰质检门、T 周期复核和销量/RAG 数据资产已进入生产。
- 管理层市场结构图已放大并修复坐标轴、标签重叠；05—08 信息看板桌面高度增加，移动端及原信息架构保持不变。
- 全局检查修复方法说明仍引用旧 Demo 口径的问题；当前页面直接展示实际数据合同与 25%/50%/80% 动态参考线。
- 本地 232 项完整测试和 127 项发布门禁通过；生产相关 46 项专项测试通过。
- 生产双旗舰模型 A/B 均验证通过，管理结论发布门已打开；6 个服务运行正常，近期错误日志为 0。
- 服务器代码备份：`backups/code_before_b30f0ae_20260717_040913.tar.gz`；运行数据备份：`backups/mmn_backup_20260717_040947.tar.gz`。
- 公网 IP 健康接口、资源版本、真实业务接口和核心文件哈希均已验证。

---

## 2026-07-18｜昨夜迭代统一发布

- 发布版本：`beta-1.02-20260718-overnight-iteration-1`。
- 发布范围：MMN Eval v0.1 与可视化、Policy Intelligence、咨询输出框架、销售预警六个月历史、8 车上市周期持久化、DeepSeek V4 模型档位，以及对应管理层/驾驶舱 UI 与 API。
- 数据保护：车型上市日期只在服务器无持久化文件时初始化；已有服务器记录不会被仓库版本覆盖。销售预警历史保留缺失月份，不补零。
- 首轮本地完整测试 324/324 通过；生产首发修复后工作区完整测试 333/333 通过，发布门禁 131 项业务专项及浏览器检查通过，`failed: []`、`runtimeErrors: []`；语法、空白和敏感信息检查通过。
- GitHub 发布提交：`5060d56 feat: ship overnight MMN decision iteration`、`2e3e766 fix: bootstrap production decision dashboards`、`ec18050 fix: publish Eval fixtures to persistent volume`。
- ECS 于 2026-07-18 14:14（Asia/Shanghai）完成最终应用重建；代码备份 `backups/code_before_2e3e766_20260718_141313.tar.gz`，环境备份 `backups/env_before_deepseek_v4_20260718_141313`，运行数据备份 `backups/mmn_backup_20260718_141341.tar.gz`。
- 生产管理员真实业务流验证：集团驾驶舱 8 台监测车型、Eval 种子 10 案例、Policy Intelligence 6 条生效政策均返回 200；DeepSeek 生产档位为 `deepseek-v4-flash` / `deepseek-v4-pro`，6 个服务全部运行且核心服务健康。
- 公网 IP `http://121.40.60.90` 的首页与健康接口通过。`mmnsh.com` 的 HTTP-01 请求被云侧 `403` 阻断，当前无法签发独立 HTTPS 证书；域名 443 仍会命中既有 Credo 证书，不能作为本次 MMN 验收入口。该问题不影响 IP 入口与应用服务，但需要完成域名备案/云侧放行后再签发证书。

---

## 2026-07-18｜界面质感与全系统健康修复发布

- 最终版本：`beta-1.02-20260718-ui-health-2`；GitHub `main` 最终功能提交：`45e9bc3`，主功能提交：`eb9ca4b`。
- 博主蒸馏孵化页面完成视觉层级、资产生成链、证据状态、指标、操作区和生产资产重构；共享内容容器继续复用，但侧栏高亮与页面标题保持“博主蒸馏孵化”。
- 修复社媒趋势、项目权重和 Eval 在 390 像素视口下的整页横向溢出；桌面端与移动端 20 个主导航模块逐页验收均无溢出。
- 策略驾驶舱完成管理层版 8 个视图、8 台销量预警车型、4 类赛道、7 个政策对比车型、9 个品牌，以及标准版车型、T 周期、平台、卖点、机会与回流链路验收。
- 线上首轮验收发现云端登录前提前请求受保护接口；最终提交将主工作台与抖音热点首次加载统一延后到鉴权完成，登录前和登录后异常响应均为 0。
- 本地完整 Python 测试 378/378、全部 JavaScript 专项、语法与差异检查通过；发布门禁 137 项业务专项和真实浏览器检查通过，`failed: []`、`runtimeErrors: []`。
- ECS 最终重建时间：2026-07-18 20:15（Asia/Shanghai）。6 个服务全部运行，应用、数据库、Redis、达人 Worker 和调度器健康；近 10 分钟错误日志为 0。
- 生产管理员业务接口验证：Eval 10 案例、Policy Intelligence 6 条政策、销量预警 8 台车型、政策对比 7 台车型、博主公开样本 407 条、内容知识 752 条，均返回 200。
- 生产真实浏览器验证：登录前受保护接口异常响应 0；登录后 20/20 主导航、策略驾驶舱 8/8 视图和 390 像素布局通过，页面异常、失败请求及 HTTP 异常响应均为 0。
- 关键线上文件与本地提交 SHA-256 一致；发布前代码归档 `backups/code_before_eb9ca4b_20260718_195928.tar.gz`，运行数据备份 `backups/mmn_backup_20260718_200003.tar.gz` 与 `backups/mmn_backup_20260718_201452.tar.gz`。
- 正式验收入口仍为 `http://121.40.60.90`。`mmnsh.com` 继续受 ICP/云侧 403 与 443 证书问题影响，本次未越界改动域名、备案或 Credo 路由。

---

## 2026-07-19｜行动预算入口阶段性隐藏

- 发布版本：`beta-1.02-20260719-hide-action-budget-1`；GitHub 功能提交：`a385b7d`。
- 侧边栏“行动预算”入口和对应页面已隐藏；直接页面调用返回决策驾驶舱。
- 底层预算字段、配置和已有数据全部保留，未删除、未迁移、未重置。
- 本地完整发布门禁通过：137 项业务测试、浏览器检查 `failed: []`、`runtimeErrors: []`。
- ECS 发布前代码备份：`backups/action_budget_20260719_011259`；运行数据备份：`backups/mmn_backup_20260719_011321.tar.gz`。
- 生产 6 个服务运行、0 个不健康服务、近 5 分钟错误日志为 0；首页与健康接口返回 200。
- 生产真实浏览器确认入口和页面均隐藏，直接调用安全返回驾驶舱；页面异常和失败请求均为 0。
- 公网资源已切换到 `style.css?v=beta-1.02-20260719-hide-action-budget-1` 与 `app.js?v=beta-1.02-20260719-hide-action-budget-1`，核心发布文件与本地提交 SHA-256 一致。

---

## 2026-07-19｜全板块产品健康检查与客户发布

- 发布版本：`beta-1.02-20260719-global-readiness-2`；全板块功能提交：`cab9914`，传输层终态修复提交：`545cb74`。
- 新增全板块真实浏览器门禁，逐一打开 19 个客户可见主导航板块和管理层 8 个视图，同时覆盖 1440 桌面与 390 窄屏。
- 全局检查发现并修复管理层“销量预警”和“赛道环境”在 390 像素下的整页横向溢出；宽图表改为组件内滑动。
- 同步修正博主蒸馏页面的旧文案合约测试，客户页继续使用中性能力名称，不外显供应商名。
- 生产全板块快速切页暴露客户端主动断开后的响应写回日志异常；服务端已将这类连接断开作为传输层结束，不再误记为业务失败并二次写回。
- 本地完整测试 386/386 通过；发布门禁、语法、差异和研发档案检查通过。本地与生产全板块真实浏览器验收均为 `failed: []`、`runtimeErrors: []`、`failedResponses: []`。
- ECS 于 2026-07-19 03:08（Asia/Shanghai）完成最终重建；6 个服务全部运行，0 个不健康服务。重复生产全板块验收后，HTTP 5xx、Traceback 与断开写回异常均为 0。
- 生产健康版本与 `style.css`、`app.js`、`group-dashboard.css` 缓存版本均为 `beta-1.02-20260719-global-readiness-2`。
- 生产业务流验收：管理看板 8 车/4 赛道/7 政策对比/9 品牌，Eval 10 案例，政策看板 5 条规则与 7 个对比点，博主能力库 507 条来源/507 条样本/3 个画像/519 个知识片段，销量快讯 8 条，全部返回 200。
- 最终发布前回滚点：`backups/code_before_545cb74_20260719_030651.tar.gz`、`backups/env_before_545cb74_20260719_030651`、`backups/mmn_backup_20260719_030708.tar.gz`。服务器原有车型上市日期和 `/app/data` 持久化业务数据均已保留。
- 正式验收入口仍为 `http://121.40.60.90`。独立域名 HTTPS 仍受备案/云侧放行与证书问题影响，本次未越界修改其他产品路由。
