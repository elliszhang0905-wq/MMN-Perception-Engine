# MMN 2026-08-24 P0安全止血发布交接

## 发布终态

- 工单：`MMN-SEC-P0-20260824-01`
- 生产版本：`beta-1.03-20260824-p0-security-1`
- 应用提交：`50b754c3f0628c7cfa07c2043cc47e9f6f8acfaa`
- 发布分支：`codex/p0-security-release-preflight-20260824`
- 同步范围：本地8765、GitHub发布分支、GitHub `main`、ECS `/opt/mmn-perception-engine`
- 应用归档SHA-256：`59961c99c0d2bfaba9b8fc0d85a5a8793b3430f545b356c6844657e7bf87dceb`
- ECS发布前备份：`/opt/mmn-perception-engine/backups/release_p0_security_20260824_165618`

## 安全改动

- 认证与内部调度密钥必须是至少32位的独立强密钥；缺失、示例或低熵配置失败关闭。
- 未启用登录的服务只能绑定回环地址；本地启动默认使用 `127.0.0.1`。
- 调度请求使用方法、路径与时间戳签名并限制时钟偏差；公网代理清除全部调度身份请求头。
- 试用身份不能执行共享发布、共享复盘和任意公网链接采集；静态与动态试用POST共用账号级请求窗口与并发预算。
- 公网内容采集拒绝私网、回环、链路本地、凭据URL、异常端口和跳转后的非公网目标，并限制跳转次数与响应大小。
- 失败、缺失或冲突证据继续保留 `manual_required` 等不确定状态，不补0、不伪造确定结论。

## 发布与验收证据

- 38项P0聚焦测试通过；最终安全扫描 `f235f41b-7c31-43cd-8681-9472aad05156` 覆盖6个变更表面，发现数0；秘密扫描和差异格式检查通过。
- 完整发布门禁通过：全量Python/JavaScript、隔离服务、数据优先流程、19个客户入口、8个管理视图、1440px/390px，`failed=[]`、`runtimeErrors=[]`、`failedResponses=[]`。
- ECS蓝绿候选健康后切流，正式应用、后台任务与调度服务均为healthy；候选容器和部署锁清零。
- 真实权限对照：管理员登录与业务读取200；试用账号对3类管理/共享接口403；跨3条动态重试路由轮换第13次返回429；伪造和旧式调度身份被拒绝；合法签名通过、过期签名被拒绝。
- 本地与ECS容器内分别完成1440px/390px共80项全表面浏览器检查，失败检查、控制台错误和失败响应均为0。
- 本地8765进程工作目录为本发布工作树，版本正确，读取既有本地商业库；本地86表逻辑变化0，`quick_check=ok`。

## 数据保护与清理

- 生产浏览器验收产生7条 `project_snapshots`，每条均记录主键并计算完整行SHA-256；清理前再次校验内容未变化，只删除这7条。
- 验收更新的 `executive_brief_review.json` 已从发布前归档提取并通过原SHA-256校验后恢复。
- 最终生产主库发布前后均为84张表，`changedTableCount=0`、`quick_check=ok`；其他数据文件哈希一致。SQLite物理文件哈希因事务页布局变化而不同，不作为业务漂移判断。
- 不得用本地库或整库备份覆盖生产；如需回滚，只能先确认同期业务写入，再按明确主键/内容哈希或经过核验的代码归档执行。

## 残余风险与下一工单

- `mmnsh.com` HTTP从外部网络访问会命中阿里云备案拦截；HTTPS当前返回只覆盖 `credo.mmnsh.net` 的证书，主机名校验失败。
- 因此前述边界，本次服务器浏览器验收在ECS容器内经正式Nginx与正式应用完成，不能写成公网域名已可正常登录。
- 下一工单应独立处理：域名备案状态、MMN专属HTTPS证书、443虚拟主机与外部双端登录验收。该工作不得复用或改动Sales Credo配置、证书或运行状态。

## 回滚

- 代码与配置：使用备份目录中的 `source_config_before.tar.gz`、旧镜像ID及精确归档校验恢复，然后走同一蓝绿健康门禁。
- 数据：发布前数据归档为 `backups/mmn_backup_20260824_165618.tar.gz`；除灾难恢复外不得整库覆盖。正常回滚须先做同期业务增量审计并优先采用精确记录级处理。
- 生产应用标记应保持为应用提交 `50b754c3f0628c7cfa07c2043cc47e9f6f8acfaa`；后续仅文档收口提交不需要重新部署应用。
