# 2026-07-25 beta 1.03 抖音视频第三路独立分析安全重试修复

## 变更目标

修复抖音热门视频洞察中第三路独立分析因额度权限、错误映射、证据重取和通用超时造成的失败，并保证恢复后只重试失败槽位。

## 工程边界

- 三路独立分析继续使用同一冻结证据包和同一证据指纹。
- 单槽位恢复不覆盖另外两路的成功结果，也不重新抓取视频证据。
- 客户界面只显示中性的 MMN 独立分析槽位和可执行提示，不暴露供应商、模型、密钥或原始响应。
- 本次不迁移数据库，不导入或覆盖业务数据。

## 实施内容

- 深度分析默认模型更新为已验证可用的 `kimi-k2.6`，并增加视频洞察专用 180 秒超时配置。
- 403 额度错误按真实响应码和实际配置生成内部诊断，客户输出保持脱敏。
- 重试任务保留冻结证据包、结果和证据指纹，只调度所选失败槽位。
- 重试上限按当前证据指纹和所选槽位计算，避免历史其他槽位失败误伤恢复。
- 增加配置、错误映射、客户文案、同包重试、持久化和单槽位计数回归测试。

## 本地验证

- 定向 Python 测试：39 项通过。
- Python 完整回归：639 项通过。
- JavaScript 契约测试：32 个文件全部通过。
- 语法检查与 `git diff --check` 通过。
- 本地真实任务 `1ae3903c9da049e6a799af71a612af75` 已证明槽位 1、2 计数不变，槽位 3 仅增加一次；三路最终状态均为 `completed`，验证状态为 `verified`。

## 发布与回滚

- 应用版本：`beta-1.03-20260725-video-insight-retry-1`。
- 生产发布前必须分别备份源码、`.env` 和 `/app/data`，并记录数据库逻辑指纹。
- ECS 使用精确 Git 提交归档同步，部署后核验 `.deployed-git-commit`、关键文件哈希、容器、健康接口和真实业务页面。
- 回滚时恢复发布前源码归档和环境配置；若发生数据漂移，再恢复对应持久数据备份，并重新执行部署脚本。

## 生产验收

待生产部署完成后回填应用提交、备份标识、数据零漂移、桌面端与 390px 真实业务流结果。
