# 抖音逐视频证据链恢复修复工单

## 工单结论

- 类型：现有逐视频分析链路缺陷修复与生产发布
- 优先级：P0
- 状态：已完成生产闭环
- 版本：`beta-1.03-20260730-douyin-video-evidence-chain-3`
- 影响面：本竞品抖音内容雷达 → 单条视频 → 生成/重新分析

## 问题与根因

智己 LS6 视频 `7660088055264546082` 在本地真实流程中结束为 `limited_analysis`，界面只显示“当前仅形成有限分析”。只读核对确认：

- 分享地址使用 `iesdouyin.com/share/video/...`，现有页面校验只接受 `douyin.com`。
- 服务端写入 `duration_ms`，媒体处理器读取 `media.durationMs`，长视频误入短音频通道并报“音频过长”。
- 页面与榜单内容未通过一致性取证后，未获得可用浏览器关键帧；直接媒体地址又被公网安全门禁拒绝。
- OCR 返回非结构化结果后没有形成可用证据。
- 最终只有标题与评论，字幕、关键帧、OCR、视觉证据均为 0；三路独立分析没有启动。
- 雷达卡片把所有非运行状态统一显示为“重新分析”，没有说明当前仍处在补取证据阶段。
- 首轮生产重启验收进一步发现：逐视频任务已持久化，但车型热榜最新结果接口没有返回对应任务，页面刷新后卡片会退回“生成逐视频洞察”，造成“后台有结果、前台像未分析”的假象。

## 目标

1. 对合法抖音分享地址进行安全归一化，并继续按平台内容 ID 校验页面一致性。
2. 修正媒体时长契约，使长视频进入长音频转写链路。
3. 优先使用受控浏览器关键帧形成视觉与 OCR 证据，不降低私网/保留地址拒绝规则。
4. 对结构化 OCR 输出提供一次同证据修复机会，失败时保持明确缺失状态。
5. 让用户能够区分“正在取证、证据不足、分析未启动、三路分析失败或完成”。
6. 页面刷新或服务重启后，按当前榜单视频恢复同企业、同版本的最近逐视频任务，不跨车型或企业借用。
7. 完成测试、提交、GitHub 主线同步、本地更新、ECS 部署及真实业务验收。

## 范围

- `douyin_video_insights.py`：分享地址识别、标准页面地址与原始来源保留。
- `server.py`：媒体处理入参契约、证据获取与状态信息。
- `creator_distillation/media_processing.py`：时长兼容、关键帧优先、OCR 结构化修复。
- `douyin-vehicle-radar.js` 与对应样式：逐视频证据状态、缺失项与可执行按钮。
- 车型热榜最新结果接口：只为当前榜单内容返回最近逐视频任务，供页面刷新恢复。
- 对应 Python、前端合同与真实流程测试。
- 发布状态、研发档案与交接记录。

## 非目标

- 不改变正式榜单的播放量核验与排序口径。
- 不自动批量分析榜单中的所有视频；仍只处理用户明确点击的单条内容。
- 不绕过私网、回环、保留地址和任意域名安全校验。
- 不用标题、评论或模型常识补写缺失的视频本体证据。
- 不覆盖本地或生产 SQLite；不回滚整表。
- 不把 `limited_analysis`、`manual_required` 或真实冲突伪装为完成。

## 权限与隔离

- 已授权：代码与测试修改、文档更新、提交、推送、部署、真实验收、精确清理验收副作用。
- 隔离工作树：`fix/douyin-video-evidence-chain-20260730`，基线为授权时最新 `origin/main`。
- 现有 LS6 工作树中的未提交修改和数据库保持不动，不纳入本工单提交。
- 生产数据为权威来源；发布只传输提交归档，不同步本地数据库。

## 工程契约

- 分享地址必须为 HTTPS、受支持的抖音域名，并能提取与当前榜单一致的平台内容 ID。
- 原始分享地址与归一化页面地址分别保存，便于追溯；地址可打开不等于已读取视频本体。
- 媒体时长在服务端与处理器之间使用单一兼容契约；超过 5 分钟必须走长音频转写。
- 浏览器关键帧必须来自匹配内容 ID 的实际播放页面，携带时间点与媒体指纹。
- 安全门禁继续拒绝私网、回环、链路本地、保留及多播地址；不得以扩大通配白名单解决取证失败。
- OCR 修复只处理同一次模型输出的结构，不新增不可追溯内容；仍无有效对象时记录缺失。
- 证据覆盖不足时不启动三路分析；达到门槛后，每路输出必须独立持久化并通过结构校验。
- 客户界面使用简体中文和中性能力标签，不显示供应商、模型名、密钥、原始内部错误或证据哈希。

## 验收矩阵

- 地址：`douyin.com/video/{id}` 与 `iesdouyin.com/share/video/{id}` 可归一化；非 HTTPS、非抖音域名、内容 ID 不一致均拒绝。
- 时长：超过 5 分钟的视频进入长音频链路；短视频保持原路径。
- 画面：浏览器关键帧优先于不可直接读取的媒体 URL，并保留真实时间点。
- OCR：标准 JSON 正常通过；包裹文本/首个完整对象可恢复；无有效对象时明确失败且不生成证据。
- 门禁：无视频本体仍为有限分析且三路运行数为 0；具备转写和视觉证据后才启动三路分析。
- 状态：用户看见取证阶段、缺失项、分析是否启动以及下一步按钮；刷新后状态保持。
- 恢复：生产服务重启后重新打开车型热榜，已保存的 `manual_required` 或已完成任务仍回显到原视频卡片；其它企业、版本或榜单外视频不返回。
- 桌面与移动：1440px、390px 下状态和操作不遮挡、无整体横向溢出。
- 安全：私网/保留地址仍被拒绝，外部界面不泄露内部能力名称。
- 数据：部署前后主库及相关持久库通过 `quick_check` 与逐表、主键、字段逻辑比对；验收写入按记录 ID 与内容哈希精确清理。

## 发布与回滚

- 发布前分别备份源码、环境与持久数据库。
- 从已提交版本构建 Git 归档，上传前后校验 SHA-256；不一致立即停止。
- ECS `/opt/mmn-perception-engine` 保留发布前完整回滚目录。
- 部署后验证提交标识、关键文件哈希、容器、接口、任务状态与真实页面。
- 回滚只恢复本次代码版本；只有确认本次发布产生且无并发变化的数据，才按明确主键处理。

## 完成定义

- 专项测试、完整发布门禁和状态检查通过。
- GitHub `main`、发布分支与版本标签指向已验收提交。
- 隔离本地服务与 ECS 均以同一功能提交通过门禁；现有 8765 因承载 LS6 未提交工作而保持不切换。
- LS6 真实视频取得可引用的视频本体证据并实际启动三路分析；若外部能力仍受限，页面必须准确显示可执行原因且不得误报完成。
- 生产桌面/移动真实流程无新增控制台错误、失败请求或 HTTP 5xx。
- 验收副作用精确清理，逻辑数据差异为 0，残余风险明确交接。

## 实施与验收结论

- 功能提交：`f9a61db40370c2666faa9f2cc5141cdc539aaaa0`，已同步发布分支与 GitHub `main`。
- 生产版本：`beta-1.03-20260730-douyin-video-evidence-chain-3`；ECS 提交标记、服务器源文件和运行容器核心文件哈希一致，六个服务运行，活动任务为 0。
- 提交归档 SHA-256：`544c3837a21e9ff34aa359e3060919fd3ae96fdca249db366f3cccd30214ff6b`。
- 本地真实 LS6 视频 `7660088055264546082` 形成完整视频证据，单路结构失败后只重试失败槽位，冻结证据指纹不变，最终为 `majority_aligned`。
- 生产真实 L6 视频 `7667383336609261947` 形成 1 段转写、12 个关键画面、24 条画面文字、4 段视觉描述和 4 条评论；三路独立分析均完成，但核心判断冲突，系统正确保持 `manual_required`。
- 服务重启后，最新车型热榜接口恢复任务 `4eaae53102cd4ac1901b4ba2c831b28e`。1440px 与 390px 页面均回显“核心判断等待人工复核”“人工复核前不可直接发布”，按钮为禁用的“等待人工复核”，横向溢出、控制台错误、页面错误和失败请求均为 0。
- 专项后端、前端合同及完整 `npm run release:gate` 均通过；隔离数据库 86 张表在准备、启动、浏览器与源库检查中变化为 0。
- 两次生产部署前后，主库 84 张表与达人库 9 张表逐表、逐主键、逐字段逻辑差异均为 0，两库 `quick_check=ok`。最终页面验收产生的 13 条项目快照按主键与 payload SHA-256 精确删除，`project_snapshots` 恢复本轮发布前的 211 条。
- ECS 主回滚目录：`/opt/mmn-perception-engine/backups/releases/douyin_video_evidence_chain_20260730_233829_pre`；刷新恢复补丁回滚目录：`/opt/mmn-perception-engine/backups/releases/douyin_video_evidence_chain_20260731_reload_hotfix_pre`。
- 本地 8765 当前仍由 LS6 未提交工作树提供服务。为避免覆盖用户并行工作，本工单只在隔离服务完成本地真实验收，没有强制切换 8765；GitHub 与 ECS 已更新到正式功能提交。
