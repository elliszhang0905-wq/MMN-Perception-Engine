# MMN研发团队｜知识工作区 S1 独立预览发布

- 日期：2026-09-08；原型版本：s1-20260908-1。
- 用户授权：在 S1 原型交付后明确要求“推送、变更服务器”。本次承接独立原型发布，不把模拟能力接入正式资产库。
- 分支：codex/mmn-knowledge-workspace-s1-20260908；基于 origin/main 的 8c28cfb。
- 状态：S1 独立预览已推送、已部署并完成限定验收；不表示 S2/S3 或正式产品界面已升级。

## 范围与技术方案

MMN研发团队仅同步 output/mmn-knowledge-workspace-s1 的原型文件、浏览器检查脚本、原型工单、状态和研发记录。保留原有字体、灰阶 Token、RAG资产库命名与导航层级；正式 index.html/app.js/style.css/server.py 以及数据库不在发布清单中。原型没有 API、模型、存储或真实证据接入。

服务器使用已有 Nginx 镜像创建独立容器，非 root、只读文件系统、资源限制、静态文件只读挂载；只允许四个静态文件，拒绝 API、目录浏览、内部文档及非 GET/HEAD 请求。服务器端口只绑定 127.0.0.1:18791，本机通过 SSH 隧道映射到 127.0.0.1:18792。预览不是公网客户服务，不修改备案、证书、反向代理或防火墙。

源工单与 README 中“未推送/未部署”描述的是 S1 制作阶段；本次新增授权和发布结果以此记录为准。源文档引用的知识审计文件、原库聚合和本地截图属于此前工作区证据，不随本次发布整库上传。原型包中的 README 只作工程交接，不由 Nginx 提供。

## 验收与风险

提交前重跑 34 项原型浏览器检查、JS 语法和状态包检查；检查实际清单并做独立审查。服务器启动后重复原型真实交互、HTTP 文件哈希、拒绝路径和只读行为验证，并核对正式容器启动时间、核心文件和数据库哈希。静态原型无构建或依赖安装环节，不宣称正式应用全量回归通过。

一次性原型历史上没有先红后绿的 TDD 证据；本次不补造，使用可重复的 34 项检查作为发布候选验证，不将其视为生产功能完成。

## 回退

这是首次新增独立预览，无旧预览需要覆盖。回退仅停止精确容器 mmn-knowledge-s1-20260908-1，并关闭对应本地 SSH 隧道；保留版本目录与 Git 分支便于恢复。不回滚正式 MMN、不恢复或覆盖数据库、不执行全站 deploy.sh。

## 发布结果

提交前本轮：候选 18793 静态预览的 34 项浏览器断言通过，page errors 0；JS 语法、git diff --check、npm run check:mmn-state 通过。独立只读审查无 Critical/Required，可继续 S1 预览发布；不替代服务端验收。测试 runner 不提供全局 URL，新增端口参数化首次失败后，经最小探针定位并改用浏览器 location.origin，随后完整重跑通过，未改原型业务行为。

### 发布身份

- 运行原型提交：`771abe388f5e8e2c41859473acf7bafd80470cff`；已推送上述独立分支，未合并 main。
- 精确 Git 归档（仅 4 个静态文件 + Nginx 配置）SHA-256：`a1fde4cecb4467f41482652cbfef1bf16fc71ae8287fb9d9a314f46288963a46`；服务器上传后重新核对一致再解包。
- 服务器版本目录：`/opt/mmn-previews/knowledge-workspace/s1-20260908-1-771abe3`；没有覆盖现有目录。
- 容器：`mmn-knowledge-s1-20260908-1`，ID `e774feae5b3d2c83fb131d7e5e57b3252787cc51c9ab7eaae8bc5426b8887b80`。
- 复用已有 Nginx 镜像 ID：`sha256:65645c7bb6a0661892a8b03b89d0743208a18dd2f3f17a54ef4b76fb8e2f2a10`，没有拉取新镜像。
- 运行检查：uid `101:101`，根文件系统 readonly，两个挂载均 RW=false，端口为 `127.0.0.1:18791 → 8080`；限制 64MiB、0.25CPU、64 PIDs，并去除 capabilities。
- 本机 SSH 隧道入口：`http://127.0.0.1:18792/`；页面实际由 ECS 返回，不是原本 18791 的本地 Python 预览。关闭隧道后本机入口不可用，服务器独立容器仍保留。未开放公网客户入口。

### 服务端验证（2026-09-08 01:35—01:37，Asia/Shanghai）

- HTTP 首页 200，`X-MMN-Preview-Version: s1-20260908-1`；CSP、nosniff、no-referrer、no-store 均存在。
- 服务器实页经 SSH 隧道完整执行同一 34 项浏览器断言，结果 34/34 PASS，page errors 0；截图保存在发布工作树 `output/playwright/mmn-kw-s1-*.png`，未推送生成截图。查看了服务器桌面截图；原型的模拟权限不代表真实多租户验收。
- HTTP 返回的 index.html、prototype.css、prototype.js、mmn-logo.png 与 Git 候选 SHA-256 全部一致。
- `/api/health`、`/README.md`、`/nginx-preview.conf`、`/.env`、`/data/commercial_demo.db`、`/output/`、`/unknown` 全部 404；`POST /` 为 403。Nginx 记录一条由该负向用例触发的 `access forbidden by rule`，是预期拒绝，不描述为零日志错误。
- 正式 MMN 既有宿主回环入口实为 8080；旧部署文档的 8765 本轮探测拒绝连接，随后按真实监听检查 8080。正式首页/健康均 200，版本保持 `beta-1.03-20260901-tikhub-social-evidence-1`。
- 正式 app 容器 ID `fa7efeed9e2df4c9d1503f8ba96647965f53b5d0176526e38fac8f497483725a`，启动时间保持 `2026-09-01T15:31:58.34724512Z`；web ID `8a9ed11f7f815b23162bb72a5fa699cffbe49aa8d1b4f9bd27bc8ac4de93887c`，启动时间保持 `2026-08-31T19:17:04.914738335Z`。

以下正式容器文件前后哈希一致，证明列明受保护对象未被本次预览发布覆盖，不冒充所有数据卷的逻辑审计：

```text
/app/server.py 519cd508169e4c6934b0c307fd6812d514926b8c3870927af003fc9e993f3e1d
/app/index.html 8c24440a9b58f51d818ddfc7e54f69ee3e5a89e02d7179cf01ae42be02b1e29a
/app/app.js 4e24f083b8c6bbf7bf711bf6bade427c2a0f740027edb675559767a4c69f5695
/app/style.css be9aeebf8fd8d192867438811562d828d8cfc9b7f9d13355585f264bcd532060
/app/data/commercial_demo.db 24975207ccfda426c7d15b193d6cc2e098ea018df885d5ca407574f28af0f7ef
```

### 仍不包含

未发布其他未提交工作；未合并 main；未接入真实来源/审核/版本接口；未开放写入；未执行生产全站回归；未改变检索开关、备案、HTTPS、正式反向代理或其他产品。独立审查仅有长行可读性 Optional，留待后续演进，不阻塞当前静态预览。
