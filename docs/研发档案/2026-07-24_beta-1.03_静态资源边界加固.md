# beta 1.03 静态资源边界加固

## 状态

- 当前状态：本地、隔离环境与生产环境验收完成
- 发布状态：已提交、推送、同步、部署并完成零漂移收口
- 数据范围：不修改业务表、业务记录或看板计算

## 目标

把Web服务的文件读取边界收缩到客户页面实际使用的前端资源，其他项目文件默认不可通过HTTP访问；同时在反向代理增加独立的敏感路径保护，避免单层配置失误重新扩大文件访问范围。

## 实现

- 应用层使用显式前端资源白名单，首页、页面脚本、样式、图标和同源内嵌页面继续可用。
- GET与HEAD共用同一文件访问门禁。
- 编码路径、双重编码和目录穿越片段不能绕过白名单。
- Nginx拒绝数据、备份、日志、内部文档、源代码和部署配置路径。
- 应用与Nginx补充`nosniff`、同源框架和来源策略响应头。
- 发布门禁加入真实HTTP边界测试，同时验证合法页面资源和API仍可访问。

## 验收

- 修复前，新增测试能够复现未授权文件访问。
- 修复后，合法首页、脚本、样式、图标和API返回200。
- 非公开文件、目录及编码绕过请求返回404。
- Nginx配置在与生产一致的镜像和网络中通过语法检查。
- 临时Nginx实例通过合法资源与敏感路径双向验证。
- Python 615项测试通过，JavaScript 31个测试文件通过。
- 隔离数据副本完成1440px与390px真实页面验收，控制台错误与警告均为0。
- 测试数据库发布前后逐表、逐主键、逐字段对比，变化表为0。

## 发布边界

- 本次不迁移数据库，不修改会话机制，不改业务接口输出合同。
- 生产代码、环境配置、数据库和逐表逻辑指纹均在部署前完成备份。
- 回滚时恢复发布目录中的源码归档、环境配置和数据库备份，再以跳过Git拉取方式重建，并复验版本、文件边界、六服务和数据库一致性。

## 生产发布结果

- 应用提交：`d8bf3c9eb5aacc06fb4346ebae16c1bb7c50ac33`。
- 应用版本：`beta-1.03-20260724-static-boundary-1`，发布日期为`2026-07-24`。
- 发布目录：`/opt/mmn-perception-engine/backups/releases/static_boundary_20260724_015944`。
- 发布前源码、环境配置和数据库备份完成；部署脚本另生成`mmn_backup_20260724_020344.tar.gz`。
- 本地、服务器源码和运行容器中的`server.py`、Nginx配置哈希一致，ECS部署标记与应用提交一致。
- 公网合法资源和健康接口返回200；源码、数据库、环境配置、备份、日志、内部文档及编码绕过请求返回404。
- 真实管理员业务链路完成19个客户入口、8个管理视图、1440px与390px共82项检查，失败项、运行时错误和失败请求均为0。
- 验收产生的2条政策分析、5条项目快照已删除，20条车型身份和1条产品评价数据集刷新已按主键恢复。
- 最终77张业务表与发布前逐表逻辑指纹完全相同，`quick_check=ok`；近15分钟应用错误与Nginx 5xx均为0。
