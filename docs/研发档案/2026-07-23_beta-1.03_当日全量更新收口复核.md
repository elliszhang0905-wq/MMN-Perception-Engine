# beta 1.03 当日全量更新收口复核

日期：2026-07-23

## 复核范围

- 当日 12 个提交、61 个变更文件，以及由其影响的竞品格局、社媒趋势、品牌穿透、销量预警、周度市场刷新、决策驾驶舱和全局车型上下文。
- GitHub 发布分支、`main`、版本标签、ECS 运行提交、容器状态、生产数据库结构和长期运行日志。
- 19 个客户入口、8 个管理视图、1440px 桌面端、390px 移动端和真实懂车帝周榜图片确认流程。

## 结论

- 未发现需要新增代码修复的产品缺陷。
- 首轮全量测试的 2 个错误来自系统自带 Python 缺少 Word 文档依赖；切换到项目完整运行环境后，610 项测试全部通过，未修改产品代码。
- 真实懂车帝周榜图片识别出 10 行、周期为 2026-07-13 至 2026-07-19；首次确认成功写入隔离数据库，第二次确认返回幂等结果。
- 正式生产库未执行图片确认写入；最终仍为 77 张业务表、0 条图片复核草稿，`quick_check=ok`。

## 验证证据

- `git diff --check`、Python 编译、状态包检查、状态包脚本自测和客户侧供应商名称保护均通过。
- 完整 Python 回归：610/610。
- 发布专项：173/173。
- 数据优先浏览器流与全入口浏览器门禁：`failed=[]`、`runtimeErrors=[]`、`failedResponses=[]`。
- 当日新增代码未发现冲突标记、待办占位、私钥或常见访问令牌。
- ECS 六个 Compose 服务均运行；五个核心服务健康，主页和健康接口返回 200。
- 自最终应用部署起的生产日志：0 个 Traceback、0 个 Exception、0 个 ERROR、0 个 HTTP 5xx。
- ECS 应用标记仍指向 `f3b35738e2327026e65fe1e6d944325c54210844`，健康接口版本为 `beta-1.03-20260723-vertical-social-reliability-2`。

## 数据与发布边界

- 全部自动化浏览器验收和真实图片确认均在隔离数据库执行，没有向正式竞争格局、社媒证据库或历史项目数据写入验收记录。
- 本地既有备份、日志、截图、输出目录、临时目录、用户文档和未跟踪数据文件保持原状，不纳入提交。
- 公开社媒证据 V2、影子模式和独立 Worker 继续按既定发布决策保持关闭；本轮不扩大生产功能开关。
- 应用代码和版本无需再次变更或重建；本次只提交日终复核记录，并同步 ECS 交接提交标记。
