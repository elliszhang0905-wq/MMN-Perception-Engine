# beta 1.03 P0安全止血发布预检

## 发布工单

- 工单号：`MMN-SEC-P0-20260824-01`
- 发布版本：`beta-1.03-20260824-p0-security-1`
- 优先级：`P0`
- 审批状态：`已批准执行`
- 执行状态：`已完成应用发布与数据收口；公网域名外部边界另行治理`
- 发布范围：候选代码提交与GitHub主线同步、本地8765同步、ECS蓝绿发布、双身份与攻击对照验收、验收写入精确清理、状态包与handoff更新。
- 非目标：P1/P2增强、生产密钥无必要轮换、MMN Essence与Sales Credo的任何变更。

## 变更边界

- 基线：`e615efadac4334bf96464a01049ce43d43b3c6bd`，对应月度销量版本。
- 工作树：`codex/p0-security-release-preflight-20260824`。
- 应用提交：`50b754c3f0628c7cfa07c2043cc47e9f6f8acfaa`；已同步发布分支、GitHub `main`、本地8765与ECS正式实例。
- 不改变 MMN Essence 与 Sales Credo 的代码、配置、数据或运行环境。

## 安全不变量

1. 云端登录必须使用至少32位独立随机认证密钥，示例值和低熵值拒绝启动。
2. 未启用登录时只允许监听回环地址，本地启动脚本默认绑定 `127.0.0.1`。
3. 内部调度保留时间戳与签名校验，并要求独立强密钥；公网代理清除全部调度身份请求头。
4. 试用身份不能执行共享发布、共享复盘和任意公网链接采集；全部试用POST共用账号级请求窗口和并发上限，动态重试与跨接口轮换不能扩大预算。
5. 公网内容采集拒绝私网、回环、链路本地、凭据 URL、异常端口及跳转后的非公网目标，并限制跳转次数和响应大小。
6. 不把抓取失败或证据不足改写为确定结论，继续返回 `manual_required`。

## 兼容策略

- 沿用最新版已有的“方法、路径、时间戳”签名协议，不降级为静态调度令牌。
- 月度销量版本号与静态资源缓存版本分开校验；未发生变化的静态资源不做无意义缓存刷新。
- 自动门禁使用临时副本；生产浏览器验收产生的写入必须按主键与完整内容哈希精确清理并复验逻辑零漂移。

## 验收要求

- 聚焦攻击用例与合法对照用例通过。
- 全量测试与未修改基线对比，不新增失败。
- 隔离服务完成桌面端与移动端真实业务流检查。
- 验收前后数据库逻辑一致且 `quick_check` 为 `ok`。
- 生产精确提交号、生产密钥强度和代理配置必须在获准发布前另行核验。

## 执行结果

- GitHub：发布分支与 `origin/main` 均已包含应用提交 `50b754c3f0628c7cfa07c2043cc47e9f6f8acfaa`。
- 安全复核：38项P0聚焦测试通过；最终安全扫描任务 `f235f41b-7c31-43cd-8681-9472aad05156` 覆盖6个变更表面，发现数为0；秘密扫描与差异格式检查通过。
- 完整门禁：全量Python/JavaScript、隔离服务、数据优先流程、19个客户入口、8个管理视图及1440px/390px通过，`failed=[]`、`runtimeErrors=[]`、`failedResponses=[]`。
- ECS：归档SHA-256为 `59961c99c0d2bfaba9b8fc0d85a5a8793b3430f545b356c6844657e7bf87dceb`；发布前备份目录为 `/opt/mmn-perception-engine/backups/release_p0_security_20260824_165618`。蓝绿候选健康后切流，正式应用、后台任务和调度服务均为healthy，候选实例与部署锁清零。
- 生产攻击对照：未登录业务请求和伪造/旧式调度身份均被拒绝；管理员登录与业务读取返回200；试用身份对公网采集、共享复盘和管理动作返回403；跨3条动态重试路由轮换在第13次触发统一429；合法调度签名通过，过期签名被拒绝。
- 浏览器：本地与ECS容器内均完成1440px/390px共80项全表面检查，两端均无失败检查、运行错误或失败响应。
- 本地8765：进程工作目录为本发布工作树，版本为 `beta-1.03-20260824-p0-security-1`，继续读取既有本地商业库；86张表逻辑变化0，`quick_check=ok`。
- 生产数据：浏览器验收新增7条 `project_snapshots` 并更新1个审阅状态文件；7条记录已逐主键、逐完整行SHA-256校验后精确删除，文件已按发布前备份哈希恢复。最终主库84张表逻辑变化0，`quick_check=ok`，其余数据文件哈希一致；SQLite物理文件哈希变化属于页布局/事务状态变化，不代表业务数据漂移。
- 密钥：生产认证与调度密钥强度已满足门禁，本轮未轮换真实密钥，也未在工单中记录密钥值。

## 残余风险

- `mmnsh.com` 当前仍存在既有公网基础设施边界：从外部网络访问HTTP会命中阿里云备案拦截；HTTPS返回仅覆盖 `credo.mmnsh.net` 的证书，严格主机名校验失败。因此本轮生产浏览器验收在ECS容器内经正式Nginx与正式应用链路完成，不能表述为公网域名已可正常登录。
- 应用安全补丁已部署且正式实例健康；域名备案、证书签发与独立MMN HTTPS入口需要单独基础设施工单处理，在修复前正式演示入口仍按既有约定使用服务器直连方式。
