# beta 1.03 历史审计加固生产发布

发布日期：2026-07-27（Asia/Shanghai）

## 发布结论

MMN-AUDIT-LEGACY-01 的任务 1 至任务 8 已完成 GitHub 主线与 ECS 生产闭环。运行应用提交为 `dca9f42a9c333557bbb74bccc9f8c7d7b04e47c6`，版本码为 `beta-1.03-20260727-audit-hardening-1`。

## 发布前保护

- 生产发布前活动任务为 0。
- 回滚目录：`/opt/mmn-perception-engine/backups/releases/20260727_dca9f42_predeploy`。
- 发布前源码归档：`source_before.tar.gz`，SHA-256 `5e50208d88134e38cec7cd1d47d0b520221dd0f3e96904148d26984ab1fdb7a0`。
- 发布前主 SQLite 在线备份：`commercial_demo_pre_dca9f42.db`，SHA-256 `6f53342e5bc3e8e790fbb779fc135466e3965e3589fc4e4999a388d0d454337e`。
- 环境配置备份：`env.before`，权限为 600，SHA-256 `7e6be9adccf4958a8121a57c0937f5e6a33059d404967e003fb986e29512782a`。
- 完整持久化数据归档：`backups/mmn_backup_20260727_083201.tar.gz`；部署脚本另生成 `backups/mmn_backup_20260727_090419.tar.gz`。
- 发布归档 SHA-256：`fdf49c5aebbefe06f5486076b9d005dc98a1b2f9090c90d3a30288445546250c`。

## 验证证据

- 本地完整发布门禁通过：Python 651 项、33 个 JavaScript 测试文件、27 项真实 HTTP 边界、状态包、数据优先浏览器门禁及全表面门禁。
- 本地真实浏览器覆盖 19 个客户入口、8 个管理视图、1440px 与 390px，失败项、运行时错误和失败响应均为 0。
- GitHub `main`、ECS 部署标记与运行应用提交一致；`server.py` SHA-256 为 `d8a94c4c8ddee6e5fe80bf389a5e88cc14a14b990ace3dec9fb92859237acd1d`，`index.html` 为 `1e268a336b7514f99c650025b5d6ccb25824f635b42645a0195d5e791c2587f3`，本地与服务器一致。
- 部署启动前后生产 SQLite 均为 78 张表，变化表 0，`quick_check=ok`。
- 生产真实管理员浏览器门禁再次覆盖同一桌面与移动范围，`failed=[]`、`runtimeErrors=[]`、`failedResponses=[]`。
- 验收新增 6 条 `project_snapshots` 测试记录；已逐条核对主键、创建时间、车型、数据版本和 payload SHA-256 后精确删除。清理后 78 张表与验收前逐表、逐主键、逐字段一致。
- 六个 Compose 服务持续运行，五个带健康检查的服务均为 healthy；活动任务为 0。近 30 分钟日志未发现 Traceback、ERROR、CRITICAL、BrokenPipe 或 HTTP 5xx。

## 入口与边界

- 正式 MMN 公网入口为 `http://mmnsh.com`，公共 DNS 指向 `121.40.60.90`；生产主机以 `mmnsh.com` Host 头访问 MMN 健康接口正常。
- 当前 `443`/HTTPS 由独立的 Sales Credo 配置与证书占用。为保持两条产品线隔离，本轮未改动 Credo TLS 路由；MMN 独立 HTTPS 入口仍是单独基础设施事项。
- 公开社媒证据 V2、客户端开关、影子模式和独立 Worker 继续关闭。

## 回滚

恢复上述 `source_before.tar.gz` 与 `env.before`，再恢复发布前完整数据归档或在线 SQLite 备份，并执行 `MMN_SKIP_GIT_PULL=true bash scripts/deploy.sh`。应用回退必须同时恢复 `.deployed-git-commit`，并重新执行健康、真实业务流程、日志和数据逻辑比较。
