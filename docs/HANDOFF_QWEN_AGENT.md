# MMN Perception Engine 智能体研发交接手册

## 2026-07-22｜产品评价部分汇总分层导入交接

- 当前生产版本为 `beta-1.03-20260722-product-evaluation-partial-1`，发布提交 `df8f220`。
- 汇总表准入必须按指标层级判断。多车型、声量平台区块和完整全网 NSR 合法时，即使没有属性 NSR，也应保存 `summaryHeat`、`summaryMetrics` 和全网 `summaryPlatformNsr`；不得因下游机会地图缺字段而丢弃上游有效数据。
- `attributeNsrAvailable=false` 是正式数据边界：属性明细行保持空，机会地图显示“未提供、不计算、不推断”。定性正负观点、分类占比和竞品总体 NSR 都不能转换成属性 NSR。
- 无法从文件名识别主车型时，现有确定性主车型规则仍优先识别启境或智己；数据包登记到源车型，同时保留同表其他车型作为对标。启境GT7正式包包含理想L8和极氪8X对标。
- 回归必须覆盖：完整属性表不受影响、缺属性表可登记、缺完整全网 NSR 仍拒绝、整体 NSR 显示、服务器目录刷新恢复、跨车型不串库、桌面/390px和运行时错误检查。生产证据与回滚点见 `release.md`。

## 2026-07-22｜车型产品评价目录交接

- 当前运行版本为 `beta-1.03-20260722-product-evaluation-catalog-2`；功能提交 `806f103`、递归保护修复提交 `0e0f505` 已推送并部署。
- 产品评价正式目录存储在 `product_evaluation_datasets`，唯一键为组织、版本和源车型。服务器目录是跨浏览器恢复依据，浏览器最近8个有效数据包只用于快速切换和网络降级。
- 数据包必须包含有效明细或汇总，源车型必须属于车型集合；空状态、超过4MB、超过100车型或5万行的数据拒绝写入。生产环境写入默认仅管理员，读取按当前鉴权组织隔离。
- 切换车型必须先保存当前有效包，再绑定目标车型目录；无数据车型必须清空上一车型数据并显示缺口。不得借用竞品数据、推断缺失NSR或把缺数状态登记为正式目录。
- `mmn:vehicle-context-updated` 只允许在 `changed=true` 时触发产品评价补载。管理看板缓存重绘会重新发布当前销量车型，但同车型事件不得再次加载缓存并重绘，否则会形成递归。
- 当前生产已登记智己L6和奥迪E7X；真实验收和回滚点见 `release.md`。以后修改目录机制必须覆盖：同车型替换、组织/版本隔离、空状态拒绝、跨浏览器恢复、缺数车型防串库、刷新恢复、桌面/390px及运行时错误检查。

## 2026-07-22｜决策闭环与旧版属性 NSR 兼容交接

- 当前运行版本为 `beta-1.03-20260722-decision-closure-1`；应用基线提交为 `619859582066f68afb074107948f68459a336c9f`，GitHub `main` 与 ECS `/opt/mmn-perception-engine` 已同步。
- 产品评价统一合同优先读取显式属性 NSR；只有字段、情绪和有效样本均满足登记合同的旧版正负聚合行才做确定性归一化。不得按车型名写特例，不得把未知格式猜成 NSR，也不得修改原始数组。
- 四象限、属性摘要、竞品选择和机会地图必须消费同一份归一化结果；切换到无数据车型必须清空上一车型结果，切回已登记车型必须可恢复。
- 标准模式由车型库控制全驾驶舱；管理层模式由销量预警唯一控制。后台预载不得抢占标准车型，缓存命中时仍需重新渲染并发布当前预警车型，但不得重复请求接口。
- 新决策、卖点、归因和资料包表只能增量创建；没有真实 Action/Result 和人工裁决，不得发布 Learning 或 Know-how。
- 发布前回滚点及生产数据计数见 `release.md`。每次继续开发均需执行状态检查、相关 Node/Python 测试、真实业务页面与桌面/移动回归；健康接口本身不能作为上线证明。

## 2026-07-21｜beta 1.03 抖音热点内容攻防交接

- 本次是大版本更新，统一版本码为 `beta-1.03-20260721-douyin-content-defense-1`。
- 榜单采集与视频洞察必须保持解耦：每日只更新六榜与原有品牌车型识别，只有用户点击单条“生成洞察”才允许取证和调用三旗舰；不得恢复全榜自动分析。
- 内部三路运行必须读取同一证据包版本且彼此独立；保留原始结构化输出、模型/提示词版本、时间、错误和分歧。客户侧统一显示“MMN三旗舰交叉分析”。
- 证据引用 ID 属于内部追溯字段，客户正文必须清理类似 `V:18ffe...` 的哈希串；完整证据统一通过“引用证据”面板展示可读类型、内容、时间点与来源。
- 原页能打开不等于后台已读完整视频。当字幕、关键帧、OCR 或评论缺失时，结果必须保留 `limited_analysis`/`incomplete`/`manual_required` 语义和缺口，不得用互动率、标题关键词或规则模板补齐。
- 本地使用已登录 Chrome CDP；服务器容器使用系统 Chromium 无头公开页模式。任一模式遇到登录/验证码/风控必须停止并降级，不得绕过。
- 数据持久化按 `orgId + edition + itemId + sourceFingerprint + promptVersion` 隔离。播放、点赞等传播指标变化不触发重分析；证据或提示词版本变化时生成新版本，不覆盖旧记录。
- 核心模块：`douyin_video_insights.py`、`douyin_browser_evidence.py`、`content_defense.py`、`creator_distillation/media_processing.py`、`server.py`、`douyin-hot-demo.js`、`douyin-hot-demo.css`。
- 发布后必须重新验证未点击视频不调模型、单条点击完整链路、缓存复用、失败降级、刷新恢复、组织隔离、1440/390 页面与客户侧供应商名扫描。
- 发布终态：主功能提交 `ee52340` 已推送，ECS `/opt/mmn-perception-engine` 已运行 beta 1.03；最终收口提交与 `beta-1.03` 标签随后发布。服务器部署前源码、环境和持久数据备份见 `release.md`。
- 生产 Gold 样本：视频 `7661947647062083663`，任务 `afecda8b179142a19cd96638f8717818`；full 证据、三路完成、`verified`，刷新后 1440/390 均显示“洞察已完成”。第二条视频 `7663349910708347867` 已在服务器取得 117.5 秒视频本体和 6 个时间点关键帧。
- 生产验收发现并修复 `_json_object` 对“一个正确 JSON 后又输出第二对象”的异常泄漏；现在只读取第一个完整对象，完全不可解析时统一降级为证据能力错误，不再导致整个异步任务异常退出。对应测试在 `tests/test_creator_distillation.py`。
- 服务器首次上线前没有六榜快照；本轮仅在空表门禁下新增六份本地已确认真实快照，未覆盖已有生产记录。今后不得把这种一次性发布同步改成每日自动全榜洞察。

## 1. 交接目的

本文档用于将 MMN Perception Engine 的后续研发工作交接给 Qwen Agent 或其他具备代码执行能力的研发智能体。

接手智能体的角色是“研发执行者”。MMN 的产品定义、策略模型、知识库结构、输出规范和发布节奏由项目所有者决定。接手智能体不得把自身模型能力等同于 MMN 产品能力。

## 2. 项目定位

MMN Perception Engine 是一个多模型驱动的汽车营销策略系统，不是简单的大模型聊天网页。

MMN 的核心资产包括：

- 汽车营销策略模型
- 车型、竞品、用户情绪、产品属性、身份认同、认知空位和传播动作工作流
- MMN 母知识库、客户私有知识库、项目学习库
- 内容资产、达人资产、垂媒正反向排名、车型资产
- 创始人蒸馏、博主/达人蒸馏、内容能力蒸馏
- 策略报告、达人 brief、短视频脚本、账号孵化方案、竞品传播口径输出规范
- 可编辑商业咨询型 PPTX、页面结构 JSON、预览图和自动校验报告输出规范
- 市场周报、客户策略汇报、品牌/高管 IP 表达资产
- 模型调度、RAG 检索、人工复核和研发档案机制

底层模型供应商可以替换，但不得改变 MMN 的品牌和方法论。

后续研发默认围绕车型策略、竞品传播分析、达人蒸馏、内容 brief、脚本生产、品牌/高管 IP 蒸馏、市场周报和策略报告输出展开。偏经销商、销售顾问、门店接待和即时销售攻防的功能属于 Credo，不进入 MMN 默认导航和交付路径。

## 2.1 MMN 与 Credo 产品边界

MMN 与 Credo 是两条独立产品线，不应混成一个系统。

MMN 面向车企品牌、市场部门、车企代理公司和汽车垂直 MCN，核心场景是营销策略、达人蒸馏、内容生产、品牌传播与报告输出。MMN 的页面信息架构、权限模型、交付口径和商业化方式应围绕品牌营销、内容资产与策略交付展开。

Credo 面向经销商、销售顾问和门店管理者，核心场景是本品卖点、竞品对比、销售攻防表达与到店接待辅助。Credo 的页面信息架构、权限模型、交付话术和商业化方式应围绕门店销售和销售顾问即时使用展开。

两者可以共享底层车型知识库、竞品资料和部分数据结构，但前端产品、用户权限、功能路径、交付口径、页面信息架构和商业化模型必须独立设计。禁止把 Credo 的门店销售功能直接塞进 MMN，也禁止把 MMN 的营销策略、达人蒸馏、内容生产和品牌传播功能直接塞进 Credo。

## 3. 当前项目路径

本地项目目录：

```text
/Users/ellis/Documents/MMN汽车营销引擎/china-auto-marketing-engine
```

本地访问地址：

```text
http://127.0.0.1:8765/
```

桌面启动文件：

```text
/Users/ellis/Documents/MMN汽车营销引擎/china-auto-marketing-engine/启动中国汽车营销引擎.command
```

GitHub 仓库：

```text
git@github.com:elliszhang0905-wq/MMN-Perception-Engine.git
```

## 4. 关键文件说明

| 文件/目录 | 作用 |
| --- | --- |
| `server.py` | 本地服务、API、SQLite 数据层、模型调用、导入解析、RAG 逻辑 |
| `index.html` | 前端页面结构 |
| `app.js` | 前端状态、交互、渲染、接口调用 |
| `style.css` | 前端视觉样式 |
| `data/commercial_demo.db` | 本地 SQLite 数据库 |
| `.env` | 本地环境变量和模型密钥配置，不得提交 |
| `.env.example` | 环境变量模板 |
| `README.md` | 项目总说明 |
| `README_DEPLOY.md` | 云端部署说明 |
| `docs/mmn-strategy-model.md` | MMN Strategy Model 说明 |
| `docs/研发档案/` | 每次迭代的研发记录 |
| `skills/mmn-consulting-pptx/SKILL.md` | MMN PPT Agent 工作规范 |
| `src/ppt-agent/` | PptxGenJS 主生成、预览导出和自动校验流水线 |
| `examples/ppt-agent/input/` | PPT Agent 示例页面结构、Marp 草稿和 Mermaid 逻辑图 |
| `output/ppt-agent/` | PPT Agent 示例产物、预览图、校验报告和结构稿 |
| `scripts/run_mmn_ppt_agent.sh` | 一键运行 MMN PPT Agent 工作流 |
| `scripts/mmn_ppt_ingest_sources.py` | MarkItDown 资料解析入口 |
| `scripts/mmn_ppt_inspect.py` | python-pptx PPTX 读取检查入口 |
| `bf_factory/` | BF领域契约、解析、抽取、生成、版本、学习、脱敏和Word导出 |
| `product_whitepaper.py` | 产品白皮书页筛选、能力抽取约束、原文定位和双模型一致性校验 |
| `bf-factory.js` | BF工厂与BF资产库前端交互 |
| `requirements-bf-factory.txt` | BF文件解析、OCR和Word导出Python依赖 |
| `deploy.sh` | 服务器部署入口 |
| `rollback.sh` | 服务器回滚入口 |
| `test_mmn_cloud.sh` | 云端功能检查脚本 |

### 社媒趋势中心（2026-07-11）

- 页面入口：`核心工作流 → 决策驾驶舱 → 社媒趋势中心`；驾驶舱父入口不可移除且仍需可点击。
- 核心实现：`social_trends.py`、`POST /api/social-trends/collect`、`GET /api/social-trends/latest`、`social_trend_snapshots`。
- TikHub 密钥只允许配置在后端 `.env` 的 `TIKHUB_API_KEY`，禁止写入 `index.html`、`app.js`、快照结果或日志。
- 三平台搜索端点使用 `TIKHUB_SOCIAL_DOUYIN_ENDPOINT`、`TIKHUB_SOCIAL_XIAOHONGSHU_ENDPOINT`、`TIKHUB_SOCIAL_WEIBO_ENDPOINT` 覆盖，升级前先核对 TikHub 端点索引。
- 真实验收必须同时确认：三平台返回有效来源、原文链接可下钻、证据哈希存在、快照可恢复、Qwen/DeepSeek 交叉复核状态可解释；无 Key 时只能标记“待真实数据验证”。

### T周期卖点市场匹配与产品白皮书（2026-07-16）

- 页面入口：`决策驾驶舱 → 卖点机会决策台 → 五类证据 → 产品能力 → 上传产品白皮书PDF`，不得另建主导航或旁路演示页。
- T0 固定为正式上市日；首发或预售日独立记录。E7X 默认首发预售日为 2026-05-08，T0 为 2026-05-29。
- 白皮书处理链：标准 PDF 文本解析 → 千问提取候选 → DeepSeek 复核 → 页码与逐字引句硬校验。解析器不是第三个判断模型。
- 只有 `dual_model_verified` 且当前穿透标签有匹配能力项时，产品证据才计入10分；扫描件、模型失败、页码错误、引句不存在或模型分歧均保持0分。
- 结果保存在 `product_whitepaper_evidence`，隔离键为客户空间、版本、车型；`GET /api/product-whitepaper/latest` 用于刷新恢复。
- 本地守护进程优先使用内置 Python 运行时，确保 `pypdf` 可用。若改用其他运行时，必须先安装 `requirements-bf-factory.txt`。
- 界面验收必须覆盖 1440、1280 和移动宽度；不得出现页面级横向溢出、文字遮挡、按钮挤压或长引句越界。宽矩阵只能在自身滚动容器内滚动。

## 5. MMN Strategy Model

每次汽车营销分析应按照以下流程组织：

1. 本品
2. 竞品
3. 用户情绪
4. 产品属性
5. 身份认同
6. 定位判断
7. 认知空位
8. 传播动作

当前策略模块包括：

- NSR
- Emotion
- Attribute
- Identity
- Positioning
- Gap
- Action
- RAG 知识库
- 周报生成
- 高管蒸馏
- 车型传播分析
- 内容资产中心
- 达人库
- 博主/达人蒸馏
- 内容能力蒸馏知识库

## 6. 模型调度规则

用户侧不直接选择 Qwen 或 DeepSeek。前端统一表达为：

- MMN 策略模型正在分析
- MMN 多模型引擎
- MMN 用户感知引擎
- MMN 高管蒸馏模型
- MMN 周报生成模型
- MMN 策略质检
- MMN 本地规则兜底

内部调度原则：

| 任务类型 | 调度原则 |
| --- | --- |
| 日常标签识别、内容摘要、达人风格拆解 | 优先 flash/plus 快速模型，不默认同步调用两个旗舰模型 |
| 策略推理、竞品拆解、反方观点、逻辑压力测试 | 一个旗舰模型先输出主结论，前台先返回初版；另一个旗舰模型只作为后台 critic 复核 |
| 中文交付、短视频脚本、PPT 文案、客户报告、长文档归纳 | 优先中文表达快速模型，深度质检按需后台触发 |
| 参数、销量、价格、配置、上市时间等事实类问题 | 优先结构化数据库、RAG 和官方来源，模型只负责解释 |
| 车型参数与配置问题 | 在来源门禁之后，由千问、DeepSeek、Kimi 三个旗舰模型独立复核；三者必须全部完成并共同引用同一证据，否则标记“需人工复核” |
| 多模型结论冲突 | 标记“需人工复核”，保留主结论、复核意见、来源、置信度和人工最终选择 |
| 模型不可用或超时 | 使用缓存、备用模型；仍失败则使用 MMN 本地规则兜底 |

任何新增模型能力都必须避免把单一模型原始答案直接暴露为最终策略结论。

## 7. 数据与知识库边界

必须遵守以下隔离原则：

- 国内版与出海版数据隔离
- 集团、品牌、车型、项目分层
- MMN 母知识库、客户私有知识库、项目学习库分层
- 原始数据、清洗数据、策略结论、人工学习分层
- 车型内容库、达人库、内容能力蒸馏库不得混写
- 服务器演示数据与本地主数据不得无控制双向覆盖

不得删除或覆盖：

- `data/`
- `imports/`
- `logs/`
- `docs/研发档案/`
- 用户手动导入的 Excel、CSV、JSON、截图和文本材料

## 8. 合规边界

采集和导入能力只能处理：

- 公开可访问页面
- 用户手动导入数据
- 合法授权导出文件
- 客户提供的内部资料
- 可见页面内容

禁止：

- 绕过验证码
- 绕过登录限制
- 绕过付费墙
- 破解签名或加密
- 伪造登录态
- 注入 Cookie 或 Token
- 批量采集评论区个人信息
- 删除水印
- 把外部观点包装成 MMN 原创结论
- 高度相似仿写外部账号原文

内容蒸馏必须表达为“方法论迁移”和“公开内容样本归纳”，不得冒充原账号。

## 9. 本地开发流程

接手任务时必须先说明：

```text
本次将检查哪些文件
本次计划修改哪些文件
预计影响哪些功能
```

开发步骤：

1. 阅读 `AGENTS.md`
2. 阅读本文档
3. 阅读相关模块代码
4. 给出修改计划
5. 只改当前任务相关文件
6. 本地测试
7. 写研发档案
8. 输出修改清单、测试结果和影响范围

不得在未确认需求的情况下重构无关模块。

## 10. 本地启动与测试

启动：

```bash
python3 server.py
```

访问：

```text
http://127.0.0.1:8765/
```

基础检查：

```bash
python3 -m py_compile server.py
```

前端脚本检查可使用本机可用 Node 环境执行：

```bash
node --check app.js
```

如果普通 `node` 不可用，应使用本机已配置的运行时或项目依赖中的 Node。

完整发布门禁统一执行：

```bash
bash scripts/release_gate.sh
```

门禁按 `NODE_BINARY`、系统 `PATH`、本机内置运行时的顺序解析 Node.js。显式 `NODE_BINARY` 可以是绝对路径或 PATH 中的命令名；显式值无效时必须直接报错，不得静默切换版本。

接口检查示例：

```bash
curl http://127.0.0.1:8765/api/health
```

PPT Agent 检查：

```bash
bash scripts/run_mmn_ppt_agent.sh
```

该命令应生成 `output/ppt-agent/mmn-strategy-deck.pptx`、页面结构 JSON、预览图、校验报告、Marp 结构稿和 Mermaid 逻辑图。交付口径必须保持 MMN 品牌营销、内容策略和策略报告输出场景，不得混入 Credo 门店销售场景。

## 11. GitHub 版本管理流程

标准流程：

1. 本地开发
2. 本地验证
3. 写研发档案
4. 更新交接手册、交接流程和发布记录
5. 查看差异
6. 提交 GitHub
7. 用户确认是否发布服务器

禁止：

- 在服务器直接改代码
- 跳过 GitHub 直接发布
- 提交 `.env`
- 提交 API Key
- 提交临时测试数据
- 使用破坏性 Git 命令回滚用户改动

## 12. 云端部署流程

服务器当前用于稳定演示，不是主要开发环境。

发布前必须确认用户明确指令，例如：

```text
发布到服务器
把所有更新同步到服务器
同步升级服务器
```

标准云端流程：

```bash
bash deploy.sh
```

回滚：

```bash
bash rollback.sh
```

云端测试：

```bash
bash test_mmn_cloud.sh
```

每次服务器同步必须同时同步：

- 业务代码
- 研发档案
- 交接手册
- 发布记录
- 部署或回滚流程说明

如果只同步代码而未同步交接资料，本次发布视为未完成。

部署文档见：

```text
README_DEPLOY.md
```

## 13. 研发档案要求

每次功能新增、修复或流程优化，必须新增研发档案。

目录：

```text
docs/研发档案/
```

命名建议：

```text
YYYY-MM-DD_beta-1.01_功能名称.md
```

内容必须包含：

- 研发日期
- 版本号
- 需求背景
- 问题分析
- 技术方案
- 功能实现
- 修改模块
- 测试结果
- 版本变更说明
- 后续计划

## 14. 当前重点模块状态

| 模块 | 状态 | 注意事项 |
| --- | --- | --- |
| 决策驾驶舱 | 已具备数据导入、车型选择、诊断结果展示 | 车型切换必须刷新对应车型数据 |
| T周期卖点市场匹配 | 已具备T周期、战略卖点、NSR竞品、白皮书产品证据和动作建议闭环 | 白皮书只采信双模型一致且可逐页定位的原文；产品证据按车型恢复 |
| 声量数据中心 | 已有样本预览和标签诊断 | 后续需持续提升可视化和车型筛选 |
| 垂媒竞争格局 | 支持汽车之家/懂车帝正反向排名导入 | 车型资产需保持品牌归属准确 |
| 内容资产中心 | 已有内容看板、达人库、蒸馏入口 | 不得混淆车型内容库与达人库 |
| 创始人蒸馏 | 已迁入内容资产中心 | 只处理公开表达，不生成伪素材 |
| 博主/达人蒸馏 | 已支持样本库、能力画像、RAG chunk | 不得复刻外部账号原文 |
| 内容能力蒸馏知识库 | 已支持账号自动蒸馏和兜底导入 | 主入口应是账号输入，不是文件上传 |
| RAG知识库 | 已接入策略库、人工学习、博主能力、内容能力 | 输出需保留来源和合规边界 |
| BF工厂 | P0已完成上传、结构化、动态生成、编辑、Word导出和终稿回流 | 三类BF只是种子范式；优质自定义终稿只允许脱敏方法论跨项目复用 |
| 云端部署 | 已有 Docker/部署脚本/回滚脚本 | 服务器只在用户明确要求时更新 |

## 15. 新功能验收标准

任何新功能交付前必须回答：

1. 用户是否能看懂入口和结果？
2. 是否会污染已有数据库或页面？
3. 是否保持国内版/出海版数据隔离？
4. 是否保留来源、时间、状态和失败原因？
5. 是否没有硬编码密钥和个人路径？
6. 是否通过本地语法和关键接口测试？
7. 是否写入研发档案？
8. 是否同步更新交接手册、交接流程和发布记录？
9. 是否说明了是否影响服务器版本？

## 16. 交接给 Qwen Agent 的执行提示

Qwen Agent 接手时，应以以下身份工作：

```text
你是 MMN Perception Engine 的研发执行智能体。
你必须遵守 MMN Strategy Model、数据隔离、合规边界、GitHub 发布流程和研发档案规则。
你不能把 Qwen 模型能力等同于 MMN 产品能力。
你不能绕过平台权限。
你不能直接修改服务器代码。
你不能硬编码 API Key。
你必须先本地验证，再由用户确认是否发布服务器。
```

## 17. 交接结论

MMN 可以交接给 Qwen Agent 或其他研发智能体继续开发，但交接内容必须包括代码、文档、数据边界、模型调度规则、发布流程和验收机制。

MMN 的长期价值不在某一个底层模型，而在 MMN 自有的汽车营销策略模型、知识库、工作流和可持续学习机制。

## 18. BF工厂 P0 交接说明

BF工厂服务品牌客户商业化内容 Brief 闭环，不是普通文档总结器。前台入口为“核心工作流 → MMN策略输出 → BF工厂”，后台入口为“资产沉淀 → BF资产库”。

### 稳定结构与开放范式

- `strategy`、`product`、`content`、`execution`、`risk`、`materials` 六层是稳定知识本体。
- 探店、云评/口播、高质感摄影是种子范式，不得改回封闭三选一模板。
- 未知和混合需求进入 `CUSTOM`，按内容意图动态组合章节。
- 优质 `CUSTOM` 终稿回流后，可沉淀脱敏章节范式；后续相似需求应通过 `find_matching_template_profile` 真正召回使用。

### 关键接口

- `GET/POST /api/bf/projects`
- `GET/POST /api/bf/documents`
- `GET /api/bf/schema`
- `GET /api/bf/briefs` 与 `GET /api/bf/briefs/{id}`
- `POST /api/bf/generations`
- `POST /api/bf/briefs/{id}/versions`
- `POST /api/bf/briefs/{id}/finalizations`
- `POST /api/bf/briefs/{id}/exports`
- `GET /api/bf/template-profiles`
- `GET /api/bf/knowledge-chunks`

### 模型、降级与安全

- 标准链路：DeepSeek内部策略判断 → Qwen中文初稿 → DeepSeek逻辑与风险复核。
- 模型不可用时必须返回 `EDITABLE_DEGRADED`，保留可编辑本地结构化结果。
- 外部模型调用默认脱敏，客户、车型、竞品、预算、联系人、链接和提取码不得进入审计明文。
- 原始文件按组织、客户和项目目录隔离；反例只用于风险，禁用样本不得进入生成上下文。
- 服务器首次构建会安装 LibreOffice、Tesseract中文OCR和Noto CJK字体，构建时间会明显长于纯Python版本；Dockerfile已固定阿里云Debian镜像源，并设置30秒超时与3次重试，避免官方源长时间挂起。

### 验收命令

```bash
python3 -m unittest discover -s tests -v
python3 -m unittest tests.test_bf_runtime_compat tests.test_bf_api -v
bash scripts/release_gate.sh
```

BF P0 当前验收基线为 28 项完整测试通过、系统Python兼容与HTTP闭环4项通过、发布门禁 `failed: []` 且 `runtimeErrors: []`。P1继续处理批量上传、深度表格解析、效果回流、多版本对比和PDF导出。

### 2026-07-10 云端发布状态

- GitHub主功能提交：`e5b21f0`；ECS镜像源稳定化提交：`7e769d5`。
- ECS目录仍为 `/opt/mmn-perception-engine` 无 `.git` 发布树，使用提交归档同步并以 `MMN_SKIP_GIT_PULL=true bash deploy.sh` 构建。
- 回滚代码归档：`backups/code_before_e5b21f0_20260710_191059.tar.gz`；部署前数据备份：`backups/mmn_backup_20260710_191652.tar.gz`。
- 生产镜像已验证 LibreOffice 25.2、Tesseract中文OCR、Noto CJK字体和BF测试28/28。
- 公网首页、健康接口、BF前端资源和管理员认证后的 `/api/bf/schema` 均验证通过；发布后错误日志为0。

## 19. 产品规则外显边界

- 用户给出的规则、约束、架构判断和学习要求默认是研发输入，应落实到代码、测试、配置或内部文档，不得直接改写成页面说明或交付稿文案。
- 只有用户明确要求展示，或该信息是用户完成操作所必需的状态、安全提示和业务字段时，才允许产品端外显。
- BF工厂尤其不得展示“种子范式、封闭模板、动态编排、学习最终版本”等内部设计解释；页面只展示功能名称、业务用途、可操作选项、结果和必要风险提示。
- BF工厂不再显示“新建BF、达人BF生成器、探店BF生成器”等横向重复子菜单；类型选择和后续操作统一留在工作台内。
- BF工厂当前工作台显式提供智能识别/自定义、探店、云评/口播、高质感摄影、静态实拍、动态实拍和底盘实拍七种创建入口；后三类分别使用 `STATIC_SHOOT`、`DYNAMIC_SHOOT`、`CHASSIS_SHOOT`。
- 新增或修改用户可见文案后，必须执行发布门禁和敏感短语扫描，确认研发说明没有进入页面、导出稿、空状态、Toast或示例内容。

## 20. 产品评价汇总表导入质量门禁

- `数据整理` 中的产品评价汇总表必须按区块解析：声量平台、整体情感、属性NSR不能跨列混读。
- 导入时必须从 `Read Me` 读取时间范围，并从文件名或明确车型字段确定本品；不能默认取第一款车型。
- 汇总表没有目标人群、购买意向、标签声量或风险量级时，页面和导出报告必须显示不适用，不得用规则权重伪造数值。
- 缺少车型、全网NSR或属性NSR任一必需区块时必须拒绝导入，不能回退到通用文件解析。
- 每次改动汇总表导入路径，必须运行 `tests.test_product_summary_import` 和浏览器发布门禁中的汇总表场景。

### 2026-07-10 汇总表导入修复发布状态

- GitHub提交：`768d7c4`；ECS通过提交归档同步到 `/opt/mmn-perception-engine`。
- 发布前代码归档：`backups/code_before_768d7c4_20260710_220429.tar.gz`；运行数据备份：`backups/mmn_backup_20260710_220429.tar.gz` 与 `backups/mmn_backup_20260710_220444.tar.gz`。
- 本地和生产容器均验证完整测试33/33通过；浏览器发布门禁的真实NSR与旧版隔离场景均通过。
- 公网首页已加载 `app.js?v=beta-1.01-summary-import-2`，健康接口正常，发布后错误日志为0。
- 后续替换导入必须同时重置 `dashBrandOpen` 和 `dashboardPlatformFilter`；驾驶舱车型选择器只能基于 `dashboardModelOptions()` 读取当前导入数据，不能使用全局资产车型集合。
- 2026-07-10 补充发布：GitHub `4674cf8`，ECS代码归档 `backups/code_before_4674cf8_20260710_221845.tar.gz`，运行数据备份 `backups/mmn_backup_20260710_221848.tar.gz`；公网资源版本为 `beta-1.01-summary-import-3`，生产完整测试33/33通过。
- 策略驾驶舱首屏不展示口碑健康 NSR、目标人群穿透 IPS、购买意向指数卡片；如后续重新规划驾驶舱，仅在用户明确要求时恢复相应展示模块。
- 产品评价汇总表不得进入情绪四象限：驾驶舱应展示全网声量/互动量车型对比和属性NSR矩阵；四象限空位不得复用已有标签强制填满。
- 产品评价汇总表驾驶舱第一模块固定读取 `summaryHeat`：使用源表“声量”和“互动量”的全网列，车型选择与“添加车型”只允许 `state.models` 中的当前导入车型。
- `summaryHeat` 同时保留 `platformVolume` 分平台声量。热度行点击后展示分平台声量气泡；不得从属性NSR行或规则权重推导平台声量。
- 汇总驾驶舱左侧必须将本品与竞品分开：本品固定且不可取消，只有竞品可筛选和重新添加。首屏四个运行计数保持隐藏，内容字段使用“当前可用产品点”。
- 声量与互动量柱采用独立尺度，页面固定显示不可直接比较绝对柱长的说明；后续修改缩放逻辑时必须同步更新发布门禁。
- 主图渲染顺序必须以本品为第一行；竞品气泡的每个平台必须同时显示竞品柱和下方本品柱，并使用统一尺度。关闭按钮叉号由居中的CSS线段绘制，不得改回字体字符。
- 2026-07-10 竞品对比修复发布：GitHub `1e5ce61`；ECS代码归档 `backups/code_before_1e5ce61_20260710_232633.tar.gz`；运行数据备份 `backups/mmn_backup_20260710_232633.tar.gz`；公网资源版本 `beta-1.01-summary-platform-compare-1`。本地门禁无失败/运行时错误，生产完整测试33/33通过，近期错误日志为0。
- 竞品气泡按每个平台独立归一化：同平台两款车中较高者满柱，另一款按相对比例显示；柱尾不显示数字。竞品粉红 `#E6A0AA`，本品淡蓝 `#9CCFE3`，占比与绝对声量仅保留在数据和悬停提示中。
- 2026-07-10 最终趋势气泡发布：GitHub `81856a6`；ECS代码归档 `backups/code_before_81856a6_20260710_234111.tar.gz`；运行数据备份 `backups/mmn_backup_20260710_234111.tar.gz`；公网资源版本 `beta-1.01-summary-platform-pair-1`。本地门禁无失败/运行时错误，生产完整测试33/33通过，近期错误日志为0。
- 顶部分析对象选择器使用完整 `modelOptions()` 品牌车型资产；汇总图表左侧使用 `dashboardModelOptions()`/`state.models`。两者必须隔离，导入文件不得缩减全局品牌车型库。
- 本机 `scripts/ensure_local_mmn.sh` 必须同时检查健康状态和后端代码新旧；`server.py` 或 `bf_factory/*.py` 新于进程标记时自动重启8765，避免静态页面已更新但上传接口仍运行旧内存代码。
- 产品评价汇总表的整体平台NSR与属性NSR必须分开：`summaryPlatformNsr`保存车型级全网/垂媒/抖音/小红书/微博/B站/视频号结果，`rows`中的全网/垂媒/抖音继续用于产品点属性NSR；禁止互相推导。
- 整体平台NSR图沿用本品固定、竞品可勾选的车型状态，平台下拉只切换平台；图表与气泡固定使用-100%至100%零轴，本品淡蓝、竞品粉红，缺失值显示缺失而不是0。
- 页面吸顶样式必须限定为 `main>header`，不得再使用全局 `header` 选择器，否则模块标题滚动后会覆盖主导航。
- 2026-07-11 整体平台NSR拆分已发布：GitHub `e7f19b1`，公网资源 `beta-1.01-platform-nsr-split-1`，ECS代码归档 `backups/code_before_e7f19b1_20260711_001551.tar.gz`；生产33/33测试通过，近期错误日志为0。
- 2026-07-10 驾驶舱轻量优化发布：GitHub `a8d3232`；分平台声量读取、点击气泡、本品锁定、竞品筛选、运行计数隐藏、产品点文案和独立尺度说明均已进入生产。
- ECS发布前代码归档：`backups/code_before_a8d3232_20260710_231425.tar.gz`；运行数据备份：`backups/mmn_backup_20260710_231425.tar.gz`；公网资源版本为 `beta-1.01-summary-platform-volume-1`。
- 本地发布门禁 `failed: []`、`runtimeErrors: []`；真实源表接口验证九个平台声量与原表一致。生产容器在仅测试进程关闭登录要求后完整测试33/33通过，运行容器健康，近期错误日志为0。

### 2026-07-10 BF界面修复发布状态

- GitHub提交：`0172cca`（规则外显清理、子菜单隐藏、三种实拍BF类型）与 `5c9c561`（前端缓存版本刷新）。
- ECS最终发布前代码归档：`backups/code_before_5c9c561_20260710_194257.tar.gz`；运行数据备份：`backups/mmn_backup_20260710_194259.tar.gz`。
- 公网已确认BF工厂横向子菜单隐藏，内部设计说明不再出现，静态实拍、动态实拍、底盘实拍三种类型正常显示。
- 生产类型库已写入 `STATIC_SHOOT`、`DYNAMIC_SHOOT`、`CHASSIS_SHOOT`；生产容器内31项测试全部通过，近期错误日志为0。

## 21. E7X决策驾驶舱证据矩阵与竞品气泡

- 决策驾驶舱的客户对象固定为汽车品牌市场营销负责人；页面输出品牌传播主张、认知竞争、内容/公关/媒介/达人资源判断，不输出门店销售话术。
- 产品评价汇总表继续作为全网、分平台和属性NSR的唯一统计基准；不得重新采集一套数据后与源表混算。
- 汽车之家正反向排名进入驾驶舱真实候选车型集合，按最新有效周期区分主力竞品、内部替代和观察竞品。
- 属性NSR矩阵必须显示证据状态；同一属性跨来源差异较大时标记来源冲突，不得仅凭单平台数值形成传播结论。
- 属性单元格保留气泡交互，点击后展示本品与竞品在全网、垂媒车主口碑、抖音的NSR和差值；本品沿用淡蓝、竞品沿用粉红。
- 2026-07-12本地发布门禁16项全部通过，`failed: []`、`runtimeErrors: []`；真实E7X数据浏览器验收无横向溢出、控制台错误0。
- 2026-07-12已同步云端：代码备份 `backups/code_before_e7x_cockpit_20260712_012528.tar.gz`，数据备份 `backups/mmn_backup_20260712_012530.tar.gz`；汽车之家关系2400条已进入云端资产库，公网资源版本 `beta-1.01-e7x-cockpit-1`。

## 22. 全局数据读取与客户空间隔离

- 数据读取必须区分 `缺失` 与真实 `0`：空值、破折号、无效文本不参与排名、均值、覆盖率和策略阈值；真实 0 必须保留并显示为 0。
- 产品评价整体 NSR 只能读取连续的“全网”区块，不得被相邻垂媒或分平台区块覆盖；没有完整全网 NSR 或属性 NSR 时必须明确拒绝导入。
- 垂媒专用格式命中后不得再进入通用解析器；同文件重导必须替换该文件快照，不得累积已消失的旧关系。
- 社媒助手允许没有标题但存在可追溯用户文本的新版导出；文本回退顺序固定为“标题、大家都在搜、视频话题、所属合集”，并保存 `titleSource`。
- 客户业务数据与浏览器缓存统一按 `org_id + edition` 隔离；机会地图上下文再加 `model`。不得以查询参数、请求体或本机旧缓存覆盖服务端认证范围。
- 历史无租户垂媒资产只允许在四张表全部仍为 `local` 且不存在任何非本地数据时，由确定的管理员空间一次性整体认领；禁止对所有客户做读取时 fallback。
- 2026-07-12 本地验收：Python 119/119、JavaScript 5/5、发布门禁无失败和运行时错误。生产库副本迁移后关系 3,267 条，其中汽车之家 1,200 条、懂车帝 2,067 条；重复执行结果不变。
- 本次云端发布前备份为 `backups/code_before_data_audit_20260712_033326.tar.gz` 与 `backups/commercial_demo_before_data_audit_20260712_033326.db`；恢复时必须同时使用同一时间点的代码和数据库快照。
- 云端部署脚本另生成 `backups/mmn_backup_20260712_033426.tar.gz`。生产迁移后垂媒关系 3,267 条（汽车之家 1,200、懂车帝 2,067），8 个批次与 8 条 Learning 保留；管理员空间可读 3,267 条，试用空间为 0。六个容器正常，健康版本码 `beta-1.01-data-audit-1`，近 10 分钟应用异常 0。
- 决策驾驶舱不再展示“NSR × 垂媒合并判断”区块；该层与后续产品证据和策略选择重复。原始 NSR、垂媒走势、竞争气泡和后续策略计算继续保留。版本码 `beta-1.01-cockpit-hide-evidence-1`，云端代码备份 `backups/code_before_cockpit_hide_evidence_20260712_034654.tar.gz`。
- 本次仅做展示层热更新，不迁移数据库；公网 HTML / JavaScript 已确认不存在旧区块及渲染函数，六个容器正常，近 5 分钟应用异常 0。
- 2026-07-11 至 2026-07-12 的驾驶舱、数据读取、租户隔离、社媒趋势、机会地图与决策资产回流变更统一归入 `beta 1.02` 系统级迭代，版本码 `beta-1.02-system-iteration-1`。后续不得再以 beta 1.01 的单点热修名称描述这批能力。
- beta 1.02 主提交为 `0481374`，已推送 GitHub `main` 并完整同步 ECS；99 个变更文件哈希一致。发布前代码/数据库备份为 `backups/code_before_beta_1_02_20260712_035556.tar.gz`、`backups/commercial_demo_before_beta_1_02_20260712_035556.db`，部署备份为 `backups/mmn_backup_20260712_040139.tar.gz`。公网版本、六个容器、管理员/试用空间隔离和 3,267 条生产关系均验证通过。

## 23. 2026-07-14 社媒与数据智能增量交接

- 最终生产提交为 `28a9ba2`，主功能提交为 `23a7f8e`；两者均在 GitHub `main`。
- 健康版本码为 `beta-1.02-20260714-social-data-1`。前端资源至少应包含 `app.js?v=beta-1.02-social-benchmark-risk-1`、`douyin-hot-demo.js?v=collector-7`、`xhs-content-ranking-demo.js?v=xhs-radar-3`。
- 社媒趋势竞品仍最多 3 台，但必须通过 MMN 品牌车型库按“品牌 → 车型”选择；本品不得进入竞品候选。
- “本品与竞品正向内容热度”必须显示本品 Benchmark 行，并与竞品使用相同的正向内容热度求和及比例尺；竞品排名仍从 1 开始。
- 本品风险 KPI 有负向内容时必须可点击和键盘操作，气泡展示具体内容、平台、作者、热度和原文；无负向内容时不得伪造下钻项。
- 品牌传播与社媒趋势的后台任务必须按组织隔离、可查询进度、同条件复用进行中任务；不同条件不得错误复用。
- 抖音热点实体只有双模型一致或人工确认后才可进入品牌/车型雷达；冲突、缺配置和异常输出不得伪装为确认实体。
- 社媒、小红书和抖音公开内容只作为传播证据，不等于完整市场需求或成交证明。
- 懂车帝销量运行时同时比较独立采集器、`data/dongchedi_sales/latest.json` 和 `latest_mmn_perception_feed.json`，只展示最新有效月份。
- Docker 的 `/app/data` 是持久化卷，镜像内数据文件会被遮挡；`scripts/deploy.sh` 必须在容器启动后把销量 feed 与最新 RAG 文件复制到该卷。不得删除这一步。
- 2026-07-14 最终公网 `/api/sales-marquee` 返回 8 个 `2026-06` 榜单；如果再次退回旧月份，优先检查卷内 feed、文件签名和应用缓存，不要先改展示文案。
- 本地验收基线：JavaScript 11/11、Python 175/175、发布门禁专项 85/85，浏览器 `failed: []`、`runtimeErrors: []`。
- 最终服务器备份：`backups/code_before_28a9ba2_20260714_022900.tar.gz` 与 `backups/mmn_backup_20260714_022931.tar.gz`；批次前备份为 `code_before_23a7f8e_20260714_022425.tar.gz` 与 `mmn_backup_20260714_022531.tar.gz`。
- 手工验收顺序固定为：健康接口 → 首页资源版本 → 社媒趋势竞品选择 → 本品 Benchmark → 风险内容气泡 → 销量月份 → 6 个服务状态 → 近期错误日志 → 本地/服务器哈希。

## 24. 2026-07-15 集团管理层看板交接

- 国内版左侧“管理层版”是看板开关，不是第三套数据版本。默认关闭；打开后将集团看板插入国内版决策驾驶舱最上方，关闭后仅隐藏该面板。
- 开关不得调用 `loadEditionData`、`resetOpportunityContextState`、`save` 或任何项目/车型/样本推导链路。
- 看板使用 E7X 五车工作簿聚合数据、懂车帝细分榜与已存社媒/VOC 证据；百分比仅表示细分榜 Top10 合计环比，不是市场份额。
- 能源形式固定拆为纯电、插混、增程和燃油；车身级别按懂车帝轿车、SUV、MPV 区间分开。
- Docker `/app/data` 持久化卷会遮挡镜像内 E7X JSON；不得删除 `scripts/deploy.sh` 中的 `data/e7x_product_evaluation_2026-06.json` 卷同步步骤。
- 云端看板请求必须复用 `authHeaders()`，否则本地免登录正常但云端会返回 401。
- 最终提交链为 `7b2651e` → `d9a9b6f` → `f2deafa`；公网 IP 首页、健康接口、管理员登录、E7X 看板开关、5 车/7 平台/15 属性数据均验收通过。

## 25. 2026-07-16 T周期卖点匹配与产品白皮书证据交接

- 上市周期以正式上市日为 T0；E7X 的首发预售为 2026-05-08，正式上市为 2026-05-29，2026-07-16 对应 T+48 销售转化期。页面周期、证据日期和营销动作必须使用同一 T 周期上下文。
- “卖点机会决策台”依次交叉验证集团战略卖点、用户 NSR 与竞品认知；产品能力证据由 PDF 白皮书进入第三模型，不能使用文件名或未核验摘要直接计分。
- 产品白皮书入口仅接受 PDF。文本可读后由 Qwen 提取、DeepSeek 复核，并以页码原文再次硬校验；只有 `dual_model_verified` 且命中当前标签的证据才计产品能力 10 分。
- 扫描件、无有效文本、页码原文不一致或双模型冲突时必须保持“待核验”，不得生成可用产品能力证据。原文在界面中使用可展开引用，避免长文本撑破卡片。
- 证据持久化于 `product_whitepaper_evidence`，按 `org_id + edition + model` 隔离；刷新后由 `GET /api/product-whitepaper/latest` 恢复。上传接口为 `POST /api/product-whitepaper/analyze`，请求体是 PDF 原始二进制。
- UI 发布门槛：1440 像素三栏完整显示且不溢出；窄桌面仅允许决策台组件内部横向滚动；390 像素页面宽度必须等于视口宽度，长标题、标签、气泡与引文均需换行或组件内展开，禁止整页横向滚动和遮盖。
- 本地发布闸门通过：114 项 Python 测试、前端专项测试、真实浏览器检查均无失败与运行时错误。真实 E7X 文件 307 页全部可读，刷新后产品证据状态与 10/10 计分稳定恢复。
- 生产提交 `a7e9541` 已推送 GitHub `main` 并部署到 `/opt/mmn-perception-engine`；代码备份 `backups/code_before_a7e9541_20260716_065811.tar.gz`，数据备份 `backups/mmn_backup_20260716_065901.tar.gz`。
- 公网版本码 `beta-1.02-20260716-whitepaper-evidence-1`；生产实测白皮书返回 `dual_model_verified`，6 个服务运行，应用与 Web 近期错误日志为 0。

## 26. 2026-07-17 全量细分市场销量预警与全局检查交接

- 功能提交 `b30f0ae` 已推送 GitHub `main`，生产最终运行版本码为 `beta-1.02-20260717-sales-warning-global-1`。
- 销量预警只监测最新懂车帝“八车周对比次数正反向排名”中的本品车型；当前生产为 8 个监测车型、8 个销量指标就绪。车型匹配忽略空白与英文大小写，不扩大到表外车型。
- 全量预警市场固定使用“车身形式 × 懂车帝尺寸 × 能源形式”；市场总量覆盖该组合全部车型，基准为排除本品后的同市场销量前 3 名竞品中位数。红/黄/绿参考线为基准的 25%/50%/80%。
- 方法说明必须由当前 `salesWarnings.summary.method` 与 `thresholds` 动态生成，禁止再次写回旧 Demo 的“有效起售价筛选、前 5 名”文案。
- 数值先由确定性规则锁定，再由双旗舰模型独立质检。生产新指纹已完成验证：旗舰模型 A/B 均为 `verified`，`managementConclusionPublished=true`。
- 管理层 05 市场结构图已扩大绘图区并修复轴与标签重叠；05—08 信息看板仅在桌面端增加高度。左侧导航、管理层开关、01—08 页签和移动端结构保持不变。
- 本地验证：232 项完整测试、127 项发布门禁、Python/JavaScript/Shell 语法及空白检查通过；1440×1000、1280×900、390×844 三档浏览器检查无横向溢出，控制台与页面运行时错误为 0。
- 生产验证：销量预警、双模型门禁、T 周期及数据读取 46 项专项测试通过；6 个服务全部运行，近 10 分钟应用与 Web 日志无错误。
- 服务器使用提交归档发布到 `/opt/mmn-perception-engine`；发布前代码备份 `backups/code_before_b30f0ae_20260717_040913.tar.gz`，运行数据备份 `backups/mmn_backup_20260717_040947.tar.gz`。
- 公网 IP 健康接口、首页资源版本与核心文件哈希均已核对；`server.py`、`group_dashboard.py`、管理层 JS/CSS 和 `index.html` 与本地一致。
- 本次全局检查修复了 1 个产品口径 BUG，并清理 1 个既往浏览器验收遗留的本地重复 8765 进程；本地已恢复 watchdog 单实例。

## 27. 2026-07-17 MMN Eval v0.1 本地评测基础

- 新增独立 `mmn_eval/` 包和 `scripts/run_mmn_eval.py`，支持严格 JSONL 合同、硬门禁、五维质量评分、人工复核队列和 baseline/candidate 回归比较。
- Rubric 版本为 `mmn-eval-v0.1`；当前阈值 80 分通过、65–79.99 分人工复核、低于 65 分失败，任一硬门禁直接失败。阈值在 MMN 负责人完成首批真实案例裁决前均属内部校准值。
- 硬门禁覆盖编造事实、未知证据、缺失写零、多模型未完成、车型配置三模型共同证据不完整、平台指标越界、来源独立性不足，以及未区分事实/推断/假设/未知。
- 合成种子文件为 `data/eval/mmn_eval_seed_v0.1.jsonl` 与 `data/eval/mmn_eval_seed_outputs_v0.1.jsonl`；种子集故意包含失败输出，整套运行返回退出码 1 是预期门禁行为。
- 操作和人工标注说明见 `docs/mmn-eval.md`；研发记录见 `docs/研发档案/2026-07-17_beta-1.02_MMN-Eval-v0.1.md`。
- 本地专项 27/27、完整 Python 269/269、发布门禁专项 127/127、浏览器 `failed: []` / `runtimeErrors: []`、语法和 `git diff --check` 通过。本次未改 UI、路由、API、数据库和模型调用，也未同步 GitHub 或 ECS。
- 下一步是脱敏抽取 20–50 条真实案例、由 MMN 负责人冻结阈值，再接全模型链路 Trace、Langfuse 数据集和正式发布门禁。

## 28. 2026-07-17 MMN Eval 可视化页面

- 在保留现有信息架构的前提下，将 `Eval评测` 加入「系统设置」，页面展示发布判断、五维质量表现、案例筛选、真实基线空状态和人工复核进度。
- 新增 `mmn_eval/dashboard.py` 作为独立数据服务；`server.py` 只增加 `GET /api/eval/report`、`POST /api/eval/run`、`POST /api/eval/human-review` 三个经过现有认证层的薄路由。
- 人工判断按 `orgId` 隔离写入 `data/eval/mmn_eval_human_reviews.json`。只有 verdict 为 `human_review` 的案例可处理，驳回必须填写理由，硬门禁不能被人工结论绕过。
- 页面只展示真实报告字段；没有 baseline 时明确显示“尚无可对比基线”。种子集持续标记为机制验证数据，不代表 MMN 真实业务能力成绩。
- 浏览器已验证页面加载、运行 Eval、待复核筛选、人工判断保存和测试数据清理；接口均返回 200，控制台和页面运行时错误为 0。截图见 `output/playwright/mmn-eval-dashboard.png` 与 `output/playwright/mmn-eval-human-review.png`。
- 本次仍是本地实现，没有同步 GitHub 或 ECS。真实 Gold Set 扩充和阈值冻结仍需 MMN 负责人介入。
# 2026-07-17｜Policy Intelligence 模块交接

- 新增市场环境变量领域模块 `policy_intelligence.py`，包含来源门禁、原文留存、结构化规则、人工审核、车型影响、五维Eval及知识门禁。
- 新增只读看板API与管理员采集/导入/解析/审核/分析/Eval API；未审核政策不进入车型计算，Eval低于80不进入机会地图。
- 决策驾驶舱新增“政策环境分析”，管理层版新增“政策环境”视图；现有模块与计算不改写。
- 采集脚本：`python3 scripts/refresh_policy_intelligence.py`。robots不允许或无法确认时必须失败并走人工官方原文导入，不得绕过。
- 完整研发记录见 `docs/研发档案/2026-07-17_beta-1.02_Policy-Intelligence-MVP.md`。

## 29. 2026-07-18 昨夜迭代统一发布基线

- 发布标识统一为 `beta-1.02-20260718-overnight-iteration-1`，覆盖 MMN Eval v0.1 与可视化、Policy Intelligence、咨询输出框架、销售预警近六个月历史和车型上市周期持久化，以及相关管理层/驾驶舱 UI 与 API 更新。
- MMN Eval 仍以机制验证为边界：合成种子不代表真实业务质量，正式 Gold Set 与阈值冻结继续由 MMN 负责人裁决；硬门禁不可被人工复核绕过。
- Policy Intelligence 只允许官方来源、人工审核和 Eval 达标内容进入车型影响与机会地图；条件未确认时只展示条件权益上限，不伪装为已获得补贴。
- 销售预警历史只读展示最近六个自然月，缺失月份明确标记且不补零；8 台车型上市日期已从浏览器存储迁移到 `data/sales_warning_cycles.json`。部署脚本仅在服务器文件不存在时初始化，服务器已有记录永不被版本文件覆盖。
- 咨询输出统一执行金字塔结构、MECE 检查、事实/推断/假设分层与证据引用；不满足框架检查的结果不作为正式 MMN 模型输出策略。
- 本地发布证据：完整 `unittest` 324/324 通过，`scripts/release_gate.sh` 通过 131 项业务专项与浏览器检查，`failed: []`、`runtimeErrors: []`；JS/Python 语法、`git diff --check` 和敏感信息扫描通过。
- 生成截图、Playwright 会话文件和 `output/mmn-eval-seed-report.*` 属验收产物，不进入发布提交；种子数据合同、研发档案、脚本和测试进入 GitHub。
- GitHub `main` 已同步至 `ec18050`；其中 `2e3e766` 修复 Eval 首次启动缺少生成报告及登录租户无政策基线，`ec18050` 确保 Eval 种子 JSONL 随部署进入 `/app/data` 持久化卷。
- ECS 最终应用重建时间为 2026-07-18 14:14。回滚点：`backups/code_before_2e3e766_20260718_141313.tar.gz`、`backups/env_before_deepseek_v4_20260718_141313`、`backups/mmn_backup_20260718_141341.tar.gz`。
- 生产管理员登录后验证：集团驾驶舱监测 8 车、Eval 运行 `seed-v0.1` 共 10 案例、Policy Intelligence 生效政策 6 条，三个业务接口均为 200；DeepSeek 快/深档分别为 `deepseek-v4-flash`、`deepseek-v4-pro`。
- 6 个 Docker 服务均运行，应用、数据库、Redis、达人 Worker、调度器健康；公网 IP 健康接口返回版本 `beta-1.02-20260718-overnight-iteration-1`。
- 公网域名剩余基础设施问题：`mmnsh.com` 的 ACME HTTP-01 被云侧返回 403，无法签发 MMN 独立证书；443 会命中同机既有 Credo 证书。当前正式验收入口使用 `http://121.40.60.90`，待备案/云侧放行后再补域名 HTTPS，不得复用 Credo 证书或改写 Credo 路由。

## 30. 2026-07-18 界面质感与全系统健康修复交接

- 最终版本码为 `beta-1.02-20260718-ui-health-2`；GitHub `main` 最终提交 `45e9bc3`，主功能提交 `eb9ca4b`。生产 `/opt/mmn-perception-engine` 使用提交归档发布，关键文件哈希与本地一致。
- 博主蒸馏孵化继续位于内容资产共享容器，但入口身份、侧栏高亮和页面标题必须保留 `bloggerskill` 语义；不要改回高亮“内容看板”。页面视觉顺序固定为证据、判断、资产，原业务字段、样本和数据入口继续保留。
- 窄屏必须保持整页无横向滚动：社媒操作与阈值单列、项目权重两列、Eval 宽表只允许组件内滚动。20 个主导航页面的 1440 与 390 像素验收已全通过。
- 策略驾驶舱管理层版 8 个视图必须保持单页签、单面板和 `1/8—8/8` 进度一致；销量预警 8 车、政策对比 7 车、4 类赛道、9 个品牌和标准版 T 周期链路均已实测。
- 云端受保护模块不得在登录门禁完成前启动。`app.js` 通过 `mmn:auth-ready` 发出就绪信号，`douyin-hot-demo.js` 只在信号到达后首次取数；本地免登录和已有有效会话仍直接启动。修改初始化顺序时必须运行 `tests/test_frontend_lifecycle_scope.js` 并做云端登录前网络检查。
- 最终本地基线：Python 378/378、全部 JavaScript 专项、发布门禁 137 项业务专项及真实浏览器检查通过，失败与运行时错误均为 0。
- 生产管理员数据基线：Eval 10 案例、Policy Intelligence 6 条政策、销量预警 8 台、政策对比 7 台、博主样本 407 条、内容知识 752 条；6 个服务运行且核心服务健康，近期错误日志为 0。
- 回滚证据：代码归档 `backups/code_before_eb9ca4b_20260718_195928.tar.gz`，运行数据备份 `backups/mmn_backup_20260718_200003.tar.gz` 与 `backups/mmn_backup_20260718_201452.tar.gz`。回滚时保持代码和同一时间点数据成对恢复。
- 公网正式验收继续使用 `http://121.40.60.90`。`mmnsh.com` 的 ICP/云侧 403 和 443 证书问题仍属外部基础设施阻塞；不要复用 Credo 证书或改写 Credo 路由。

## 31. 2026-07-19 全板块客户发布交接

- 全板块功能提交为 `cab9914`，客户端断开终态修复提交为 `545cb74`，最终版本码为 `beta-1.02-20260719-global-readiness-2`。
- `scripts/release_gate_all_surfaces.js` 是全板块客户发布门禁：动态遍历全部未隐藏主导航，当前应为 19 个；再遍历管理层 8 个视图。新增或删除客户板块时必须同步调整数量合约。
- 门禁默认验收本地免登录环境；设置 `MMN_URL`、`MMN_USERNAME`、`MMN_PASSWORD` 后可对云端登录环境执行同一套验收。账号与密码只能从服务器环境读取，不写入脚本、日志或文档。
- 发现并修复的产品 BUG 为：390 像素下管理层第 3 视图“销量预警”与第 4 视图“赛道环境”的桌面端可见溢出规则撑宽整页。现在整页宽度固定为视口宽度，宽表与宽图仅在组件内滑动。
- 生产全板块快速切页后暴露过客户端主动断开时的写回异常；`Handler.send_json` 现在对 `BrokenPipeError`、`ConnectionResetError` 和 `ConnectionAbortedError` 安静关闭连接，禁止再进入业务异常二次回写。
- 本地完整基线为 386/386；本地和生产的 1440/390 全板块浏览器结果均为 `failed: []`、`runtimeErrors: []`、`failedResponses: []`。
- 生产数据基线：管理看板 8 台监测车型、4 类赛道、7 台政策对比车型、9 个品牌；Eval 10 个案例；博主能力库 507 条来源、507 条样本、3 个画像、519 个知识片段。新账号孵化任务库与旧能力库是两个状态，任务库为 0 不得解读为博主样本丢失。
- ECS 于 2026-07-19 03:08 完成最终重建，6 个服务正常、不健康服务 0。重复生产全板块验收后 HTTP 5xx、Traceback 与断开写回异常均为 0。回滚点为 `backups/code_before_545cb74_20260719_030651.tar.gz`、`backups/env_before_545cb74_20260719_030651`、`backups/mmn_backup_20260719_030708.tar.gz`。
- 生产持久化数据已保护：发布时明确保留服务器已有 `sales_warning_cycles.json`，不用仓库版本覆盖；代码、环境和运行数据有三个独立回滚点。
- 公网正式验收入口仍为 `http://121.40.60.90`。独立域名 HTTPS 的备案/云侧放行与证书问题仍是客户公网上线前的外部基础设施风险，本次未修改其他产品的域名或路由。

## 32. 2026-07-20 决策驾驶舱与博主蒸馏交付闭环

- 最终生产版本为 `beta-1.02-20260720-cockpit-creator-2`；GitHub `main` 提交链为 `b805069` → `ddaab3b`。服务器 `server.py` 与 `index.html` 的 SHA-256 已与本地逐文件核对一致。
- 决策驾驶舱新增车型线索看板，并通过共享车型上下文与 T 周期、产品、NSR 和策略模块联动。集团周报、管理层简报和赛道判断必须读取同一已发布周度批次，不得混用不同周次。
- 周度市场数据在周二 00:00 主抓，未发布时周二至周五上午补抓；月度销量预警独立刷新。状态必须区分已发布、等待发布、失败与基线，不得把超时或缺失写成业务结论。
- 新能源销量预警按尺寸与统一新能源组比较，统一新能源包括纯电、增程和插混；基准仍为排除本品后的同市场销量前三竞品中位数，阈值为 25%/50%/80%。生产当前为 8 台上汽车型、22 款市场车型、3 台预警。
- 博主导入任务表为 `blogger_skill_import_jobs`，阶段固定为 `import → distillation → analysis → delivery`。服务重启会把未完成任务安全标记为失败，用户可使用保留的原始文件重试；只有完整能力卡和全部阶段门禁通过才能到 100%。
- 导入成功后必须同时写入博主能力库与内容能力资产库。`ddaab3b` 修复了此前“能力卡已完成但达人未进入脚本选择器”的断链，并增加“导入后可选达人”回归测试；以后不得只检查导入任务状态而忽略下游资产发布。
- 线上 `超哥超车` 任务 `blogger_import_8b888a6c065441919155312f0825edb4` 已用用户原始 Excel 验证：500 条样本、12 条进度事件、完整能力卡、30 天选题库，最终 100%，错误为空。内容资产库现有 3 位达人，`超哥超车` 对应 500 条抖音能力片段。
- 脚本任务表为 `creator_script_jobs`。四个平台必须保持独立规则；交付字段固定包含标题、开头钩子、完整口播、字幕重点、画面建议、事实边界和质量说明，同时提供复制、Word 导出、修改重生成、持久化进度、失败原因和重试。
- 线上 `超哥超车` 脚本任务 `creator_script_017ac22166f64f9e88ec6d33fc82a8ed` 已验证：抖音、100%、320 字完整口播、5 条字幕、5 组画面、自然表达通过、模板短语命中 0、Word 为有效 OOXML。
- 本地完整测试为 `421/421`；最终修复专项 `28/28`。生产全表面门禁为桌面和 390 像素各 19 个客户页面、8 个管理视图，`failed`、`runtimeErrors`、`failedResponses` 均为空，整页横向溢出为 0。
- 最终 6 个容器运行且核心服务健康。发布窗口精确 HTTP 5xx、Traceback、BrokenPipeError、应用 ERROR 均为 0；3 条 nginx 499 是自动验收快速切页时客户端主动取消，不能误记为服务端 5xx。
- 发布时保留服务器 `.env`、持久化数据库与 `sales_warning_cycles.json`。整批发布前回滚点：`backups/mmn_source_pre_20260720_032628.tar.gz`、`backups/mmn_env_pre_20260720_032628.env`、`backups/mmn_backup_20260720_032650.tar.gz`；修复前回滚点：`backups/mmn_source_pre_hotfix_20260720_034051.tar.gz`、`backups/mmn_backup_20260720_034052.tar.gz`。
- 正式公网验收继续使用 `http://121.40.60.90`。独立域名 HTTPS 的备案、云侧放行和证书问题仍为外部基础设施风险，不得复用其他产品证书或修改其他产品路由。
