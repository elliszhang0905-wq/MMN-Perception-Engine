# MMN工单：E7X线索One-Page图表看板

## 0. 工单元信息

- 工单日期：2026-07-24
- 工单状态：本地完成
- 产品负责人：Ellis
- 产品范围：MMN国内版决策驾驶舱中的车型线索看板
- 当前对象：奥迪E7X
- 权限边界：允许本地代码、样式、测试和真实本地页面验收；不授权提交、推送、部署或生产数据写入
- 数据边界：只使用现有E7X阶段目标、实际线索、实际订单、销量预警和既有归因研判结果

## 1. 一句话目标

把当前卡片式E7X线索汇总重构为一屏可读的阶段图表看板，让管理者先看出“哪一阶段发生线索与订单表现背离”，再查看当前阶段进度、研判与证据边界。

## 2. 用户价值

当前页面需要在四个阶段卡片和三个判断卡片间人工比较八个达成率。新版以“线索与订单阶段达成”双轨图为视觉主轴，保留100%目标基准，并把7月未完结周期明确标记为“进行中”，降低扫读成本和误判风险。

## 3. 范围

### 3.1 本期必须实现

1. 保留MMN现有信息架构、车型上下文和管理层驾驶舱位置，不新建页面或导航。
2. 顶部显示E7X、销量预警、累计销量、当前T阶段和“车型总体线索｜暂未分平台”。
3. 使用现有四阶段数据绘制线索/订单达成双轨图，保留100%目标基准。
4. 视觉突出6.16—6.30线索117.7%、订单64.6%的表现背离。
5. 7.1—7.31明确显示“进行中”，并提示不能按完整月直接判定。
6. 当前阶段独立显示线索、订单的实际值、目标值和达成率。
7. 底部保留阶段异常、跨域归因研判、全局联动，以及原有三路复核弹层。
8. 页尾持续显示来源和证据边界。
9. 支持1440px桌面和390px移动端；移动端不得产生页面级横向溢出。

### 3.2 明确不做

- 不新增或推断平台来源、CPL、有效线索率、意向等级或自然/投放拆分。
- 不把订单与线索表现背离命名为真实转化率下降。
- 不进入门店、销售顾问、跟进过程或具体成交承接操作。
- 不修改E7X业务数据、SQLite、销量预警规则、归因接口或三路复核契约。
- 不提交、推送、部署或发布生产环境。

## 4. 工程契约

### 4.1 输入

- `leadData[model].phases[]`
  - `name`
  - `leadTarget`
  - `leadActual`
  - `leadRate`
  - `orderTarget`
  - `orderActual`
  - `orderRate`
- `warningContext`及既有`MMNVehicleContext`
- 既有`/api/attribution-reasoning`结果

### 4.2 输出

- `lead-dashboard.js`输出语义化、可访问的阶段图表DOM。
- `lead-dashboard.css`输出MMN视觉体系内的桌面/移动端布局。
- 图表数值文本必须保留，颜色和柱高不能成为唯一的信息载体。
- 动态柱高只由现有`leadRate/orderRate`计算，最大显示尺度为150%；超出时截断视觉高度但不得修改文本值。

### 4.3 不变量

- E7X四阶段原始值不变。
- 车型仍由销量预警和全局车型上下文统一控制。
- 非E7X车型仍使用现有缺失状态，不借用E7X数据。
- 三路复核的加载、运行、失败、分歧、对齐和弹层键盘行为不变。
- 客户界面不显示模型、插件、供应商或内部技术名称。

## 5. 实施任务

### Task 1：静态UI合同

- 更新`tests/test_lead_dashboard_ui.js`，锁定主图、100%目标线、当前阶段进度、进行中说明、证据边界与响应式布局。
- 先运行测试确认RED，再实现到GREEN。

### Task 2：One-Page结构

- 重构`lead-dashboard.js`渲染结构。
- 保留既有归因逻辑、事件监听和车型联动。
- 使用现有数据生成四阶段图表，不新增后端字段。

### Task 3：MMN视觉与响应式

- 重构`lead-dashboard.css`，建立主图/侧栏/判断区的信息层级。
- 桌面保持一屏高密度扫读；移动端改为单列和两列阶段图，不产生页面级横向滚动。
- 提供键盘焦点、文本状态和非颜色提示。

### Task 4：真实流程验收

- 执行Node UI合同、JavaScript语法、差异检查及相关回归测试。
- 本地启动MMN，验证1440px和390px。
- 检查DOM、网络、控制台、车型联动、归因弹层和缺失车型状态。

## 6. 验收标准

### 正常路径

- E7X进入页面后显示四阶段双轨图，八个达成率与现有数据一致。
- 100%目标基准清晰可见。
- 6月下半月表现背离成为主视觉焦点。
- 归因弹层可点击打开、Escape关闭，并保留三路复核状态。

### 边界路径

- 7月显示“进行中”和“月度进行中，不按完整月直接判定”。
- 未知车型不展示E7X数据，显示真实缺失状态。
- 图表柱高受到150%视觉上限保护，文本值仍显示原值。

### 失败与恢复

- 归因接口失败时保留页面事实数据并显示既有失败状态，不伪造研判。
- 页面刷新后仍从现有静态数据和车型上下文恢复同一看板。
- 归因弹层关闭后焦点返回触发按钮。

### 数据保护

- 不修改任何数据库、Excel、销量快讯或E7X阶段数据。
- 不覆盖工作树中其他任务的未提交改动。

### 真实页面

- 1440px：主图、当前阶段侧栏和三块判断区清晰，无重叠或截断。
- 390px：内容单列，阶段图可读，页面无横向溢出。
- 控制台0错误；相关请求状态符合既有契约。
- 页面文本持续声明“暂未分平台”和“不能认定为真实转化率下降”。

## 7. 完成口径

只有代码合同、相关回归和本地真实业务页面全部通过，才可标记为“本地完成”。本工单不包含Git提交、远端推送、ECS同步或生产验收。

## 8. 本地验收记录

- Node UI合同、JavaScript语法、归因研判单元/API回归、集团看板UI回归和全页面版本门禁均通过。
- 1440px真实页面：四阶段双轨图、100%目标线、当前阶段进度和三块判断区无重叠或截断。
- 390px真实页面：阶段图按2×2排列、判断区单列，`scrollWidth = clientWidth = 390`。
- E7X刷新后仍由全局车型上下文恢复；非E7X车型保持真实缺失状态，不借用E7X数据。
- 归因弹层可打开，Escape关闭后焦点返回“展开完整归因推理论证”按钮。
- 控制台0错误、0警告；E7X集团看板、归因研判及关联读取请求均返回成功状态。
- 页面加载触发了既有产品评价目录幂等写回；数据集版本、指纹和业务内容未变化，仅`updated_at`刷新。本次实现未主动修改业务数据或接口。
- 本地实现未提交、未推送、未部署。
