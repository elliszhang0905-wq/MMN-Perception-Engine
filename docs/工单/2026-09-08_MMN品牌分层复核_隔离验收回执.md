# MMN 品牌分层复核：隔离验收回执

日期：2026-09-08。工单：`MMN-BRAND-REVIEW-20260908-001`。

结论：实施候选及隔离工程验收完成；整张部署工单尚未完成。未提交、推送、本地部署、服务器部署或启用，未进行真实付费模型调用。

## 本轮解决范围

- 按来源信息、传播判断、条件化动作、未知分别确定发布资格，不再因一项分歧隐藏整张品牌卡；同时覆盖本品逐竞品区块。
- 保留三路独立复核。只修复失败槽位，品牌复核最多六次调用；有实质反对、证据不成立或复核不完整的判断继续阻断，不用多数投票或置信度选文。
- 增加追加式复核版本、人工采纳/修改/否决、原因、版本冲突与幂等保护。人工不能绕过来源、时间、对象和隔离硬门禁；随意改写不能自动发布。
- 取消与发布通过锁串行判断：取消先成功则不追加结论，已保存时返回冲突，不谎报取消成功。已派发远程请求不保证即时终止或退费。
- 默认 legacy、白名单为空；历史快照不覆盖，旧 v3 策略不修改。页面导航、品牌区块与视觉令牌保持原结构。
- 离线评测工具和部署脚本仅代码模式已提供；不复制业务资产、不无条件重启后台服务，候选验证失败与标签提升失败有恢复路径。

能力边界：自动解释首版只覆盖可确定核验的来源提及及双品牌同平台样本提及，候选动作限完整条件的人工观察。受限同命题中性差异可人工确认；任意营销意义、策略优先级、消费者替代或销量因果仍需证据与人工判断。这不等于通用语义裁决已完成。

## 验收证据

| 项目 | 已核验结果 | 边界 |
| --- | --- | --- |
| 完整发布门禁 | 863 项 Python、97 个模块；38 份前端测试文件；98 项既有浏览器检查通过 | 独立副本、无凭据、默认关闭；不是实际部署 |
| 新 UI 与夹具 | 27 项 UI、7 项夹具安全测试通过 | 拒绝临时数据库符号链接/硬链接逃逸；精确 CSP 校验 |
| 新真实浏览器流程 | 开始 202、取消 200→cancelled、重新分析、来源采纳、受限提案采纳、修改、否决、刷新均通过 | 真实 HTTP/服务端/SQLite，供应商边界使用明确合成替身 |
| 状态持久化 | 来源采纳仍 source_only；受限提案为 human_confirmed；修改后待确认；否决来源会阻断依赖判断 | 人工确认不冒充三路共识 |
| 页面与键盘 | 1440/1280/390，无横向溢出/嵌套按钮/运行错误；手机 Enter、双向 Tab 循环、Escape/关闭焦点恢复通过 | 不增加 allow-forms 或 allow-same-origin |
| 数据保护 | 默认关闭门禁的 86 张表：启动、浏览器、源备份逻辑变化均为零；quick_check=ok；门禁期间工作树零漂移 | 合成专用实例的允许追加写入单独记录，不声称它零写入 |
| 部署工具 | 17 项替身 Docker 测试及独立复审通过 | 未实际启动 Docker、SSH 或发布 |
| 离线评测工具 | 13 项机制测试及独立复审通过；实际合成回放 not_ready | 无真实 Gold/审批；准确率、恢复率、真实成本与时延不发布 |
| 独立复审 | 策略、运行、取消、界面、评测、部署增量均无剩余 Critical/Required | 不替代真实业务标注或双环境验收 |

核心规则 `brand-review-layered-v1`；输出 schema `brand-penetration-analysis-v4`。候选基线为 `3c8987b56f17552cdcd0240c0ff8bc329eba212e`，当前未提交，不能将基线 SHA 当作本轮发布版本。

工作树：`/Users/ellis/Documents/MMN汽车营销引擎/.worktrees/mmn-brand-review-v4-20260908`。

证据位于工作树下：

- `output/playwright/brand-review-v4/final-release-gate.log`
- `output/playwright/brand-review-v4/w3-synthetic-verification.json`
- `output/playwright/brand-review-v4/w3-history-verification.json`
- `output/playwright/brand-review-v4/w3-keyboard-verification.json`
- `output/playwright/brand-review-v4/w3-synthetic-clean-1440.png`
- `output/playwright/brand-review-v4/w3-synthetic-clean-390.png`
- `output/playwright/brand-review-v4/w3-dialog-keyboard-390.png`
- `output/playwright/brand-review-v4/eval/synthetic-replay-report.json`

合成实例仅使用 `/tmp/mmn-brand-review-browser.51WAqR/data`，本轮已停止。临时副本和证据保留。一个确认七张表均为空的候选测试数据库已移至上述忽略目录保存，没有删除用户业务数据。

## 未完成项与发布前条件

1. 候选与当前受管本地运行包仍有 38 项跟踪文件差异。必须先对齐运行基线与已有功能，禁止整包覆盖。
2. 匹配截图的历史结果已定位，但未保存三路原始输出；不得伪造。须按主工单补齐负责人批准的真实案例、独立保留集及门槛验收。
3. 本地和服务器分别需要明确发布授权、备份与精确制品、显式复核表迁移、灰度白名单、实际运行验证、数据保护及回滚验收。Linux 容器和真实模型成本/延迟未验收。
4. 当前未改变受管 8765、服务器配置、模型密钥或其他产品运行；应用版本常量仍为候选基线，不宣称新发布版本。
5. 既有项目配置表单存在编辑顺序问题：先修改项目名/本品再增删竞品可能重置未保存输入。本轮未扩大范围修复；先调整竞品，再填写项目名/本品并保存可避开。

中途失败不计最终成功：旧 CSP 阻止执行、沙箱表单提交、焦点逃逸和夹具排序问题均已修复重验；早期缺少文档检查工具路径的门禁记录只作过程记录。最终门禁后仅同步本回执与状态文档并再次检查文档合同，业务源码未再改变。
