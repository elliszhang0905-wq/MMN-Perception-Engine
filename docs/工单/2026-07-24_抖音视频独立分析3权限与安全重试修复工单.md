# 抖音视频独立分析3权限与安全重试修复工单

- 日期：2026-07-24
- 状态：生产完成
- 负责人：Codex
- 授权：实施、测试、提交、远端推送、部署与生产验收

## 1. 目标与业务价值

修复抖音热门视频洞察中“MMN独立分析 3 · 失败”的真实根因，使第三路分析能够读取与前两路完全相同的冻结证据包并形成可校验结果；同时让失败提示反映实际调用模型和可执行原因，避免旧模型名误导排障。

业务价值：

- 保住三路独立盲审和确定性聚合的质量门禁。
- 失败时保留成功结果，只重试失败项，避免重复取证和无效模型成本。
- 客户界面继续使用中性槽位名称，不暴露供应商；内部诊断保留可操作信息。

## 2. 当前事实

- 视频：抖音 `7663847419184077925`。
- 前两路已完成，第三路连续三次返回 HTTP 403。
- 当前深度配置为 `kimi-k2.6`，现有错误提示仍固定写成 `kimi-k2.5`。
- 本地工作树已有其他未提交修改与业务数据，本工单不得覆盖或清理它们。

## 3. 范围

### 包含

1. 核对现有 Key、模型、端点组合的最小调用可用性。
2. 仅在证据证明可用后更新本地 Kimi 运行配置。
3. 将 401/403 提示改为基于实际配置的、脱敏且可执行的错误信息。
4. 增加配置选择和错误映射回归测试。
5. 备份数据库后重启本地服务。
6. 对目标视频仅重试失败槽位 3，复用前两路结果与原证据包。
7. 核对任务状态、三路运行记录、证据指纹与页面展示。

### 初始不包含（2026-07-25 后续发布授权已扩展）

- 改写前两路已保存结果。
- 重抓榜单或重新提取视频证据。
- 降级为两路结果冒充三路完成。
- 静默替换第三路为前两路任一模型。
- 修改 MMN 之外的 Sales Credo 配置或凭据。
- 初始授权不含提交、部署或生产环境变更；后续“一次性全部执行完毕”已明确扩展为完整发布权限。

## 4. 工程契约

1. 三路分析必须接收同一 `evidenceFingerprint` 和同一冻结证据包。
2. 模型不可读取其他槽位输出；聚合只能发生在三路独立返回之后。
3. 第三路失败时，任务保持 `incomplete`，不得发布为完整交叉验证结论。
4. 单槽位重试必须复用证据指纹一致的已完成槽位；如果前置结果缺失或指纹不一致，系统才允许重新运行三路。
5. 外部页面只显示“MMN独立分析 1/2/3”；供应商、Key、业务空间和原始响应仅限内部诊断。
6. 错误信息不得包含 API Key、请求头或未经脱敏的供应商响应。
7. 任何数据库写入前必须完成可恢复备份，并记录目标任务主键。

## 5. 实施任务

### 任务 1：最小化权限复现

验收：

- [x] 分别验证当前 Kimi Key 与现有百炼 Key 的模型访问结果。
- [x] 只输出 HTTP 状态、错误码和脱敏信息，不输出密钥。
- [x] 确认最终采用的模型 ID、端点和 Key 来源。

### 任务 2：运行配置与错误提示修复

验收：

- [x] 深度分析实际使用已验证可用的配置。
- [x] 403 提示引用实际模型，不再固定写成 `kimi-k2.5`。
- [x] 401、403、超时和其他 HTTP 错误保持可区分。
- [x] 客户页面不新增供应商名称泄漏。

### 任务 3：回归保护

验收：

- [x] 配置测试覆盖默认、环境变量覆盖和深度档位。
- [x] HTTP 错误映射测试覆盖实际模型名和脱敏边界。
- [x] 视频洞察测试证明失败槽位安全重试会保留其余成功槽位。

### 任务 4：真实流程验收

验收：

- [x] 备份前后数据库路径和目标任务主键可追溯。
- [x] 本地服务重启后 `/api/ai/status` 反映新配置。
- [x] 修复后的目标视频重试仅新增槽位 3 记录。
- [x] 三路最新记录均完成且证据指纹一致。
- [x] 页面不再显示“独立分析 3 · 失败”。

## 6. 场景验收矩阵

| 场景 | 预期 |
|---|---|
| 正常 | 三路完成，进入确定性聚合；页面展示完整或需人工裁决的真实状态 |
| 边界 | 两路完成、一路失败时保留成功结果，不发布完整结论 |
| 失败 | 401/403/超时分别给出脱敏、可执行原因 |
| 恢复 | 权限恢复后只重试失败槽位，证据不重抓、成功槽位不覆盖 |
| 持久化 | 刷新页面或重启服务后，运行历史、失败原因和完成状态仍存在 |
| 真实业务流 | 从目标视频卡片的失败状态进入安全重试并回到完成/人工裁决状态 |
| 桌面端 | 当前桌面驾驶舱状态、按钮和结果文案正确 |
| 移动端 | 不新增布局或信息泄漏回归；相关现有 UI 测试继续通过 |
| 回归 | 三路独立、共同证据、人工最终裁决和供应商名称隐藏规则不变 |

## 7. 完成门禁

只有同时满足以下条件才可将工单标记为“本地完成”：

1. 针对性自动测试通过。
2. 真实目标视频的第三路调用成功。
3. 三路最新结果证据指纹一致。
4. 页面状态与数据库状态一致。
5. 明确报告本地完成，不把未提交、未部署描述为已发布。

## 8. 本地实施结果

- 根因一：阿里云返回 `AllocationQuota.FreeTierOnly`，免费额度耗尽且开启“仅使用免费额度”。关闭该开关后，`kimi-k2.6` 最小调用返回 HTTP 200。
- 根因二：旧代码把所有 Kimi 403 固定解释为 `kimi-k2.5` 权限错误，现已按真实错误码和实际配置模型生成内部诊断；客户页面只显示中性额度恢复指引。
- 根因三：旧的“单项重试”会清空冻结证据包并重新取证，导致证据指纹变化和三路全部重跑。现已保留原证据包、原结果和证据指纹，只运行失败槽位。
- 根因四：第三路视频洞察沿用 90 秒通用复核时限。现设独立可配置时限 `MMN_VIDEO_INSIGHT_KIMI_TIMEOUT=180`。
- 针对性验证：35 个 Python 测试通过，客户界面契约测试通过。
- 业务任务：`1ae3903c9da049e6a799af71a612af75`。
- 最终证据指纹：`26cd3647fbe5c5fa9d0e035d90b4d22e5e08b3c49abfefc4b7dd97df84976476`。
- 最新三路状态：全部 `completed`；验证状态 `verified`；共同证据 5 条；`retryable=0`。
- 修复后重试前后计数：槽位 1 保持 4 次，槽位 2 保持 4 次，槽位 3 从 4 次增加到 5 次。
- 页面验收：桌面驾驶舱显示三路“已完成”，刷新后持久；不显示失败原因、重试按钮、供应商或模型名。
- 数据备份：
  - `data/commercial_demo.db.bak-20260724-video-insight3-pre`
  - `data/commercial_demo.db.bak-20260724-video-insight3-pre-attempt5`
- 过程说明：修复前的第 4 次尝试暴露了重新取证和 90 秒超时缺陷，并新增了三路第 4 次历史记录；记录未删除或改写。修复后的第 5 次尝试才满足单槽位、同证据指纹验收。
- 发布结果：应用提交 `70d17d501d289683555a4227f1ba47f4f08de197` 已同步 GitHub `main`、发布分支与 ECS，发布标签为 `beta-1.03-20260725-video-insight-retry-1`。
- 生产真实调用：`kimi-k2.6` 最小深度调用返回有效结果，未再出现免费额度拒绝。
- 生产业务流：视频 `7661947647062083663` 的持久任务保持 `completed / verified`，三路运行均为 `completed` 且证据指纹一致；1440px 与 390px 页面均显示三路完成，无供应商泄漏、溢出、控制台错误或失败请求。
- 数据保护：部署前后均为 77 张表、21,763 行；验收新增的 2 条项目快照按主键精确清理后，逻辑指纹恢复为 `4eece0c2ea4bd82a3e81426143d2f7296143456cf10788a7fa99dd3ac657cd07`，`quick_check=ok`。
- 生产备份：源码 `backups/code_before_video_insight_retry_20260725_014848.tar.gz`，环境 `backups/env_before_video_insight_retry_20260725_014848`，持久数据 `backups/mmn_backup_20260725_014848.tar.gz`；部署脚本另生成 `backups/mmn_backup_20260725_014910.tar.gz`。
- 运行状态：六个 Compose 服务正常，应用错误与真实 HTTP 5xx 均为 0。
