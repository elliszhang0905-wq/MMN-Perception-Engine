# MMN 品牌分层复核：双环境代码部署回执

- 工单：`MMN-BRAND-REVIEW-20260908-001`。
- 日期：2026-09-08，Asia/Shanghai。
- 当前阶段：两端代码切换、健康和只读页面验收通过；服务器恢复入口后 15 分 9 秒受控观察通过。
- 范围：用户明确要求“部署本地、服务器”；本轮仅安装已验证代码，不启用 v4，不进行付费调用或历史重算。
- 整单不关闭：真实 Gold、质量门禁、负责人批准、受控启用与真实新分析/人工裁决验收尚未完成。

## 1. 制品与环境身份

| 项目 | 实际结果 |
| --- | --- |
| 版本及已推送标签 | `beta-1.03-20260908-brand-review-v4-1` |
| 已推送代码提交 | `98f017f310e10bb81cd8dcfab4515b5f0cdd78a0` |
| 发布分支 | `codex/mmn-brand-review-v4-20260908`，本轮不更新 GitHub main |
| 本地受管入口 | `http://127.0.0.1:8765` |
| 本地物理 release | `c9fbda7b4585cf9896e77453377173205479b1bd3ec191b27f5815ef05d6e322` |
| 本地旧 release | `091f95c67773ce53868f82579cfe0fd705e3c88ed1d096bc380daa8d74582800` |
| ECS 入口 | 公网 `http://121.40.60.90`；源站代理 `127.0.0.1:8080` |
| ECS 正式应用 | `78c563457e194d6412f343d92749a10fd1b763d1e6c54fc7c35eedc8e4007bb8` |
| ECS 正式镜像 | `sha256:76da6281de5cbde3191fd282df9f11d9c16b9ff5ccc1409c556fd280f5c5b96a` |
| ECS 旧镜像 | `sha256:38c022d9ff516987af7cd0d25d3d08a0e482d2131a511f4f12d8fc5530d01c81` |
| Git 运行归档 SHA-256 | `4a43a75e37d69eef1ee48c3b5428e9bb2344b47a7c546f04a1dcf5c8a95c7d44` |
| 规则 / schema | `brand-review-layered-v1` / `brand-penetration-analysis-v4` |
| 两端开关 | `legacy`，allowlist 空；没有启用范围 |

本地从旧物理包增量整合，只改 5 个既有文件、新增 4 个模块；保留 143 个未受影响文件及本地既有 UI、受管运行与 runtime-only 文件。服务器复用当前健康镜像依赖层，只叠加 Git 提交的运行文件，保留 Compose、Dockerfile、数据卷与原后台容器。两端不是整包相同，禁止将本地 38 项基线差异和 12 个 runtime-only 文件复制到 ECS。

四个新模块两端字节完全相同：

| 文件 | SHA-256 |
| --- | --- |
| brand_review_policy.py | `ae66039681383c975454dd15276a0e019b3d006c00c796dae1b741ca964484ab` |
| brand_review_repository.py | `462f1360df3646b3c7f53be0bfe955a974ae45f61cd86f027a93d6a1805b4968` |
| brand_review_runtime.py | `3770de30d272d066399bab551feb082e52fb92df7a9d1170ac50a3c681054275` |
| mmn_eval/brand_review.py | `fc2622bacbe3bb8cf07a86a9455083bea2df2265be5e1316f5820ca44f4a7ad5` |

## 2. 实施与自动化验收

- 版本和 app 缓存标识补齐后，清洁环境完整门禁重新通过：863 项 Python（97 模块）、38 份前端测试文件、98 项既有浏览器检查；86 张表逻辑零变化，工作树零漂移。
- 本地增量包：95 项品牌 Python、27 项新 UI、11 项知识工作区、14 项完整旧品牌模块测试通过；默认关闭导入不创建新库。
- ECS Linux 最终依赖层：98 项测试在无网络、无凭据、只读根文件系统与隔离数据下通过。真实业务库一致性副本中的默认关闭启动/读取没有逻辑变化。
- 独立审查已确认代码及本地静态整合 Critical/Required 为 0。备份辅助脚本最初遗漏零字节历史 DB 文件，已修复并独立复验；最终备份覆盖全部文件。
- 隔离 Linux 预检第一次因默认公网绑定且关闭登录被安全门禁拒绝，随后将隔离实例绑定回环，重新通过。未因此修改正式安全配置。
- 本地使用受管 publisher 与 agent 完成切换，未手工改 live 源码。服务器使用仅代码蓝绿流程，候选健康并接流后才替换正式应用，正式健康后切回原应用名称。

## 3. 真实只读验收与数据保护

- 两端健康接口返回新版本，活动任务 0，持久化任务检查 `ok`。
- 1440px 和 390px 实际页面检查通过：导航、项目配置展开、键盘关闭、刷新、iframe sandbox、横向布局；无捕获的页面 JavaScript 异常或 HTTP 5xx。
- 本地保留旧真实数据库快照：194 条品牌匹配内容，日期 2026-07-22。未重算或改写旧结论。
- ECS 真实管理员登录后，当前品牌组合仍是部署前已有的“尚未形成公开传播证据集”状态。其既有 V2 路径未更改，不将空态当作新功能产出，也不复制本地快照填充服务器。
- 新接口真实 GET 返回 `legacy` / `enabledForScope=false` / `review=null`。服务器未认证读取为 401，数据库路径为 404。
- 六项公网静态资产返回 200、MIME 正确且哈希匹配；服务器 9 项运行文件宿主/正式容器哈希一致。本地 HTTP 资源与新物理 release 一致。
- 浏览器验收真实登录与 GET 不模拟；为防止旧启动流程写入，其他启动 POST 在浏览器中隔离。此次不构成正式写入、模型新分析或人工裁决业务验收。
- 本地 6 个 SQLite 文件逐表逻辑完全一致：86/9/7/1 张表及 2 个零字节历史文件。ECS 6 个 SQLite 文件完全一致：84/9/7 张表、2 个各 41 表的历史备份库及 1 个零字节文件。所有非空库 `quick_check=ok`；没有新增 v4 表。
- 本地 52 个、ECS 44 个非数据库数据文件与冻结备份哈希一致。本地 tar 额外生成的 80 个 AppleDouble 元数据条目按文件魔数识别，不冒充源目录业务文件，也不构成业务文件删除。PostgreSQL 冻结 dump 与当前规范化 schema/data 输出一致；只排除 dump 随机安全令牌，不忽略业务行。
- 本地配置字节不变、状态无迁移。ECS 环境仅修改版本镜像标签，原 nginx、Compose、Dockerfile 已恢复/保留；三个原后台容器及其原镜像恢复健康，数据库、缓存、代理容器没有重建。

## 4. 维护与观察

- ECS 写入口冻结：2026-09-08 19:44:33 CST 起，暂拒绝非 GET/HEAD/OPTIONS 请求；确认活动/排队任务为空后停止原三后台容器。
- 正式新应用接流：19:48:41 CST；未同步任何版本数据资产。
- 原 nginx 和后台恢复完成：19:51:05 CST；15 分钟观察从此时计时。
- 最终观察终点：20:06:14 CST（12:06:14 UTC），从恢复入口起累计 909 秒。应用、调度器、社媒后台、达人后台和代理的容器日志严重模式匹配均为 0，代理 HTTP 5xx 为 0；最终健康正常，活动任务 0，持久化任务检查 `ok`。调度器文件日志在恢复时更新后没有新增内容。最终 SQLite 与非数据库资产比较仍一致。
- 调度器在有界停止时退出码 137，恢复后同一容器健康；不将所有退出都称为优雅退出。无活动任务被本轮取消。
- 此观察是低流量、默认关闭的安装安全验证，不是新规则正确率、长期稳定性或付费链路验证。

## 5. 备份与代码回退

本地根：`/Users/ellis/Library/Application Support/MMN/local-runtime`。

- 专项备份：`backups/brand-review-v4-20260908.1Uypkj`，含只读一致性恢复验证的 6 个 SQLite 文件、停写后全状态归档与权限受限的原配置。
- 受管发布记录：`backups/20260908T113347.675855Z-publish`；agent 记录：`backups/20260908T114002.129182Z-agent`。
- 回退沿用受管发布器，目标为本回执旧物理 release；切换前确认当前身份和任务状态，再经受管 agent 重启及验证。不得在 immutable release 中手工覆盖文件。

服务器专项备份：`/opt/mmn-perception-engine/backups/releases/brand-review-v4-20260908.IbMUSs`。

- 保存原环境、Compose、Dockerfile、nginx、源码归档、冻结全数据 tar 和 PostgreSQL dump。SQLite 一致性备份位于备份卷 `/app/backups/brand-review-v4-20260908.flznML/sqlite-frozen-quiet`，是在三个后台容器全部退出后采集。
- 保留旧镜像精确 ID 和 `brand-review-base-20260908` 固定标签。无 `.git` 的 ECS 不使用要求 Git 的旧 rollback.sh。
- 代码回退先核对服务身份、冻结写入口并排空；将镜像标签恢复旧值，只重建应用，待健康后恢复原 nginx 路由和原后台容器。源码恢复仅限本轮精确文件，新模块移入回滚归档，不整包覆盖其他内容。
- 若蓝绿脚本保留候选以保障可用性，必须先检查真实路由与健康，不可盲目改回旧代理配置。
- 普通代码回退不恢复业务数据库。只有确认数据损坏并取得恢复授权后，才制定保留新增合法数据的专项恢复方案。
- 部署脚本清理了可重建的悬空镜像/构建缓存；正式旧镜像、业务数据与专项备份保留。

## 6. 证据位置与未完成事项

验收产物位于本发布工作树 `output/playwright/brand-review-v4/`：最终全门禁、Linux 测试、发布日志、受管回执、两端浏览器 JSON/截图、SQLite 前后证明、ECS 配置/卷/文件证明及观察 JSON。产物不纳入运行镜像；配置备份不进入 Git。

独立最终报告确认候选代码、版本/cache、本地静态整合与备份脚本修复 Critical/Required 为 0；未独立追认本轮运维辅助脚本、最终实际切换及部署回执。本文实际运行结论由本轮直接执行证据支持，不混称独立审查结果。隔离预检容器均已停止，未删除其他任务的预览服务。

未完成：真实 Gold、独立负责人批准、真实质量改善率、v4 allowlist 启用与新分析/人工裁决验收。当前自动解释能力仍限可确定核验的来源提及与双品牌样本提及；任意营销解释不会因模型多数一致或人工任意改写而自动发布。因此，用户截图中的历史拦截卡片不会因本次安装自动恢复。

运维安全残项：一次运行身份检查意外返回环境凭据，已停止输出环境值，后续只记录脱敏字段；凭据没有写入代码、发布包或回执。建议负责人轮换受影响密钥，本轮未擅自轮换。TLS/域名、Docker 旧构建器弃用提示属于独立基础设施事项。
