# MMN 2026-07-23 竞品格局与社媒可靠性发布交接

## 最新发布结论

- 最终版本：`beta-1.03-20260723-vertical-social-reliability-2`
- 应用运行提交：`f3b35738e2327026e65fe1e6d944325c54210844`
- GitHub：`codex/social-evidence-v2`、`main` 与最终版本标签已同步。
- ECS：`.deployed-git-commit` 与应用提交一致；六个 Compose 服务运行，五个核心服务健康。
- 竞品格局的完整关系由服务器权威保存，驾驶舱不得跨来源拼接排名、占比、趋势和周期。
- 图片导入必须经过人工确认；Linux OCR 只在完整结构成立时允许平台标题缺失的结果进入复核，不能直接写入正式资产。
- 生产同一张真实周榜图识别 10 行和连续自然周，确认按钮的禁用/启用反馈正确；正式竞品资产无变化，验收草稿和证据文件已清理。
- 本地 `610/610` 与完整发布门禁通过；生产桌面/移动 82 项通过，无运行时错误或失败请求。
- 数据迁移只新增空的 `vertical_image_reviews` 表，原有 76 张表逐表逻辑一致；最终 77 张表、草稿 0、`quick_check=ok`。
- 完整备份、逻辑清单、浏览器结果和主键回滚证据位于 `backups/releases/vertical_social_reliability_20260723_221333`。

---

## 发布结论

- 版本：`beta-1.03-20260723-cockpit-social-brand-1`
- 应用运行提交：`889b63167604b06792829eddebabb4bc01f5e6f7`
- 发布分支：`codex/social-evidence-v2`
- GitHub：应用提交已同步发布分支、`main` 和同名版本标签。
- 本地：`http://127.0.0.1:8765/` 已由本仓库守护进程运行，健康接口返回本版本和 `releaseDate=2026-07-23`。
- ECS：`/opt/mmn-perception-engine` 部署标记与应用提交一致；六个 Compose 服务运行，五个核心服务健康，Nginx 正常。

## 本次交付范围

1. 决策驾驶舱：全入口、标准/管理层上下文、数据切换、无数据清空、刷新恢复和桌面/移动布局完成回归，既有决策业务口径不变。
2. 社媒趋势中心：V2 关闭时自动读取既有权威快照；恢复快照自己的时间窗、竞品和平台；无快照显示可操作空态；新分析或导入会取消旧恢复请求的回写资格。
3. 品牌穿透中心：在同一冻结证据上完成三路独立复核，分别输出逐品牌结论与“本品 × 每个竞品”结论；逐项确定性融合，任一路或结构失败时失败关闭。
4. 客户输出：只显示中性 MMN 结论、共同证据和人工复核状态，不显示底层供应商、平行意见、原始异常或技术指纹。
5. 版本一致性：前端版本、服务端版本、静态资源和品牌内嵌页缓存令牌统一为本次发布版本。
6. 公开社媒证据 V2 代码随包交付，但生产开关与独立 Worker 保持关闭。

## 验证证据

- Python 完整回归：`586/586`。
- 全部 Node 合同、状态门禁、研发档案检查与 `git diff --check` 通过。
- 仓库完整发布门禁：161 项后端/契约、19 个客户入口、8 个管理视图、E7X 数据绑定、无数据清空、社媒分析/导入/自定义日期/风险与来源下钻全部通过。
- 发布门禁桌面与 390px：`failed=[]`、`runtimeErrors=[]`、`failedResponses=[]`。
- 本地 8765 真实流程：社媒趋势恢复 7 天、1 个竞品和 4 项指标；品牌穿透显示 6 张逐品牌卡、5 张逐竞品卡；驾驶舱可达，两端无横向溢出或控制台错误。
- 生产真实管理员流程：社媒趋势奥迪E7X恢复 7 天、4 项指标、3 个竞品和 5 条证据；品牌穿透显示 6 张逐品牌卡、5 张逐竞品卡与 24 个事件，点击奔驰对照后显示 4 个奔驰事件。桌面与移动端无横向溢出、运行时错误或失败请求。
- 生产代码：宿主机与实际 `mmn-app` 镜像中的 `app.js`、`server.py`、`social_trends.py`、`brand_penetration_analysis.py` 和品牌内嵌页 SHA-256 一致。

## 数据变更与保护

- 发布前数据库为 76 张表，`quick_check=ok`；代码部署后 76 张表逐表逻辑哈希完全一致。
- 生产原库缺少品牌穿透项目快照，因此按“只增不覆盖”新增 1 条已验证快照：ID `2f7912474cdcb62de6e76bcc`，194 条唯一证据、6 个品牌结论、5 个逐竞品结论。
- 生产浏览器验收产生 3 条临时 `project_snapshots` 和 9 条 `model_identity_assets` 字段刷新，已依据发布前数据库按主键精确回滚。
- 最终只有 `social_trend_snapshots` 从 9 增至 10；其余 75 张表逐表逻辑哈希与发布前一致，`project_snapshots=210`、`model_identity_assets=5789`、`quick_check=ok`。
- 本地发布验收造成的数据库变更也已恢复；商业库与社媒证据库均与本地发布前逐文件备份 SHA-256 一致。

## 备份与回滚

- 发布目录：`/opt/mmn-perception-engine/backups/releases/cockpit_social_brand_20260723_032724`
- 发布前源码：上述目录 `source_before.tar.gz`，SHA-256 `3efe86c399a729373cad4feff4c7bbc42bfc4006f69583d59da42afd73da8d95`
- 发布前环境：上述目录 `env_before`，SHA-256 `69d834bd972b3c32f04ac32c52221a21ffa661a1e75aa84618f19a09c1d22208`
- 发布前完整数据：`/opt/mmn-perception-engine/backups/mmn_backup_20260723_032725.tar.gz`，SHA-256 `e902898d5b753064c1c8de3208cc6d51dd8c86a003e636f02f5f109de4168f13`
- 部署脚本自动数据备份：`/opt/mmn-perception-engine/backups/mmn_backup_20260723_032833.tar.gz`，SHA-256 `2beb7e2ce777ba5e779042b9b4c4e025d91f9e3514729c63170556cdee2ba995`
- 发布目录保留部署前、部署后、最终清理后的数据库清单，以及 `browser_side_effect_rollback.json` 精确回滚记录。
- 回滚顺序：停止写入流量，恢复源码归档和环境文件，使用 `scripts/restore.sh` 恢复选定数据备份，再以 `MMN_SKIP_GIT_PULL=true bash scripts/deploy.sh` 重建；恢复后复核版本、六服务、数据库一致性和真实业务页面。

## 运行边界与后续维护

- 三路品牌结论必须保持同一冻结证据、独立首轮输出和确定性融合；未达门槛项目保持复核未完成或待人工复核，不得补造结论。
- “全量”只指所选时间窗和查询词下公开搜索接口可访问且分页穷尽的结果，不等于平台全库。
- 公开社媒证据 V2 和独立 Worker 当前关闭；启用前需单独执行容量、成本、限流、合规、数据迁移与回滚门禁。
- `mmnsh.com` 当前 HTTP 返回 403、HTTPS 握手失败，属于既有 ICP/TLS 外部阻断；本轮未修改域名或 Credo 路由。
- 本地 `data/` 备份/状态、日志、截图、`output/`、`tmp/`、用户文档及既有未跟踪文件均未提交、删除或同步服务器。
- ECS 目录没有 `.git`；应用通过可校验 Git 归档同步，`.deployed-git-commit` 记录实际运行提交，文档收口提交通过 `.deployed-handoff-commit` 单独记录。
