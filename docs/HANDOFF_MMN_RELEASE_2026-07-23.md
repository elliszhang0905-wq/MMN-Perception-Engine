# MMN 2026-07-23 综合发布交接

## 发布结论

- 版本：`beta-1.03-20260723-social-intelligence-1`
- 应用运行提交：`b4e75cf2a2d64cdf98f9e8f627639d346fdd6fe9`
- 发布分支：`codex/social-evidence-v2`
- GitHub：应用运行提交已同步发布分支与 `main`；本文件所在收口提交另行记录 handoff，不改变应用镜像内容。
- 本地：`http://127.0.0.1:8765/` 已重启为单实例，健康接口返回本版本和 `releaseDate=2026-07-23`。
- ECS：`/opt/mmn-perception-engine` 已部署应用运行提交，六个 Compose 服务运行，五个核心服务健康，Nginx 正常。

## 本次交付范围

1. 社媒趋势车型身份统一：本品与竞品互斥、竞品去重、默认展示全部已选车型，证据表不再默认只剩本品。
2. 时间窗公开内容采集：按车型规范名和别名持续分页，保留标题、描述、话题、查询词和命中字段，跨别名按平台内容 ID 去重。
3. 指标口径重构：相关内容量、正文正向率和风险均基于无热度门槛的相关池；热门门槛只控制排行榜。删除不透明的样本量“置信度”，改为采集完整性和分析覆盖率。
4. 三路交叉验证：三路读取同一冻结证据并独立审阅，只在 KPI 后、本竞品对比前发布一处中性 MMN 统一洞察；证据或任一路不足时条件发布或暂缓。
5. 公开社媒证据 V2 与 NSR 验证：代码和独立 Worker profile 随包交付，默认关闭，不把公开可访问范围表述为平台全库。
6. 周度市场刷新、集团看板和车型综合决策链路同步升级；移动端 19 个客户入口和管理层入口保持可达。
7. 生产门禁兼容 `playwright-core`，并修正版本发布日期。

## 验证证据

- Python 完整回归：`576/576`。
- 新增 V2、NSR 和全表面专项：`46/46`。
- 仓库完整发布门禁：前端语法、Node 合同、160 项后端发布专项、数据优先驾驶舱、19 个客户页面、8 个管理视图、E7X 绑定/缓存升级/无数据隔离全部通过。
- 桌面与 390px：`failed=[]`、`runtimeErrors=[]`、`failedResponses=[]`，页面级横向溢出为 0。
- 生产浏览器：在实际 `mmn-app` 镜像内，通过内部 Nginx 和真实管理员鉴权复跑同一门禁，结果全部通过。
- 生产运行：健康接口返回 `beta-1.03-20260723-social-intelligence-1`、`releaseDate=2026-07-23`、持久化检查 `ok`；发布后近 10 分钟日志未发现 Traceback、Exception、Fatal 或 Panic。
- 生产数据：验收产生的 7 条项目快照、2 条政策分析缓存、18 条车型身份复核写入和 1 条目录更新时间均已按发布前备份精确回滚；最终 76/76 张表按主键排序逐表一致，无新增、缺失或逻辑内容变化，`project_snapshots=210`，两端 `quick_check=ok`。

## 备份与回滚

- 发布目录：`/opt/mmn-perception-engine/backups/releases/social_intelligence_20260723_014820`
- 发布前源码：上述目录 `source_before.tar.gz`，SHA-256 `686a35b54d2f32fc81d9a1b6c39671a46f8657679e4e087cbf3686ae0aa457a0`
- 发布前环境：上述目录 `env_before`，SHA-256 `69d834bd972b3c32f04ac32c52221a21ffa661a1e75aa84618f19a09c1d22208`
- 最终生产验收前完整数据：`/opt/mmn-perception-engine/backups/mmn_backup_20260723_015828.tar.gz`
- 其他自动备份：`mmn_backup_20260723_014857.tar.gz`、`mmn_backup_20260723_014943.tar.gz`
- 回滚顺序：停止写入流量，恢复源码归档和环境文件，使用 `scripts/restore.sh` 恢复选定数据备份，再以 `MMN_SKIP_GIT_PULL=true bash scripts/deploy.sh` 重建；恢复后必须复核版本、六服务、数据库一致性和真实业务页面。

## 运行边界与后续维护

- “全量”仅指所选时间窗和查询词下，平台公开搜索接口可访问且分页穷尽的结果，不等于抖音、小红书或微博平台全库。
- 公开社媒证据 V2 生产开关和独立 Worker 当前保持关闭；启用前需单独执行容量、成本、限流、合规和回滚验收。
- 风险为 0 只有在采集完整且风险覆盖完整时才能解释为“该证据范围内未发现”；采集或分析不完整必须显示缺口。
- 本次部署后工作树出现的并行品牌传播穿透改动（`server.py`、`social_trends.py`、`brand_penetration_analysis.py` 及对应测试）未混入本次提交、GitHub、ECS 或本地运行进程；其状态原样保留，留给所属工作流独立验证和发布。
- 本地 `data/` 备份/状态、日志、截图、`output/`、`tmp/`、用户文档及编辑器锁文件均未提交、未删除、未同步服务器。
- ECS 服务器目录没有 `.git`；应用通过可校验 Git 归档同步，`.deployed-git-commit` 记录实际运行提交。文档收口提交使用独立 `.deployed-handoff-commit` 标记。
